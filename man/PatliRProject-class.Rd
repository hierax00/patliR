% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R
\docType{class}
\name{PatliRProject-class}
\alias{PatliRProject-class}
\title{The patliR project object}
\description{
\code{PatliRProject} is the single state-carrying object of the \code{patliR}
pipeline. It is deliberately split into two kinds of slots:
\itemize{
\item \strong{Structural slots} (\code{compounds}, \code{matrix_raw}, \code{binarized}) hold the
handful of tables that almost every family of functions depends on, and
whose shape is stable across the whole pipeline. These have a fixed
\code{data.frame} type and are checked by \code{\link[methods:validObject]{methods::validObject()}}.
\item \strong{The \code{results} slot} is a named \code{list} "bag" that holds every other,
more heterogeneous result the pipeline produces (ADME tables, target
predictions, one network per condition, bias audits, rankings, ...).
New families of functions (or future versions of \code{patliR}) can add new
named entries to \code{results} without ever requiring a change to the class
definition -- the same pattern
\code{SummarizedExperiment::SummarizedExperiment} uses for its \code{metadata()}
slot, applied here to the whole pipeline rather than to one assay.
}

None of this replaces the CSV files on disk, which remain the durable
source of truth (see \code{\link[=patliR_project]{patliR_project()}} and \code{\link[=patliR_load]{patliR_load()}}); the S4
object is the in-memory, self-validating, pipeable mirror of those files
for the duration of one R session.
}
\section{Slots}{

\describe{
\item{\code{project_dir}}{Character scalar. Path to the project directory where
every pipeline step writes its numbered CSV output (e.g.
\verb{01_compounds.csv}).}

\item{\code{cache_dir}}{Character scalar. Path to a directory for derived,
always-regenerable caches (e.g. the \code{.rds} built by
\code{\link[=refdb_rebuild_cache]{refdb_rebuild_cache()}}). Never required for the analysis to continue.}

\item{\code{compounds}}{A \code{data.frame} with one row per compound and columns
\code{id}, \code{pubchem_id}, \code{name}, \code{smiles}, \code{canonical_smiles}, \code{source}.
\code{canonical_smiles} (CDK's own canonical form, via \code{\link[=prep_compounds]{prep_compounds()}})
is \code{patliR}'s structure-identity key for deduplication and joins --
see the note in \code{.check_structures()} (internal) for why this is not
an InChIKey.}

\item{\code{matrix_raw}}{A \code{data.frame} with one row per compound and one column
per condition, holding the (averaged-over-replicates) raw abundance
values. Never binarized, kept so that later steps can still use
relative concentration.}

\item{\code{binarized}}{A \code{data.frame} with the same shape as \code{matrix_raw}, with
0/1 presence/absence calls.}

\item{\code{results}}{A named \code{list} holding every other pipeline result (see
Description). Access it with \code{\link[=patliRResults]{patliRResults()}}.}

\item{\code{log}}{A \code{data.frame} with columns \code{step}, \code{id}, \code{message}, \code{timestamp},
accumulating one row per notable event (dropped compound, fetch
failure, threshold used, ...) across every function that has touched
this project. This is the backbone of the fully-logged report produced
by \code{report_generate()}.}

\item{\code{version}}{Character scalar. The \code{patliR} package version that created
or last updated this object, for provenance.}
}}

\seealso{
\code{\link[=patliR_project]{patliR_project()}} to create a new project, \code{\link[=patliR_load]{patliR_load()}} to
reconstruct one from disk.
}
