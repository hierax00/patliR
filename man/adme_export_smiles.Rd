% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adme_import.R
\name{adme_export_smiles}
\alias{adme_export_smiles}
\title{Export compounds as a plain SMILES list, ready to paste into SwissADME
(or similar)}
\usage{
adme_export_smiles(proj, compound_ids = NULL, out_file = NULL)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id}, or \code{NULL}
(default) for every compound currently in \code{\link[=compounds]{compounds()}}.}

\item{out_file}{Character scalar, base path to write to (e.g.
\code{"chilcuague_smiles"}), or \code{NULL} (default) to only return the result
without writing anything. When given, writes \verb{<out_file>.txt} (the
plain SMILES list, ready to paste) and \verb{<out_file>_map.csv} (columns
\code{row_order}, \code{compound_id}, \code{name}, \code{smiles} -- \code{row_order} matches
the line number in the \code{.txt} file, i.e. the row order the platform's
own output will come back in).}
}
\value{
Invisibly, a \code{list(smiles_text, mapping)}: \code{smiles_text} the
full newline-joined SMILES list as a single string, \code{mapping} the
\code{data.frame} described above (also written to \verb{<out_file>_map.csv}
when \code{out_file} is given).
}
\description{
\code{adme_local()} covers Ro5/Veber/Ghose/Egan/Oprea and BOILED-Egg from
\code{rcdk} alone, no network needed -- but a platform like SwissADME
computes properties \code{patliR} does not (e.g. its own consensus LogP,
synthetic accessibility, P-gp substrate/CYP inhibition predictions) that
can only be brought in via \code{\link[=adme_import]{adme_import()}}. Those platforms take their
input as a pasted list of SMILES, one per line, in their own web form --
\code{patliR} does not scrape them (same policy as \code{\link[=adme_import]{adme_import()}}'s own
documentation) -- so this writes exactly that: one canonical SMILES per
line, in a fixed, deterministic order, plus a companion mapping file so
the platform's output (which is only ever ordered by input position, no
compound id of its own) can be matched back to \code{compound_id}/\code{name}
afterwards. Round-trip: \code{adme_export_smiles()} -> paste into the
platform -> download its export -> \code{\link[=adme_import]{adme_import()}}.
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
export <- adme_export_smiles(proj)
cat(export$smiles_text)
export$mapping

}
