% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adme_filter.R
\name{adme_filter}
\alias{adme_filter}
\title{Mark, and optionally remove, compounds by ADME rule}
\usage{
adme_filter(
  proj,
  rules = c("ro5", "veber", "ghose", "egan", "oprea", "route"),
  source = c("local", "imported"),
  hard_cutoff = FALSE,
  ask = interactive()
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{rules}{Character vector, any of \code{"ro5"}, \code{"veber"}, \code{"ghose"},
\code{"egan"}, \code{"oprea"}, \code{"route"}. \code{"oprea"} is the lead-likeness rule
(Oprea 2000) -- narrower than the drug-likeness rules, meant for
hit-to-lead triage rather than a final candidate; see \code{\link[=adme_local]{adme_local()}}
for every rule's exact criteria and citation. \code{"route"} expands to
every \verb{route_*} column \code{\link[=adme_local]{adme_local()}} computed (whichever routes you
asked for there).}

\item{source}{Currently only \code{"local"} (i.e. \code{\link[=adme_local]{adme_local()}}'s output) is
supported; \code{"imported"} will raise an informative error until a future
version adds it.}

\item{hard_cutoff}{Logical (default \code{FALSE}). If \code{TRUE}, compounds that
fail \emph{any} selected rule are removed from \code{\link[=compounds]{compounds()}} (see \code{ask});
if \code{FALSE}, nothing is removed -- the return value only adds the
\code{adme_filtered} marks.}

\item{ask}{Logical (default \code{interactive()}). Only used when
\code{hard_cutoff = TRUE}. If \code{TRUE}, prompts before removing anything
(\code{y}/\code{n}/\code{v} to view the list first). If \code{FALSE} (e.g. in a script),
the cut is applied without prompting, but full detail is always
written to \code{\link[=projectLog]{projectLog()}} regardless.}
}
\value{
The updated \code{proj}, with an \code{adme_filtered} entry in
\code{\link[=patliRResults]{patliRResults()}} in long format (columns \code{compound_id}, \code{rule},
\code{pass}), also written to \code{results/adme_filtered.csv}. If
\code{hard_cutoff = TRUE} and removal was confirmed (or \code{ask = FALSE}),
\code{\link[=compounds]{compounds()}} is also updated and re-written to \verb{01_compounds.csv}.
}
\description{
By default \code{adme_filter()} only \emph{marks} pass/fail per rule -- it never
removes a compound silently. Set \code{hard_cutoff = TRUE} to actually drop
failing compounds from \code{\link[=compounds]{compounds()}} going forward, which always shows
you what would be removed (and why) first.
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- adme_local(proj)
proj <- adme_filter(proj, rules = c("ro5", "veber"))
patliRResults(proj, "adme_filtered")

}
