% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adme_import.R
\name{adme_import}
\alias{adme_import}
\title{Import ADME properties exported from an external platform}
\usage{
adme_import(
  proj,
  path,
  platform = c("swissadme", "admetlab", "other"),
  column_map = NULL,
  mapping_file = NULL
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{path}{Path to the CSV file to import.}

\item{platform}{\code{"swissadme"}, \code{"admetlab"}, or \code{"other"}. Selects a
built-in \code{column_map} (see Details) so you do not have to write one by
hand for a supported platform's default export headers.}

\item{column_map}{Named character vector mapping \emph{your} file's column
names to patliR's property names, e.g.
\code{c(MW = "mw", Consensus.Log.Po.w = "logp")}. Required when
\code{platform = "other"}; ignored (a preset is used instead) for
\code{"swissadme"}/\code{"admetlab"} unless you explicitly pass one, in which
case yours takes precedence.}

\item{mapping_file}{Path to the \verb{<out_file>_map.csv} written by
\code{\link[=adme_export_smiles]{adme_export_smiles()}}, or \code{NULL} (default). When given, rows are
matched to \code{compound_id} \strong{by position} against that file (the
platform returns its output in input order), not by SMILES or CID --
the reliable way to reconcile an export when the platform's own
canonical SMILES need not match patliR's. Takes precedence over the
\code{platform} matching rule.}
}
\value{
The updated \code{proj}, with an \code{adme_imported} entry in
\code{\link[=patliRResults]{patliRResults()}} in long format (columns \code{compound_id}, \code{property},
\code{value}, \code{source}, \code{import_date}), also written to
\code{results/adme_imported.csv}. Rows that could not be matched to a known
compound are logged (\code{"adme_import_unmatched"}) and excluded.
}
\description{
Imports a CSV exported from a third-party ADME platform (SwissADME,
ADMETlab, pkCSM, ...). \code{patliR} never scrapes these platforms -- you run
them yourself in your browser, download the CSV they export, and this
function reconciles it against \code{\link[=compounds]{compounds()}}.

Different platforms key their export by different identifiers: the
bundled example for SwissADME uses \code{Molecule} (a PubChem CID), while the
ADMETlab example uses \code{smiles} directly. \code{adme_import()} reconciles
either against \code{\link[=compounds]{compounds()}} via a canonical-SMILES structure key
(recomputing it from \code{smiles} when needed -- see the note in
\code{.check_structures()}, internal, for why this is not an InChIKey), so it
never assumes a shared identifier across platforms.
}
\details{
Built-in presets (confirmed against the column headers real exports
actually use):
\itemize{
\item \code{platform = "swissadme"}: \code{Molecule} (PubChem CID, used to match
\code{compounds(proj)$pubchem_id}), \code{MW}, \code{Consensus.Log.Po.w} (-> \code{logp}),
\code{HBD}, \code{HBA}, \code{TPSA}, \code{GI.absorption}, \code{BBB.permeant},
\code{Pgp.substrate}, \code{Bioavailability.Score}.
\item \code{platform = "admetlab"}: \code{smiles} (canonicalized the same way
\code{\link[=prep_compounds]{prep_compounds()}} does, and matched against
\code{compounds(proj)$canonical_smiles} -- no shared numeric id with
SwissADME). Note: the bundled \code{import_tox_admetlab.csv} example is
toxicity data (see \code{\link[=tox_import]{tox_import()}}), not ADME -- there is no
ready-to-run ADMETlab \emph{ADME} example yet.
}
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- adme_import(
  proj,
  system.file("extdata", "import_adme_swissadme.csv", package = "patliR"),
  platform = "swissadme"
)
patliRResults(proj, "adme_imported")

}
