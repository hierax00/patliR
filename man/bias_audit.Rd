% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias_audit.R
\name{bias_audit}
\alias{bias_audit}
\title{Audit database bias -- MAD-based "promiscuous" compound/target outlier
flag}
\usage{
bias_audit(
  proj,
  check_homogeneity = TRUE,
  mad_threshold = 2.5,
  categories = NULL
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{check_homogeneity}{Logical, default \code{TRUE}. Must be \code{TRUE} in this
version -- \code{categories} (categorical enrichment) is not implemented yet,
so \code{FALSE} here would leave nothing to compute.}

\item{mad_threshold}{Single positive number, default \code{2.5} (the threshold
used in most applications of Leys et al. 2013). A compound/target with
\code{mad_score > mad_threshold} is flagged \code{"promiscuo"}.}

\item{categories}{\code{NULL} (default, the only implemented option). Any
other value raises an informative "not implemented yet" error -- see
the Description.}
}
\value{
The updated \code{proj}, with a \code{bias_homogeneity} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{id}, \code{tipo} (\code{"compound"} or \code{"target"}),
\code{frecuencia_global_refdb} (number of distinct targets/compounds this
id is associated with across the whole \code{reference_bioactivity} table),
\code{mad_score}, \code{categoria} (\code{"promiscuo"} or \code{"normal"})), also written
to \code{results/bias_homogeneity.csv}.
}
\description{
Flags compounds and targets whose degree in the project's own reference
database (\code{reference_bioactivity}, from \code{\link[=refdb_build]{refdb_build()}}) is a statistical
outlier on the high side -- i.e. compounds tested against an unusually
large number of targets, or targets hit by an unusually large number of
compounds, relative to every other compound/target actually present in
\code{reference_bioactivity}. This is the mechanism behind the "homogeneity"
finding in Zhang, Q. (2026), \emph{Frontiers in Pharmacology} 17:1748478 (the
same hub molecules/targets keep showing up across unrelated network
pharmacology studies) and quantified in Cell Genomics 2023 (PMC10363916):
in STRING, a node's degree alone predicts whether it is a known drug
target with AUC = 77.6\% -- curation bias, not necessarily real biological
relevance.

This function \strong{never removes anything} -- it only categorizes. Use
\code{\link[=bias_reweight]{bias_reweight()}} afterwards to get a log-ratio-adjusted score (never
replacing the raw one) for entries flagged \code{"promiscuo"}.
}
\section{Outlier rule}{

Uses the median-absolute-deviation method of Leys, C. et al. (2013),
\emph{Journal of Experimental Social Psychology} 49(4), 764-766 (\code{stats::mad()}
already applies the constant that makes it a consistent estimator of the
standard deviation under normality). Only the high side is flagged --
unlike a generic two-sided outlier test, an unusually \emph{low} degree is not
a promiscuity/bias concern, just a rarely-touched compound/target.
Compounds and targets are each compared only against their own kind (a
compound's degree is "how many distinct targets it was tested against";
a target's degree is "how many distinct compounds tested against it" --
the two are not on the same scale).
}

\section{What this does not do yet}{

\code{categories}/categorical enrichment against a disease-term background is
not implemented -- see the design note at the top of \code{R/bias_audit.R}
and \code{patliR_manual.md}, section "Planeado / no implementado", for why.
Passing a non-\code{NULL} \code{categories} raises an informative error rather
than pretending to succeed.
}

\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- refdb_build(proj, sources = c("pubchem", "chembl")) # needs internet
proj <- bias_audit(proj)
patliRResults(proj, "bias_homogeneity")
}

}
\seealso{
\code{\link[=bias_reweight]{bias_reweight()}}, \code{\link[=bias_report]{bias_report()}}.
}
