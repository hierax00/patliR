% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias_audit.R
\name{bias_report}
\alias{bias_report}
\title{Summarize the "promiscuous" compounds/targets found by the bias audit --
never a removal}
\usage{
bias_report(proj)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}
}
\value{
A \code{list} with two elements:
\itemize{
\item \code{summary}: a \code{data.frame} (columns \code{id}, \code{tipo}, \code{score_crudo},
\code{score_ajustado}, \code{categoria}), restricted to \code{categoria == "promiscuo"} and ordered by \code{score_ajustado} (ascending -- the most
heavily discounted, i.e. most promiscuous, entries first).
\item \code{note}: the fixed disclaimer string, always identical
regardless of the data.
}
}
\description{
Read-only summary over \code{\link[=patliRResults]{patliRResults()}}\verb{(proj, "bias_reweighted")},
following the same contract as \code{\link[=tox_report]{tox_report()}}: nothing is written to
\code{proj} or to disk, and a fixed disclaimer is always attached. The
\code{"promiscuo"} table is \strong{always present} in the returned \code{summary} (zero
rows, with a warning, if \code{\link[=bias_audit]{bias_audit()}}/\code{\link[=bias_reweight]{bias_reweight()}} have not run
yet) -- consistent with the rest of the package's "absence of a flag is
always visible, never just an omitted table" convention (see
\code{\link[=tox_safetyome]{tox_safetyome()}}).
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- refdb_build(proj, sources = c("pubchem", "chembl")) # needs internet
proj <- bias_audit(proj)
proj <- bias_reweight(proj)
report <- bias_report(proj)
report$summary
report$note
}

}
\seealso{
\code{\link[=bias_audit]{bias_audit()}}, \code{\link[=bias_reweight]{bias_reweight()}}.
}
