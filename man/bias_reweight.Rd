% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias_audit.R
\name{bias_reweight}
\alias{bias_reweight}
\title{Log-ratio-adjusted score for "promiscuous" compounds/targets}
\usage{
bias_reweight(proj)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}
}
\value{
The updated \code{proj}, with a \code{bias_reweighted} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{id}, \code{tipo}, \code{score_crudo},
\code{score_ajustado}, \code{categoria}), also written to
\code{results/bias_reweighted.csv}.
}
\description{
For every entry flagged \code{"promiscuo"} by \code{\link[=bias_audit]{bias_audit()}}, computes
\deqn{score_{adjusted} = degree \times \log(N_{universe} / degree)}
the same log-ratio formula \code{\link[=network_hub_penalty]{network_hub_penalty()}} uses at the
per-condition-network level, applied here at the whole-reference-database
level instead: \code{degree} is \code{frecuencia_global_refdb} (from
\code{\link[=bias_audit]{bias_audit()}}), and \code{N_universe} is the total number of distinct
targets (for a compound entry) or distinct compounds (for a target
entry) across the whole \code{reference_bioactivity} table -- recovered
directly from \code{\link[=patliRResults]{patliRResults()}}\verb{(proj, "bias_homogeneity")} (one row per
distinct id of each kind), with no need to re-read \code{reference_bioactivity}
itself. Entries not flagged \code{"promiscuo"} keep \code{score_ajustado == score_crudo} -- no penalty is applied where none was warranted.

\code{score_ajustado} is always a \strong{new column} -- \code{score_crudo} (the raw
\code{frecuencia_global_refdb}) is never overwritten or dropped, so any
downstream consumer can always fall back to the unadjusted count.
}
\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- refdb_build(proj, sources = c("pubchem", "chembl")) # needs internet
proj <- bias_audit(proj)
proj <- bias_reweight(proj)
patliRResults(proj, "bias_reweighted")
}

}
\seealso{
\code{\link[=bias_audit]{bias_audit()}}, \code{\link[=bias_report]{bias_report()}}, \code{\link[=network_hub_penalty]{network_hub_penalty()}} for the
same formula at per-condition-network scope.
}
