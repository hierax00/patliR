% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compounds_classify.R
\name{compounds_classify}
\alias{compounds_classify}
\title{Classify compounds into natural-product chemical families (NPClassifier)}
\usage{
compounds_classify(
  proj,
  compound_ids = NULL,
  fetch_mode = c("warn_and_cache", "abort")
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id}, or \code{NULL}
(default) for every compound currently in \code{\link[=compounds]{compounds()}}.}

\item{fetch_mode}{\code{"warn_and_cache"} (default) or \code{"abort"}. See
\code{.fetch_external()} (internal): this is enrichment
data, so failures do not stop the pipeline by default -- a compound
that cannot be classified gets \code{pathway = NA} and a log entry, not a
hard stop.}
}
\value{
The updated \code{proj}, with a \code{compounds_classified} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{compound_id}, \code{pathway}, \code{superclass},
\code{class}, \code{isglycoside}, \code{source}, \code{fetch_date}), also written to
\code{results/compounds_classified.csv}.
}
\description{
Queries the free, no-auth NPClassifier REST API
(\url{https://npclassifier.gnps2.org}, Kim, H.W. et al. (2021), "NPClassifier:
A Deep Neural Network-Based Structural Classification Tool for Natural
Products", \emph{J. Nat. Prod.} 84, 2795-2807, \doi{10.1021/acs.jnatprod.1c00399})
for every compound in \code{\link[=compounds]{compounds()}} (or a subset), by canonical SMILES.
This is what \code{\link[=plot_chemical_space]{plot_chemical_space()}}'s \code{color_by = "family"} uses to
color points by chemical family (alkaloids, terpenoids, phenylpropanoids,
...) -- the natural-product analogue of coloring by molecule category in
the classic "chemical space" figures (e.g. Reymond & Awale, 2012).
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- compounds_classify(proj) # needs internet
patliRResults(proj, "compounds_classified")
}

}
