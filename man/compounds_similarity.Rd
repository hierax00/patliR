% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compounds_similarity.R
\name{compounds_similarity}
\alias{compounds_similarity}
\title{Pairwise structural similarity between compounds}
\usage{
compounds_similarity(
  proj,
  compound_ids = NULL,
  fingerprint_type = c("standard", "extended", "circular", "maccs", "pubchem"),
  method = c("tanimoto", "dice", "cosine")
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id} to
restrict to, or \code{NULL} (default) for every compound with a valid
structure.}

\item{fingerprint_type}{One of \code{"standard"} (default, hashed path-based
fingerprint), \code{"extended"}, \code{"circular"}, \code{"maccs"}, \code{"pubchem"} --
passed straight to \code{rcdk::get.fingerprint(type = ...)}.}

\item{method}{One of \code{"tanimoto"} (default), \code{"dice"}, \code{"cosine"} --
passed straight to \code{fingerprint::fp.sim.matrix(method = ...)}.}
}
\value{
The updated \code{proj}, with a \code{compounds_similarity} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{compound_a}, \code{compound_b}, \code{similarity},
\code{fingerprint_type}, \code{method}; one row per unordered pair, self-pairs
excluded), also written to \code{results/compounds_similarity.csv}.
Compounds whose SMILES does not parse, or whose fingerprint could not
be computed, are excluded and logged -- never silently dropped from
view.
}
\description{
Computes a molecular fingerprint (via \code{rcdk::get.fingerprint()}) for
every compound and a pairwise similarity score (via
\code{fingerprint::fp.sim.matrix()}) between every pair. Purely structural
(2D fingerprints); says nothing
about shared targets or biological activity on its own -- pair this
with \code{\link[=network_degeneracy]{network_degeneracy()}} (pathway overlap) or \code{network_edges}
(shared targets) for that.
}
\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- compounds_similarity(proj)
patliRResults(proj, "compounds_similarity")
}

}
