% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disease_genes.R
\name{disease_genes_import}
\alias{disease_genes_import}
\title{Import a curated disease gene list into the \code{disease_genes} slot}
\usage{
disease_genes_import(
  proj,
  table,
  disease_id,
  disease_name = NULL,
  source = "manual",
  uniprot_col = NULL,
  gene_symbol_col = NULL,
  score_col = NULL
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{table}{A \code{data.frame}, or a path to a CSV file, with at least a
UniProt-ID column. A \code{gene_symbol} column and an
\code{association_score}/\code{score} column are used if present.}

\item{disease_id}{The disease identifier these genes belong to (e.g. an
EFO/MONDO ID, or any stable label). Becomes the \code{disease_id} that
\code{\link[=network_proximity]{network_proximity()}} is called with. Required.}

\item{disease_name}{Optional human-readable disease name, stored in the
\code{disease_name} column (\code{NA} if not given).}

\item{source}{Free-text provenance string for the \code{source} column (e.g.
\code{"OMIM"}, \code{"GWAS Catalog"}, \code{"DisGeNET_curated"}). Default \code{"manual"}.}

\item{uniprot_col, gene_symbol_col, score_col}{Optional explicit column
names in \code{table}. When \code{NULL} (default) they are auto-detected
(case/whitespace-insensitively) from the usual spellings
(\code{uniprot_id}/\code{uniprot}/\code{accession}/...; \code{gene_symbol}/\code{symbol}/\code{gene};
\code{association_score}/\code{score}).}
}
\value{
The updated \code{proj}, with the curated rows upserted into the
\code{disease_genes} slot (keyed on \code{disease_id}, so this replaces any
previous rows for the same disease), also written to
\code{results/disease_genes.csv}. Rows with a missing/empty UniProt ID are
logged (\code{"disease_genes_import_no_uniprot"}) and excluded.
}
\description{
The manual counterpart to \code{\link[=disease_genes_fetch]{disease_genes_fetch()}}, for curated
disease-gene lists exported from OMIM, the GWAS Catalog, DisGeNET, or a
hand-assembled table. Writes into the same \code{disease_genes} slot / schema
as \code{\link[=disease_genes_fetch]{disease_genes_fetch()}}, with \code{source} recording provenance and
\code{association_score} allowed to be \code{NA} (a curated list carries no Open
Targets score). Follows the shape of the other \verb{*_import()} functions in
the package (\code{\link[=adme_import]{adme_import()}}, \code{\link[=tox_import]{tox_import()}}, \code{\link[=targets_import]{targets_import()}}).
}
\section{UniProt accessions, not gene symbols}{

\code{table} must carry a column of UniProt accessions. A \code{gene_symbol} column
is accepted and stored if present, but \code{disease_genes_import()} does not
map symbols to accessions -- convert them first (UniProt's ID-mapping
tool, or \code{org.Hs.eg.db}). This keeps the import offline and deterministic
and avoids silently picking one accession per multi-mapping symbol.
}

\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
curated <- data.frame(
  uniprot_id = c("P37840", "P05067", "P10636"),
  gene_symbol = c("SNCA", "APP", "MAPT")
)
proj <- disease_genes_import(
  proj, curated,
  disease_id = "MONDO_0005180", disease_name = "Parkinson disease",
  source = "curated_demo"
)
patliRResults(proj, "disease_genes")

}
