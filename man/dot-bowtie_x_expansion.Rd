% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bowtie.R
\name{.bowtie_x_expansion}
\alias{.bowtie_x_expansion}
\title{Discrete x expansion that leaves room for the outside stratum labels}
\usage{
.bowtie_x_expansion(
  left_chars,
  right_chars,
  fig_width,
  char_in = 0.061,
  reserve_in = 2.3,
  offset = 1/8 + 0.03
)
}
\arguments{
\item{left_chars, right_chars}{Longest label (characters) on each side.}

\item{fig_width}{Figure width in inches.}

\item{char_in}{Average glyph width in inches (size-2.8 text ~0.061).}

\item{reserve_in}{Figure width not available to the panel (legend,
y-axis title, margins).}

\item{offset}{Axis units between an axis and the start of its labels.}
}
\value{
Numeric \code{c(left, right)} for \code{ggplot2::expansion(add = )}.
}
\description{
\code{plot_bowtie()} has two axes at x = 1, 2 and draws its labels outside
the strata (left of axis 1, right of axis 2), starting \code{offset} axis
units from each axis. With \code{e_l}/\code{e_r} the added expansion, the panel
spans \code{S = 1 + e_l + e_r} axis units over \code{panel_in} inches, and a
label \code{t} inches long fits when \code{(e - offset) * panel_in / S >= t}.
Writing \code{f = t / panel_in} for each side, the smallest such expansion
solves \code{e_l = offset + f_l * S}, \code{e_r = offset + f_r * S}, i.e.
\code{S = (1 + 2 * offset) / (1 - f_l - f_r)}. The label fractions are
capped at 0.8 together so the flows always keep some width.
}
\keyword{internal}
