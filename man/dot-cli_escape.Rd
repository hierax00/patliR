% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal.R
\name{.cli_escape}
\alias{.cli_escape}
\title{Escape literal curly braces so arbitrary text is safe to embed as a cli
bullet}
\usage{
.cli_escape(x)
}
\value{
Character scalar, brace-escaped.
}
\description{
\code{cli::cli_warn()}/\code{cli::cli_abort()} treat every bullet string as a
glue template -- any \code{{...}} inside it is parsed as an R expression to
evaluate, not shown literally. \code{.fetch_external()} (below) embeds
\code{conditionMessage()} from whatever error the external call raised --
text \code{patliR} does not control, e.g. a raw JSON fragment echoed back by
a failed API parse. A message containing \verb{\{}/\verb{\}} (real case: NPClassifier
returning \verb{"class_results": [...}) makes cli try to \code{parse()} that
fragment as R code and throw a confusing meta-error that fully masks
the real one. Doubling braces (\verb{\{} -> \verb{\{\{}, \verb{\}} -> \verb{\}\}}) is glue's own
escape convention for a literal brace -- this makes the substitution a
single, safe pass: the escaped text is inserted verbatim, never
re-parsed for further \code{{...}} expressions.
}
\keyword{internal}
