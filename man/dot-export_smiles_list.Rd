% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal.R
\name{.export_smiles_list}
\alias{.export_smiles_list}
\title{Export a compound set as a plain SMILES list plus a row-order bridge CSV}
\usage{
.export_smiles_list(cmp, out_file = NULL)
}
\arguments{
\item{cmp}{A \code{compounds(proj)} data frame, already subset to the
compounds to export.}

\item{out_file}{Base path, or \code{NULL} to only return the result.}
}
\value{
\code{list(smiles_text, mapping)}; \code{mapping} has columns \code{row_order},
\code{compound_id}, \code{name}, \code{smiles}.
}
\description{
Shared body of \code{\link[=adme_export_smiles]{adme_export_smiles()}} and \code{\link[=tox_export_smiles]{tox_export_smiles()}}: one
canonical SMILES per line in a fixed order, plus a \code{data.frame} mapping
each line back to \code{compound_id}/\code{name} (external platforms only ever
order their output by input position, with no id of their own).
}
\keyword{internal}
