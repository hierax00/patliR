% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal.R
\name{.fetch_external}
\alias{.fetch_external}
\title{Call an external resource with a documented, shared failure policy}
\usage{
.fetch_external(
  fetch_fun,
  cache_dir,
  cache_key,
  mode = c("abort", "warn_and_cache")
)
}
\arguments{
\item{fetch_fun}{A zero-argument function (typically a closure) that
performs the actual request and returns the parsed result.}

\item{cache_dir}{Directory to store/read the \code{.rds} cache for this
specific request.}

\item{cache_key}{Character scalar identifying this request (used to name
the cache file, and in log/error messages).}

\item{mode}{\code{"abort"}: no fallback, fail loudly if the request fails --
use this when there is no sensible way to continue without the data.
\code{"warn_and_cache"}: use the local cache if one exists, otherwise warn
and return \code{NULL} -- use this for optional enrichment steps that should
never break the pipeline.}
}
\value{
The fetched result, the cached result, or \code{NULL} (only reachable
with \code{mode = "warn_and_cache"}).
}
\description{
Every function in \code{patliR} that talks to an external API or web resource
(PubChem, ChEMBL, COCONUT, Open Targets, ...) goes through this helper,
so that failure handling is consistent and never silently swallowed.
}
\keyword{internal}
