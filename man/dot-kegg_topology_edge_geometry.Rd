% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pathway_network.R
\name{.kegg_topology_edge_geometry}
\alias{.kegg_topology_edge_geometry}
\title{Edge paths clipped at the label boxes, tee bars for inhibitions,
parallel offsets for straight reciprocal pairs}
\usage{
.kegg_topology_edge_geometry(e, nodes, bends = NULL)
}
\arguments{
\item{e}{Box-level edges (\code{from_box}, \code{to_box}, \code{effect}, \code{mode},
\code{subtypes}).}

\item{nodes}{Boxes with \code{box}, \code{x}, \code{y}, \code{hw}, \code{hh} (inches).}

\item{bends}{\code{data.frame(from, to, k, x, y)} of interior points (inches).}
}
\value{
\code{list(edges, paths, tees)}: \code{edges} one row per edge (\code{edge_id},
\code{from}, \code{to}, \code{effect}, \code{mode}, \code{subtypes}, \code{head}); \code{paths} the drawn
polyline points (\code{edge_id}, \code{ord}, \code{x}, \code{y} + the edge columns);
\code{tees} one bar per inhibition-type edge (\code{x}, \code{y}, \code{xend}, \code{yend}).
}
\description{
Edge paths clipped at the label boxes, tee bars for inhibitions,
parallel offsets for straight reciprocal pairs
}
\keyword{internal}
