% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_synergy.R
\name{.network_between_set_distance}
\alias{.network_between_set_distance}
\title{Menche et al. (2015) between-set distance <d_AB> (pooled, symmetric)}
\usage{
.network_between_set_distance(g, a, b, D = NULL)
}
\arguments{
\item{D}{Optional pre-computed distance matrix with dimnames covering
\code{ids} (so a caller can compute one \code{distances()} per condition and
every set/pair reads a submatrix).}
}
\value{
Single numeric, or \code{NA_real_} if either set is empty or any
distance is non-finite.
}
\description{
One \code{igraph::distances()} matrix, \code{apply(d, 1, min)} walking A -> B and
\code{apply(d, 2, min)} walking B -> A, denominator \code{nrow(d) + ncol(d)} on
the \strong{deduped} sets. Nodes in \verb{A n B} contribute \code{d = 0} on both sides
(intended -- Menche's \code{separation.py} does the same). Any non-finite
entry makes the whole quantity \code{NA_real_} (do not copy
\code{.network_closest_distance()}'s silent set-shrinking).
}
\keyword{internal}
