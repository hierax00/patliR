% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_centrality.R
\name{.network_bipartite_bmax}
\alias{.network_bipartite_bmax}
\title{Borgatti & Everett (1997) maximum betweenness for one mode of a
bipartite graph}
\usage{
.network_bipartite_bmax(n_mode, n_other)
}
\arguments{
\item{n_mode}{Size of the mode being normalised.}

\item{n_other}{Size of the opposite mode.}
}
\value{
A single numeric \code{B_max}, or \code{NA_real_} if either mode is empty.
}
\description{
\code{B_max} for the mode with \code{n_mode} nodes, given the opposite mode has
\code{n_other} nodes. Cross-checked 2026-08-28 against
\code{networkx.algorithms.bipartite.centrality.betweenness_centrality}
(which attributes the maxima to Borgatti & Everett). NetworkX halves its
raw scores before dividing by these maxima \emph{because} its base routine
counts ordered pairs; \code{igraph::betweenness()} on an undirected graph
already counts each unordered pair once, so the \code{0.5} here is the only
factor and the raw score is passed through unmodified.
}
\keyword{internal}
