% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_module_robustness.R
\name{.network_bipartite_membership}
\alias{.network_bipartite_membership}
\title{Remap \code{bipartite::computeModules()} membership back to node names}
\usage{
.network_bipartite_membership(mat, seed)
}
\value{
A list: \code{membership}, a named integer vector over
\code{c(rownames(mat), colnames(mat))} with \code{NA} for \code{empty()}-dropped
nodes; \code{qb}, Barber's Q_B (\code{mod@likelihood}); and \code{degenerate}, \code{TRUE}
when \code{computeModules()} found no usable partition (all nodes then in
one module).
}
\description{
\code{computeModules()} runs \code{bipartite::empty()} internally (drops zero-sum
rows/cols), so \code{module2constraints()} is indexed against the \strong{emptied}
matrix. The remap therefore uses \code{mod@originalWeb}'s dimnames, not
\code{c(rownames(mat), colnames(mat))} of the matrix passed in -- getting this
wrong shifts every assignment past the first dropped row. Nodes dropped
by \code{empty()} get \code{module_id = NA}.
}
\keyword{internal}
