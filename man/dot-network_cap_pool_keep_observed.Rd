% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_degeneracy.R
\name{.network_cap_pool_keep_observed}
\alias{.network_cap_pool_keep_observed}
\title{Cap a resampling pool to at most \code{cap} genes without ever evicting an
observed gene}
\usage{
.network_cap_pool_keep_observed(pool, observed, cap)
}
\arguments{
\item{pool}{Character vector of gene identifiers to cap.}

\item{observed}{Character vector of gene identifiers that must survive
the cap (duplicates and values absent from \code{pool} are fine, filtered
internally).}

\item{cap}{Integer, the maximum pool size.}
}
\value{
\code{list(pool, capped)}: \code{pool} is \verb{<= max(cap, length(observed))}
genes and always a superset of \code{intersect(observed, pool)}; \code{capped}
is \code{TRUE} iff the input pool was larger than \code{cap} (whether or not the
final size after guaranteeing \code{observed} still exceeds it).
}
\description{
\code{.network_degeneracy_direct()}'s \code{universe = "genome"} pool can hold
every annotated gene in \code{org.Hs.eg.db} (tens of thousands), which has to
be capped for the term x term similarity matrix to stay tractable. A
plain \code{sample(pool, cap)} caps blindly: since the pool is unrelated to
which genes any particular compound happens to target, it can (and on
real data did) evict a compound's own targets from the survivors --
silently dropping that compound from every pair it appears in. This
guarantees every gene in \code{observed} survives the cap, then fills the
remaining budget with a random sample of the rest of the pool.
}
\keyword{internal}
