% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_proximity.R
\name{.network_closest_distance}
\alias{.network_closest_distance}
\title{Guney et al. (2016) "closest" distance between two node sets on \code{g}}
\usage{
.network_closest_distance(g, source_ids, target_ids)
}
\value{
A single numeric (unweighted shortest-path hops, \code{weights = NA}
-- same rationale as \code{\link[=network_centrality]{network_centrality()}}: STRING's \code{combined_score}
is a confidence, not a distance).
}
\description{
\code{unique()} on both \code{source_ids} and \code{target_ids} before calling
\code{igraph::distances()}: igraph's underlying C routine rejects a \verb{to=}
argument with duplicate vertices. \code{.network_resample_matched()}
returns distinct nodes, but the observed sets can still contain a
repeat, and the "closest" measure only cares about the node \emph{set}, so
deduping is harmless.
}
\keyword{internal}
