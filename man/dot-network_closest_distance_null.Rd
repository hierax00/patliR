% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_proximity.R
\name{.network_closest_distance_null}
\alias{.network_closest_distance_null}
\title{Guney "closest" distance for every (compound, draw) of the proximity null}
\usage{
.network_closest_distance_null(g, s_rand, t_rand, max_cells = 1e+07)
}
\arguments{
\item{g}{The LCC \code{igraph} (undirected, unweighted hops).}

\item{s_rand}{List over compounds; each a list over the \code{n_random}
draws of integer vertex indices (the random \verb{S'}).}

\item{t_rand}{List over the \code{n_random} draws of integer vertex indices
(the random \verb{T'}, shared by every compound).}

\item{max_cells}{Upper bound on the cells of one chunk's distance block.}
}
\value{
List parallel to \code{s_rand}, each a numeric vector of length
\code{length(t_rand)} (the raw per-draw \code{d_random}, non-finite where the
draw had no finite path).
}
\description{
Batched, exact equivalent of calling \code{\link[=.network_closest_distance]{.network_closest_distance()}} once
per random draw -- \code{.network_closest_distance(g, V(g)$name[s_rand[[k]][[j]]], V(g)$name[t_rand[[j]]])} for every compound \code{k} and draw \code{j} -- which
costs \verb{|S'|} breadth-first searches per \verb{(k, j)} and dominated
\code{\link[=network_proximity]{network_proximity()}}'s run time on a real interactome (tens of
thousands of BFS runs per disease).
}
\details{
The "closest" measure only needs, for each source node \code{s}, the hop
distance from \code{s} to the \emph{nearest} member of the draw's target set
\verb{T'_j}: \verb{min_\{t in T'_j\} d(s, t)}. That is a multi-source BFS from
\verb{T'_j}, which gives the value for every node at once. It is run as a
single-source BFS from a virtual super-node: \code{g} is copied as a
directed graph (every undirected edge in both directions, matching
\code{distances()}'s default \code{mode = "all"}), and one extra vertex per draw
gets out-edges to that draw's \verb{T'_j} and no in-edges -- so no path can
pass through another draw's super-node -- and \code{distances(mode = "out")}
from it minus 1 is \verb{min_\{t in T'_j\} d(s, t)}, \code{Inf} where unreachable.
The random \verb{T'_j} is shared by every compound (drawn once per
\verb{(condition, disease)}), so the cost falls from
\verb{sum_k |S_k| * n_random} BFS runs to \code{n_random}.

Hop counts are integers, so the per-source minima are identical to the
old per-draw \code{distances()} matrix row minima; each draw's value is then
\code{mean()} over its unique, finite per-source minima -- the same
reduction, in the same function, as \code{\link[=.network_closest_distance]{.network_closest_distance()}}
(a source with no finite path to \verb{T'_j} is dropped; \code{NaN} if none has
one) -- so \code{d_random}, and every statistic built on it, is unchanged.

Memory: the draws are processed in chunks so that the
\verb{draws x needed-source-nodes} distance block stays at or below
\code{max_cells} doubles (default \code{1e7}, ~80 MB); only the columns of nodes
that actually occur in some \verb{S'} are materialised.
}
\keyword{internal}
