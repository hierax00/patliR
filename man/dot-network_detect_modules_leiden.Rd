% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_module_robustness.R
\name{.network_detect_modules_leiden}
\alias{.network_detect_modules_leiden}
\title{Leiden community detection on one connected component, unweighted}
\usage{
.network_detect_modules_leiden(g, resolution, n_iterations)
}
\value{
\code{list(members, types, log_message, warn_message, qb = NA_real_)}.
}
\description{
The \code{weight} edge attribute is \strong{deleted} (not passed as \code{weights = NA}): \code{igraph::cluster_leiden()} reads \code{E(g)$weight} via \code{strength()} for
the modularity null model even when \code{weights = NA} is passed, and any
\code{NA} weight then silently yields all singletons. \code{objective_function = "modularity"} is set explicitly because igraph's default (\code{"CPM"})
returns all singletons at \code{resolution = 1} on a sparse graph.
}
\keyword{internal}
