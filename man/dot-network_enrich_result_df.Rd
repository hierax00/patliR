% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_enrich.R
\name{.network_enrich_result_df}
\alias{.network_enrich_result_df}
\title{Build the per-row \code{network_enrichment} columns from one condition's
\code{as.data.frame(enrichResult)}, defensively.}
\usage{
.network_enrich_result_df(df, cond, db, universe)
}
\description{
Replaces a hard \code{df[, c(...)]} column subset, which threw \verb{undefined columns selected} whenever \code{clusterProfiler} omitted \code{qvalue} --
routine when the \code{qvalue} package's pi0 estimation fails on the small
p-value vectors a 10-50-gene target set produces (would otherwise abort
a run that had already spent minutes on GO/KEGG/Reactome). \code{ONTOLOGY} is
handled the same defensive way: \code{clusterProfiler} only adds it when
\code{ont = "ALL"}; keeping it present-but-\code{NA} otherwise (rather than
absent) matches the zero-row-invariant, stable-schema convention
\code{.empty_network_enrichment_row()} documents for this table.
}
\keyword{internal}
