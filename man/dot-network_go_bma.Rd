% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_degeneracy.R
\name{.network_go_bma}
\alias{.network_go_bma}
\title{BMA (and max/avg/rcmax) over a pooled term-similarity submatrix,
reproducing \code{GOSemSim::combineScores()} exactly -- including the
all-NA row/column drop, the vector / one-row / one-column -> \code{max}
branch, and the \code{round(., 3)} quantisation. Duplicate terms in
\code{terms_a} / \code{terms_b} are kept (each contributes a row/column), so the
BMA is annotation-weighted the same way \code{clusterSim()} is.}
\usage{
.network_go_bma(terms_a, terms_b, sim_mat, combine = "BMA", check = TRUE)
}
\description{
BMA (and max/avg/rcmax) over a pooled term-similarity submatrix,
reproducing \code{GOSemSim::combineScores()} exactly -- including the
all-NA row/column drop, the vector / one-row / one-column -> \code{max}
branch, and the \code{round(., 3)} quantisation. Duplicate terms in
\code{terms_a} / \code{terms_b} are kept (each contributes a row/column), so the
BMA is annotation-weighted the same way \code{clusterSim()} is.
}
\keyword{internal}
