% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_degeneracy.R
\name{.network_godata}
\alias{.network_godata}
\title{Build (and cache) a \code{GOSemSim::godata} object under \code{cacheDir(proj)}.}
\usage{
.network_godata(proj, ont, computeIC)
}
\value{
A \code{GOSemSimDATA} object, or \code{NULL} if \code{GOSemSim} is unavailable
or \code{godata()} errors (callers guard).
}
\description{
Factored so \code{\link[=network_enrich]{network_enrich()}} and \code{\link[=network_degeneracy]{network_degeneracy()}} share one
construction and one cache. Uses \code{annoDb = "org.Hs.eg.db"} (the
\verb{OrgDb =} spelling is deprecated in GOSemSim >= 2.36). The cache key
carries the \code{org.Hs.eg.db} and \code{GO.db} package versions so a Bioconductor
annotation update is not silently served a stale corpus. Safe to delete:
the \code{.rds} is a ~1 MB rebuild of a ~30 s computation.
}
\keyword{internal}
