% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_build.R
\name{.network_is_bipartite}
\alias{.network_is_bipartite}
\title{Is \code{g} a genuine two-mode (bipartite) graph, per \code{network_build()}'s own
construction contract?}
\usage{
.network_is_bipartite(g)
}
\value{
\code{logical(1)}.
}
\description{
Checks that every vertex carries a non-\code{NA} \code{type} and that \strong{no edge
connects two vertices of the same mode}. This is deliberately \emph{not}
\code{igraph::bipartite_mapping()}: that function (a) throws on a named graph
with a non-bipartite structure instead of returning \code{FALSE}, (b) reports
\code{TRUE} for any 2-colourable graph -- including the tri/quadripartite
\code{.network_layered_graph()} (compounds and pathways share a colour) and
any tree -- and (c) ignores an existing \code{V(g)$type}, computing its own
2-colouring, while \code{as_biadjacency_matrix()} trusts \code{V(g)$type} blindly.
The check here matches what the bipartite modularity path actually
requires: that \code{as_biadjacency_matrix(g)} is a faithful representation.
}
\keyword{internal}
