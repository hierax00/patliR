% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_layers.R
\name{.network_layered_graph}
\alias{.network_layered_graph}
\title{Build the directed compound-target-pathway-disease layered graph for one
condition}
\usage{
.network_layered_graph(proj, condition, pathway_db = NULL)
}
\arguments{
\item{pathway_db}{See \code{.network_target_pathway_edges()}, internal.}
}
\value{
An \code{igraph} directed graph. Every vertex has a \code{layer} attribute
(\code{"compound"}, \code{"target"}, \code{"pathway"}, or \code{"disease"}); every edge has
an \code{edge_kind} attribute (\code{"compound_target"}, \code{"target_pathway"},
\code{"target_disease"}, \code{"compound_pathway_derived"},
\code{"compound_disease_derived"}).
}
\description{
Combines three already-computed tables -- \code{\link[=network_build]{network_build()}}'s
\code{network_edges} (compound -> target), \code{\link[=network_enrich]{network_enrich()}}'s
\code{network_enrichment} (target -> pathway, reconstructed from the
\code{geneID} column of significant pathways for this condition), and
\code{\link[=targets_disease_filter]{targets_disease_filter()}}'s \code{targets_disease} (target -> disease) --
into one directed graph. Only \code{network_edges} is required; the pathway
and/or disease layers are silently omitted (not an error) if
\code{\link[=network_enrich]{network_enrich()}}/\code{\link[=targets_disease_filter]{targets_disease_filter()}} have not been run, since
this graph is meant to reflect whatever is actually available, not force
every consumer to have completed the whole pipeline.
}
\keyword{internal}
