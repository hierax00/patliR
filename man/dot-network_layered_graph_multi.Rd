% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network_layers.R
\name{.network_layered_graph_multi}
\alias{.network_layered_graph_multi}
\title{Build the directed compound-target-pathway-disease layered graph pooled
across one or more conditions}
\usage{
.network_layered_graph_multi(proj, conditions, pathway_db = NULL)
}
\arguments{
\item{pathway_db}{See \code{.network_target_pathway_edges()}, internal.}
}
\value{
An \code{igraph} directed graph, same vertex/edge attribute shape as
\code{.network_layered_graph()}.
}
\description{
Same edge-construction logic as \code{.network_layered_graph()}
(\code{network_layers.R}), generalized to pool \code{ct}/pathway/disease rows
across every condition in \code{conditions} before building a single graph,
instead of that function's one-condition-at-a-time contract (kept
as-is there since \code{\link[=network_motifs]{network_motifs()}} genuinely needs per-condition
motif counts -- this is a deliberate sibling, not a replacement).
A node/edge that appears in more than one condition is not duplicated:
\code{unique()} on the pooled tables collapses it to one.
}
\keyword{internal}
