% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network_layers.R
\name{.network_layers_module_colour_info}
\alias{.network_layers_module_colour_info}
\title{Join \code{\link[=network_module_robustness]{network_module_robustness()}}'s per-node \code{network_module_membership}
table onto a node table for \code{colour_by = "module"}, with a
\code{"not clustered"} grey fallback level}
\usage{
.network_layers_module_colour_info(proj, conditions, node_names)
}
\value{
\code{list(group, palette, legend_name = "Module")}, same shape as
\code{.network_layers_colour_info()}.
}
\description{
Shared by \code{\link[=plot_network_layers]{plot_network_layers()}} and \code{\link[=plot_network_degeneracy]{plot_network_degeneracy()}} (the
latter draws its base layout via the same \code{.network_layers_plot_data()}
pipeline, so a degeneracy pair's arc endpoints are coloured by the same
module assignments -- letting the reader see whether a degenerate pair
crosses a module boundary). Nodes present in the scope's graph but
absent from \code{network_module_membership} (e.g.
\code{network_module_robustness()} was never run, or the membership table
simply never covers a pathway/disease node -- it is only ever built
from the compound-target graph) fall into a grey \code{"not clustered"}
level rather than an \code{NA} colour. Pooled multi-condition scope: a node
built into more than one condition can carry a different \code{module_id}
per condition -- the first match (by row order in the table) wins; the
module label includes its condition when more than one is pooled. The
common case is a single condition in scope, where this is an exact
join.
}
\keyword{internal}
