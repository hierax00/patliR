% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network_layers.R
\name{.network_layers_subset}
\alias{.network_layers_subset}
\title{Split a hairball into a readable sub-network: restrict to some
compounds / one module, or to targets shared by several compounds}
\usage{
.network_layers_subset(
  proj,
  g,
  conditions,
  compound_ids = NULL,
  min_target_degree = 1,
  module_id = NULL
)
}
\value{
An \code{igraph} induced subgraph.
}
\description{
Applied after \code{.network_layers_filter()}. \code{compound_ids} drops every
other compound; \code{module_id} keeps only compound/target nodes that
\code{\link[=network_module_robustness]{network_module_robustness()}}'s \code{network_module_membership} assigns to
those module(s) in any condition in scope; \code{min_target_degree} drops
targets hit by fewer than that many distinct compounds (compound-target
edges only -- \code{2} keeps just the shared targets, which is what carries
the network's structure; singleton targets are the "dandelion" leaves).
Nodes left isolated by any of these are dropped too. Pathway/disease
nodes are kept while something still links to them (derived
compound-pathway/-disease edges included). With every argument at its
default the graph is returned unchanged.
}
\keyword{internal}
