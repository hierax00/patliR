% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_motifs.R
\name{.network_n_distinct_feedback_cycles}
\alias{.network_n_distinct_feedback_cycles}
\title{Number of DISTINCT feedback (3-cycle) loops in a \code{motif_type == "feedback"}
subset of \code{network_motifs()}'s output -- not the row count, which is
always exactly 3x this for real cycles (see \code{network_motifs()}'s own
docs)}
\usage{
.network_n_distinct_feedback_cycles(feedback_rows)
}
\arguments{
\item{feedback_rows}{A data frame with (at least) \code{node_a}, \code{node_b},
\code{node_c} columns, already restricted to \code{motif_type == "feedback"}
(zero rows is fine).}
}
\value{
\code{integer(1)}.
}
\description{
Number of DISTINCT feedback (3-cycle) loops in a \code{motif_type == "feedback"}
subset of \code{network_motifs()}'s output -- not the row count, which is
always exactly 3x this for real cycles (see \code{network_motifs()}'s own
docs)
}
\keyword{internal}
