% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_build.R
\name{.network_node_types}
\alias{.network_node_types}
\title{Node "mode" labels for a bipartite compound-target graph}
\usage{
.network_node_types(g)
}
\value{
\code{character} vector, one entry per vertex in \code{V(g)} order, each
\code{"compound"} or \code{"target"} (\code{NA} for a vertex whose \code{type} is \code{NA}).
}
\description{
The shared accessor for the compound/target split every mode-aware
\verb{network_*} function needs, so the \code{as.logical(V(g)$type)} idiom is not
re-derived (slightly differently each time) in
\code{.network_centrality_one()}, \code{network_hub_penalty()},
\code{network_module_robustness()}, etc. \code{V(g)$type == FALSE} is a compound,
\code{TRUE} is a target -- the convention \code{.network_build_igraph()} sets.
}
\keyword{internal}
