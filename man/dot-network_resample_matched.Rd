% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_proximity.R
\name{.network_resample_matched}
\alias{.network_resample_matched}
\title{Replace \code{string_ids} with an equal-size set of \emph{distinct} nodes drawn
from the same value bins (\code{\link[=.network_value_bins]{.network_value_bins()}}).}
\usage{
.network_resample_matched(string_ids, node_names, bins, bin_of_node)
}
\value{
Character vector of node IDs, length \code{length(string_ids)}.
}
\description{
Replace \code{string_ids} with an equal-size set of \emph{distinct} nodes drawn
from the same value bins (\code{\link[=.network_value_bins]{.network_value_bins()}}).
}
\details{
Nodes are grouped by their bin and, per bin, \code{k} distinct replacements
are drawn from that bin's pool -- so the returned set has exactly
\code{length(string_ids)} distinct nodes, matching the property-preserving
null (Guney et al.'s "a set of |S| proteins with matching degrees" for
\code{\link[=network_proximity]{network_proximity()}}; an annotation-count-matched gene set for
\code{\link[=network_degeneracy]{network_degeneracy()}}). Drawing each node independently with
replacement (and then \code{unique()}-ing downstream) would shrink the
random set, inflating its variance and shrinking \verb{|z_score|} in a way
that varies with set size.

\code{node_names} (the node identifiers, in the order \code{bins} indexes) and
\code{bin_of_node} (the bin index of every node, aligned to \code{node_names})
are passed in rather than rebuilt here: this function is called
\code{~2 * n_random} times per pair and both are invariant across every call.
\code{string_ids} must all be in \code{node_names} (the resampling pool) -- an
absent id would silently corrupt the output.
}
\keyword{internal}
