% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_proximity.R
\name{.network_string_lcc}
\alias{.network_string_lcc}
\title{STRING interactome largest connected component, with its degree bins}
\usage{
.network_string_lcc(proj, species, version, score_threshold, string_db = NULL)
}
\arguments{
\item{string_db}{Optional pre-constructed \code{STRINGdb} instance (avoids a
second \code{STRINGdb$new()} when the caller already built one); only used
on a cache miss.}
}
\value{
\code{list(graph, degree, bins, bin_of_node)} -- \code{graph} the LCC
\code{igraph}, \code{degree} its named degree vector, \code{bins} the list of
node-index vectors from \code{\link[=.network_degree_bins]{.network_degree_bins()}}, \code{bin_of_node} the
bin index of every node aligned to \code{names(degree)}.
}
\description{
Factored out of \code{\link[=network_proximity]{network_proximity()}} so that it and \code{\link[=network_synergy]{network_synergy()}}
operate on a byte-identical graph -- the Menche (2015) separation \code{s_AB}
and the Guney (2016) proximity \code{z} are only comparable (Cheng et al.
2019) when computed on the same interactome, at the same \code{species},
\code{version} and \code{score_threshold}.

Guney et al. (2016) and Menche et al. (2015) both restrict the STRING
graph to its largest connected component: a node outside it has no
finite distance to any module and would otherwise bias distances
downward or make \code{min()} warn.
}
\details{
Only the graph is cached, under
\verb{cacheDir(proj)/stringdb/lcc_<species>_<version>_<threshold>.rds}. The
degree, the degree bins and the per-node bin index are recomputed from
it on every load -- a future \code{min_per_bin} parameter would otherwise be
served a stale cached binning. Like \code{.network_stringdb()}'s downloaded
flat files, this \code{.rds} is \strong{safe to delete}: STRING's own flat files
under the same directory are the real cache, and deleting the \code{.rds}
just triggers one recompute of the component decomposition.
}
\keyword{internal}
