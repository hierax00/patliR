% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_bowtie.R
\name{.network_stringdb_actions_graph}
\alias{.network_stringdb_actions_graph}
\title{Download (if not already cached) and parse STRING's directed "actions"
file into an \code{igraph}, caching the parsed graph under
\verb{cacheDir(proj)/stringdb/}}
\usage{
.network_stringdb_actions_graph(proj, species, version)
}
\value{
\verb{list(graph = <directed igraph>)}.
}
\description{
Only rows where both \code{is_directional} and \code{a_is_acting} are true (see
\code{.network_actions_flag_true()}, internal -- STRING encodes these as
\code{"t"}/\code{"f"} strings, not \code{1}/\code{0}) become edges (\code{item_id_a -> item_id_b})
-- interactions STRING itself does not mark
as directional (e.g. plain physical \code{binding}) are excluded, since
including them as if they had a direction would fabricate information
the source data does not contain. \code{item_id_a}/\code{item_id_b} are already in
STRING's own \verb{<species>.ENSPxxxxxxx} ID space -- the same space
\code{STRINGdb$get_graph()}/\verb{$map()} use -- so no additional ID mapping is
needed to build this graph.
}
\keyword{internal}
