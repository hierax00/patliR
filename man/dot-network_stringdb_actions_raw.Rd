% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_bowtie.R
\name{.network_stringdb_actions_raw}
\alias{.network_stringdb_actions_raw}
\title{Download (if not already cached) and parse STRING's directed "actions"
file, caching the parsed raw table under \verb{cacheDir(proj)/stringdb/}}
\usage{
.network_stringdb_actions_raw(proj, species, version)
}
\value{
A \code{data.frame} with (at least) STRING's own \code{item_id_a},
\code{item_id_b}, \code{is_directional}, \code{a_is_acting} columns, and \code{score} when
the release provides it.
}
\description{
Just the download + parse + column-sanity-check, shared by
\code{.network_stringdb_actions_graph()} (below) and, independently,
\code{plot_target_chord()}'s own raw-score reader (same on-disk cache path,
\verb{<species>.protein.actions.v<version>.txt.gz}, read separately there
because it needs the per-edge \code{score} after the graph has already been
built). Caching the parsed data frame (not just the downloaded \code{.gz})
means a later call with a \emph{different} \code{score_threshold} never re-parses
the flat file, only re-filters an in-memory data frame.
}
\keyword{internal}
