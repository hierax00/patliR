% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_bowtie.R
\name{.network_stringdb_aliases_map}
\alias{.network_stringdb_aliases_map}
\title{UniProt -> STRING_id mapping table, read directly from STRING's own
\verb{protein.aliases.v<version>} flat file}
\usage{
.network_stringdb_aliases_map(proj, species, version)
}
\value{
A \code{data.frame(string_protein_id, alias)}, deduplicated, one row
per (STRING ID, alias) pair -- \code{alias} includes UniProt accessions
alongside gene symbols/Ensembl IDs/etc, so filtering to the accessions
of interest is a plain \code{\%in\%}/\code{match()}, no special-casing needed.
}
\description{
\code{STRINGdb$map()} performs this exact join (\code{multi_map_df()} against
\code{get_aliases()}, confirmed by reading \code{STRINGdb}'s own source) but doing
it via a \code{STRINGdb} object means instantiating one -- and
\code{STRINGdb$new(score_threshold = 0, ...)} downloads/loads the FULL
interactome (hundreds of MB, minutes) just to reach an alias table that
never depended on score at all. This reads the alias flat file directly
instead, same download-once/cache/parse pattern as
\code{.network_stringdb_actions_raw()}.
}
\keyword{internal}
