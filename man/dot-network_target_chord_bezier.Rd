% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_target_chord.R
\name{.network_target_chord_bezier}
\alias{.network_target_chord_bezier}
\title{Quadratic-Bezier arc points for \code{plot_target_chord()}'s arc diagram}
\usage{
.network_target_chord_bezier(edge_df, n_points = 40, height_scale = 0.35)
}
\arguments{
\item{edge_df}{Data frame with numeric \code{x0}, \code{x1} and integer \code{edge_id}.}

\item{n_points}{Points per arc (default \code{40}).}

\item{height_scale}{Arc height per unit of horizontal span (default
\code{0.35}).}
}
\value{
One row per (edge, point): every column of \code{edge_df} (constant
within an edge, repeated \code{n_points} times) plus \code{x}, \code{y}.
}
\description{
One arc per row of \code{edge_df} (needs numeric \code{x0}, \code{x1} and integer
\code{edge_id}, plus any extra columns to carry through unchanged to every
point of that edge's arc, e.g. \code{score}). Height is \code{height_scale * abs(x1 - x0)} -- an explicit, deterministic function of node span,
rather than relying on \code{geom_curve()}'s implicit curvature-to-chord-
length relationship (the idiom \code{\link[=plot_network_degeneracy]{plot_network_degeneracy()}}'s arc overlay
uses, on a 2-D force-directed layout where every edge spans roughly the
same screen distance; here spans range from 1, adjacent nodes, to n-1,
the two ends of the line, so a constant \code{geom_curve()} curvature would
draw a barely visible short-hop arc right next to a towering long-hop
one).
}
\keyword{internal}
