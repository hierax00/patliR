% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_layers.R
\name{.network_target_pathway_edges}
\alias{.network_target_pathway_edges}
\title{Reconstruct target(UniProt) -> pathway edges from a condition's
significant \code{network_enrichment} rows}
\usage{
.network_target_pathway_edges(proj, condition, uniprot_ids, pathway_db = NULL)
}
\arguments{
\item{pathway_db}{Character vector of \code{network_enrichment$db} values to
restrict to (any of \code{"go"}, \code{"reactome"}, \code{"kegg"}), or \code{NULL}
(default) for every \code{db} \code{\link[=network_enrich]{network_enrich()}} has been run with for this
condition. \code{network_enrich()} results accumulate across separate calls
with different \code{db} values (see its own docs) -- without this filter,
running \code{db = "go"} and \code{db = "kegg"} for the same condition silently
mixes both into the pathway layer. Worth restricting to \code{"kegg"} alone
in practice: KEGG's gene sets are far coarser/fewer than GO's, so a
\code{compound -> pathway} layer built only from KEGG has a much smaller
derived-edge fan-out per compound than one that includes GO -- a
direct, no-tuning alternative to \code{\link[=network_enrich]{network_enrich()}}'s own \code{simplify_go}
for keeping \code{\link[=network_motifs]{network_motifs()}}/\code{\link[=network_degeneracy]{network_degeneracy()}}/
\code{\link[=plot_network_layers]{plot_network_layers()}} from turning into an unreadable hairball on a
real, densely-annotated target set (confirmed against real Chilcuague
data, 2026-08-25).}
}
\value{
\code{data.frame(uniprot_id, pathway_id)}, or \code{NULL} if
\code{network_enrich()} was never run for this condition (with a matching
\code{db}, if \code{pathway_db} was given), or the mapping packages are not
installed (same optional \code{Suggests} as \code{\link[=network_enrich]{network_enrich()}} --
silently omitting the pathway layer here is consistent with this being
a best-effort enrichment of the graph, not a hard requirement).
}
\description{
\code{network_enrich()} stores \code{geneID} as a \code{/}-separated list of \emph{Entrez}
IDs per significant pathway (\code{clusterProfiler}'s own convention -- see
\code{network_enrich()}), so this maps the condition's target UniProt IDs to
Entrez the same way \code{network_enrich()}/\code{network_degeneracy()} do, then
checks which of a pathway's member Entrez IDs are among them.
}
\keyword{internal}
