% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_build.R
\name{.network_upsert}
\alias{.network_upsert}
\title{Replace rows of an existing \code{patliRResults()} table that share a key with
\code{new_rows}, keeping every other existing row untouched}
\usage{
.network_upsert(proj, name, new_rows, key_cols)
}
\arguments{
\item{key_cols}{Character vector of column names that jointly identify a
"slot" to replace (e.g. \code{"condition"}, or \code{c("condition", "method")}).}
}
\value{
\code{new_rows} with any previously-existing, non-overlapping rows of
\code{patliRResults(proj, name)} prepended.
}
\description{
The shared "rebuild a subset without nuking the rest" pattern already
used ad hoc in \code{\link[=network_build]{network_build()}} -- factored out here so every later
\verb{network_*} function (most of which are per-condition, some per
\verb{(condition, method)} or \verb{(condition, db)}) gets it for free and
consistently, instead of re-deriving slightly different upsert logic
per function.
}
\keyword{internal}
