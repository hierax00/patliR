% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_proximity.R
\name{.network_value_bins}
\alias{.network_value_bins}
\title{Contiguous-value bins over a numeric node property -- the pool each
random resampling draws from, per node, to preserve that property.}
\usage{
.network_value_bins(values, min_per_bin = 100)
}
\arguments{
\item{values}{Named or unnamed numeric vector, one entry per node, in
the node order the caller will align \code{bins} against.}
}
\value{
A list of integer vectors (indices into \code{values}), one per bin.
}
\description{
Contiguous-value bins over a numeric node property -- the pool each
random resampling draws from, per node, to preserve that property.
}
\details{
Generic over any per-node numeric value: \code{\link[=network_proximity]{network_proximity()}} passes
node degree (Guney et al. 2016, \emph{Nat Commun} 7:10331 -- the
degree-preserving null), \code{\link[=network_degeneracy]{network_degeneracy()}} passes each gene's GO
annotation count. Bins are built from \emph{consecutive values}, a bin
extended until it holds at least \code{min_per_bin} (default 100) nodes. This
keeps bins fine-grained where the values are dense and only coarsens in
a sparse tail -- unlike fixed quantile bins, where on a heavy-tailed
distribution a single decile spans an enormous range and a
high-value node can be swapped for a low-value one, destroying the
property preservation exactly where the bias it corrects for is
strongest.
}
\keyword{internal}
