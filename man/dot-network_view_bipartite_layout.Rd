% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network_layers.R
\name{.network_view_bipartite_layout}
\alias{.network_view_bipartite_layout}
\title{Two-column layout of the bipartite view}
\usage{
.network_view_bipartite_layout(
  edges,
  targets,
  compounds,
  module = NULL,
  compound_module = NULL,
  gap = 3
)
}
\arguments{
\item{edges}{The drawn compound-target rows.}

\item{targets}{The drawn targets (\code{uniprot_id}, \code{n_compounds},
\code{mean_weight}).}

\item{compounds}{Every compound id to place.}

\item{module}{\code{NULL} or a named character vector (\code{uniprot_id} ->
module); \code{NA} counts as \code{"not clustered"}.}

\item{compound_module}{\code{NULL} or a named character vector (compound id
-> module), same convention.}
}
\value{
\code{list(compounds = data.frame(name, bary, module, x, y), targets = targets + module, x, y)}, both sorted top to bottom.
}
\description{
Targets on the right (\code{x = 1}), grouped by module (largest module on
top, \code{"not clustered"} last, a gap of \code{gap} slots between groups) and,
within a group, by decreasing \code{n_compounds}, then \code{mean_weight}.
Compounds on the left (\code{x = 0}), evenly spaced and ordered by the mean
height of their targets (barycentric ordering -- the classic
crossing-reduction heuristic for two-layer drawings; Sugiyama et al.
1981), so each compound faces the block of targets it mostly hits.
When \code{compound_module} is given, compounds are first grouped in the
same module order as the targets (barycentric order within a group),
so a module's compounds face its target block. Compounds without any
drawn target go last within their group.
}
\keyword{internal}
