% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network_layers.R
\name{.network_view_collapse}
\alias{.network_view_collapse}
\title{Collapse a compound-target table to the targets shared by >= k compounds}
\usage{
.network_view_collapse(ct, min_shared = NULL, max_targets = 40)
}
\arguments{
\item{ct}{\code{data.frame(compound_id, uniprot_id, weight)}, as
\code{.network_layers_ct_table()} returns.}

\item{min_shared}{\code{NULL} (automatic \code{k}) or a single integer >= 1.}

\item{max_targets}{Target budget for the automatic \code{k}.}
}
\value{
\verb{list(k, n_compounds, targets = data.frame(uniprot_id, n_compounds, mean_weight, drawn), edges = the drawn rows of ct, badges = data.frame(compound_id, n_targets, n_unique, n_drawn, n_hidden))}. \code{targets} is sorted by \code{n_compounds}, then \code{mean_weight}
(both decreasing), then id.
}
\description{
The core of the simplified views. For every target, \code{n_compounds} is
the number of distinct compounds hitting it; targets with
\code{n_compounds >= k} are "drawn", the rest are only counted. With
\code{min_shared = NULL}, \code{k} is the smallest value >= 2 that leaves at most
\code{max_targets} drawn targets (the largest possible \code{k} if even that
leaves more; \code{1} when there is a single compound). Per compound, the
badge counts are \code{n_targets} (all its targets in scope), \code{n_unique}
(targets no other compound in scope hits), \code{n_drawn} and \code{n_hidden = n_targets - n_drawn}.
}
\keyword{internal}
