% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network_layers.R
\name{.network_view_compound_layout}
\alias{.network_view_compound_layout}
\title{Deterministic radial layout of the compound-centric view}
\usage{
.network_view_compound_layout(col, compound_order, r_min = 0.2, r_max = 0.78)
}
\value{
\code{list(compounds = data.frame(name, x, y, angle), targets = col$targets[drawn] + x, y, shell_r)}.
}
\description{
Compounds sit on the unit circle in \code{compound_order}. Each drawn target
goes on a concentric shell whose radius encodes how many compounds hit
it (all of them: innermost shell; exactly \code{k}: outermost, radius
\code{r_max}). Within a shell, targets are ordered by the (probability
weighted) circular mean angle of the compounds hitting them and spaced
evenly, rotated to stay as close as possible to those angles -- a
target sits on the side of the compounds it links to, and no two
targets overlap.
}
\keyword{internal}
