% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets_disease.R
\name{.open_targets_map_id}
\alias{.open_targets_map_id}
\title{Resolve a free-text term or existing ID to a canonical Open Targets ID}
\usage{
.open_targets_map_id(term, entity = c("target", "disease"))
}
\value{
A character scalar (the resolved ID), or throws if there is no hit.
}
\description{
Uses the \code{mapIds} endpoint (the same full-text mapping that backs Open
Targets' own search box), restricted to one entity type at a time
(\code{"target"} or \code{"disease"}) so an ambiguous term cannot silently resolve
to the wrong entity kind. Takes the top-ranked hit -- \code{mapIds} returns
hits already ordered by relevance, same as their \code{search()} endpoint.
}
\keyword{internal}
