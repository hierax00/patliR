% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets_disease.R
\name{.open_targets_target_top_diseases}
\alias{.open_targets_target_top_diseases}
\title{Query a target's top-N associated diseases (explore mode), or one named
disease with its name resolved (single-disease mode)}
\usage{
.open_targets_target_top_diseases(ensembl_id, top_n = NULL, efo_id = NULL)
}
\arguments{
\item{ensembl_id}{Open Targets target (Ensembl gene) ID.}

\item{top_n}{Requested page size when \code{efo_id} is \code{NULL} (explore mode --
\code{associatedDiseases} is already returned in descending-score order by
Open Targets, so a plain first-\code{top_n} page is the top-N).}

\item{efo_id}{\code{NULL} (explore mode) or a single resolved EFO/MONDO ID
(single-disease mode, \code{Bs} filters server-side to just that one disease,
same as \code{\link[=.open_targets_target_disease_score]{.open_targets_target_disease_score()}} but with \code{disease.name}
also requested).}
}
\value{
A \code{data.frame(disease_id, disease_name, association_score, evidence, rank)}, or \code{NULL} if Open Targets reports nothing.
}
\description{
Query a target's top-N associated diseases (explore mode), or one named
disease with its name resolved (single-disease mode)
}
\keyword{internal}
