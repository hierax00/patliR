% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal.R
\name{.parse_percent_column}
\alias{.parse_percent_column}
\title{Parse a numeric column that may be a plain fraction (\code{0.92}) or a
percentage (\code{"96.55\%"}, \code{"85"}), always returning a \verb{0..1} fraction}
\usage{
.parse_percent_column(x)
}
\arguments{
\item{x}{Character or numeric vector.}
}
\value{
Numeric vector in \verb{0..1} (\code{NA} where unparseable), same length
as \code{x}.
}
\description{
Real target-prediction exports mix conventions: some give probability
as a \verb{0..1} fraction, others as a \code{"96.55\%"} string (fails
\code{\link[=as.numeric]{as.numeric()}} outright because of the \verb{\%}), others as a bare \code{85}
meaning 85\%. Strips \verb{\%} and any other non-numeric decoration first,
then rescales to \verb{0..1} if the column looks like it was on a \verb{0..100}
scale (either it had a literal \verb{\%} somewhere, or its max parsed value
is \verb{> 1}) -- decided once per column, not per row, so one column never
ends up a mix of both scales.
}
\keyword{internal}
