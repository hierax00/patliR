% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patliR_project.R
\name{.patliR_compound_id_colclass}
\alias{.patliR_compound_id_colclass}
\title{\code{compound_id} in \verb{02_matrix_raw.csv}/\verb{03_binarized.csv} must survive
\code{\link[=patliR_load]{patliR_load()}} as \code{character} for the same reason as
\code{.patliR_compounds_colclasses()} -- these two tables otherwise have
one column per experimental \emph{condition} (user-chosen names,
\code{check.names = FALSE} on purpose), so only the one column name common
to every project can be pinned generically here.}
\usage{
.patliR_compound_id_colclass
}
\description{
\code{compound_id} in \verb{02_matrix_raw.csv}/\verb{03_binarized.csv} must survive
\code{\link[=patliR_load]{patliR_load()}} as \code{character} for the same reason as
\code{.patliR_compounds_colclasses()} -- these two tables otherwise have
one column per experimental \emph{condition} (user-chosen names,
\code{check.names = FALSE} on purpose), so only the one column name common
to every project can be pinned generically here.
}
\keyword{internal}
