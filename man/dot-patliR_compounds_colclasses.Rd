% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patliR_project.R
\name{.patliR_compounds_colclasses}
\alias{.patliR_compounds_colclasses}
\title{Identifier columns of \verb{01_compounds.csv} that must survive
\code{\link[=patliR_load]{patliR_load()}} as \code{character}, never re-typed by \code{read.csv()}'s value
inference}
\usage{
.patliR_compounds_colclasses
}
\description{
All six are validated as required by \code{\link[=PatliRProject-class]{PatliRProject}} and every
one of them can look purely numeric for a given project and so get
silently re-typed on reload if not pinned: \code{pubchem_id} is usually all
digits (becomes \code{integer}, dropping any leading zero a source database
used), and \code{name}/\code{smiles}/\code{canonical_smiles}/\code{source} are free text
that \emph{happens} to contain no compound named e.g. \code{"TRUE"}/\code{"2e5"} only
by chance for a given dataset -- \code{read.csv()} infers a column's type
from the values actually present, not from any contract on what the
column is supposed to hold. \code{id} (\code{"C0001"}-style, see
\code{.next_compound_ids()}) never collides with a purely-numeric parse
because of its letter prefix, but is included here too so this list is
the full, load-bearing schema rather than "whatever happened not to
need it yet".
}
\keyword{internal}
