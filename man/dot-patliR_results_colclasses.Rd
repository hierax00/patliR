% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patliR_project.R
\name{.patliR_results_colclasses}
\alias{.patliR_results_colclasses}
\title{Columns of any \verb{results/*.csv} that are character \emph{by contract} and must
not be re-typed by \code{utils::read.csv()}'s value inference}
\usage{
.patliR_results_colclasses
}
\description{
Most character results columns hold non-numeric-looking values
(\code{"C0001"}, \code{"P12345"}, \code{"EFO_0000537"}, \code{"P2"}, \code{"leiden"}) and survive
a \code{read.csv()} round-trip unchanged. A few do not: \code{string_version}
holds \code{"12.0"} / \code{"11.5"}, which \code{read.csv()} reads back as a numeric,
and \code{condition} is a user-chosen label that could be all digits. When
such a column is later merged by \code{\link[=network_build]{network_build()}}'s \code{.network_upsert()}
against a freshly-built (still character) column of the same name, the
type mismatch aborts the upsert. Pinning them here fixes it at the read
boundary; \code{read.csv()} silently ignores names not present in a given
file, so one shared vector covers every results CSV.
}
\keyword{internal}
