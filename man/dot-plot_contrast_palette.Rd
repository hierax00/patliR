% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-helpers.R
\name{.plot_contrast_palette}
\alias{.plot_contrast_palette}
\title{Qualitative palette in which neighbouring colours never look alike}
\usage{
.plot_contrast_palette(n, palette = c("contrast", "grey", "default"))
}
\arguments{
\item{n}{Number of colours.}

\item{palette}{\code{"contrast"}, \code{"grey"} or \code{"default"}.}
}
\value{
Character vector of \code{n} distinct hex colours.
}
\description{
For figures that place categories side by side (e.g. the term arcs and
ribbons of \code{\link[=plot_gochord]{plot_gochord()}}), where a hue ramp such as
\code{\link[grDevices:rainbow]{grDevices::rainbow()}} makes adjacent categories nearly identical.
\describe{
\item{\code{"contrast"}}{12 colours from the Okabe-Ito and Tol
colour-blind-safe sets, ordered so consecutive colours differ as much
as possible in lightness and hue (smallest CIELAB distance between
neighbours > 80). Beyond 12, the cycle repeats darkened, then
lightened, so every colour stays distinct.}
\item{\code{"grey"}}{\code{n} greys (for black-and-white print) spread from near
black to near white and interleaved dark, light, dark, light, so two
neighbours are always about half the grey range apart.}
\item{\code{"default"}}{\code{\link[grDevices:rainbow]{grDevices::rainbow()}} -- the previous behaviour.}
}
}
\keyword{internal}
