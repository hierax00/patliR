% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-helpers.R
\name{.plot_finish}
\alias{.plot_finish}
\title{Save a \verb{plot_*} figure, upsert its log row, and attach the updated
project}
\usage{
.plot_finish(
  proj,
  p,
  name,
  filename,
  log_row,
  key_cols,
  engine = NULL,
  save = TRUE,
  out_dir = NULL,
  width = 8,
  height = 6,
  dpi = 150,
  static = p
)
}
\arguments{
\item{proj}{A \code{PatliRProject}.}

\item{p}{The plot object to return (a \code{ggplot}, or already a \code{girafe}
is never passed -- the wrap happens here).}

\item{name}{Results-bag slot name for the log, e.g.
\code{"centrality_plot_log"}.}

\item{filename}{Basename of the file to write inside \code{out_dir}.}

\item{log_row}{One-row \code{data.frame}. If it has a \code{path} column that
value is overwritten with the resolved path (column position kept);
otherwise a \code{path} column is appended.}

\item{key_cols}{Passed straight to \code{\link[=.network_upsert]{.network_upsert()}}.}

\item{engine}{\code{NULL} / \code{"static"} -> return \code{p}; \code{"ggiraph"} -> return
\code{girafe(ggobj = p)}.}

\item{save}{Logical; when \code{FALSE} nothing is written and \code{p} (or its
\code{girafe} wrapper) is returned without the \code{proj} attribute.}

\item{out_dir}{\code{NULL} (default -> \code{file.path(projectDir(proj), "plots")})
or an explicit directory.}

\item{width, height, dpi}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{static}{Plot object actually handed to \code{ggsave()} -- defaults to
\code{p}, but \code{\link[=plot_network_layers]{plot_network_layers()}} / \code{\link[=plot_network_degeneracy]{plot_network_degeneracy()}} pass a
separately rendered static plot because their interactive \code{p} does not
rasterise well.}
}
\value{
\code{p} (or its \code{girafe} wrapper), with \code{attr(., "proj")} set when
\code{save = TRUE}.
}
\description{
The tail every \verb{plot_*} function shares: when \code{save = TRUE}, resolve
\code{out_dir}, \code{ggsave()} the figure, upsert a one-row log into the
\code{name} results slot (keyed by \code{key_cols}), write that slot's CSV; then
wrap the plot in \code{girafe()} for \code{engine = "ggiraph"} and stamp
\code{attr(result, "proj")} with the updated project.
}
\keyword{internal}
