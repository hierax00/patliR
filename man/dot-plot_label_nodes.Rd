% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-helpers.R
\name{.plot_label_nodes}
\alias{.plot_label_nodes}
\title{Look up display labels for a set of network nodes}
\usage{
.plot_label_nodes(proj, conditions, ids, layer)
}
\arguments{
\item{proj}{A \code{PatliRProject}.}

\item{conditions}{Character vector of conditions in scope (used to
restrict the pathway-description lookup).}

\item{ids}{Character vector of node IDs.}

\item{layer}{Character vector, recycled against \code{ids}: \code{"compound"},
\code{"target"}, \code{"pathway"} or \code{"disease"} per node.}
}
\value{
Character vector, same length/order as \code{ids}.
}
\description{
Formerly \code{.network_layers_labels()} (it lived in \code{plot_network_layers.R},
which four other plot files \verb{@include}d purely to reach it). Resolves
compound IDs to \code{compounds()$name}, UniProt IDs to gene symbols (via
\code{clusterProfiler::bitr()} + \code{org.Hs.eg.db}, when both are installed),
and pathway IDs to \code{network_enrichment$Description}; anything that does
not resolve is left as its raw ID.
}
\keyword{internal}
