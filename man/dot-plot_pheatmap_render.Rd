% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmap.R
\name{.plot_pheatmap_render}
\alias{.plot_pheatmap_render}
\title{Render a compound x target/condition matrix with \code{pheatmap()}, save/log
it, and attach the updated project}
\usage{
.plot_pheatmap_render(
  proj,
  mat,
  title,
  save,
  out_dir,
  width,
  height,
  log_name,
  log_row,
  key_cols,
  filename
)
}
\arguments{
\item{mat}{Numeric matrix to plot.}

\item{log_row}{One-row \code{data.frame} for the log table; a \code{path} column
(if present) is overwritten with the resolved path.}

\item{key_cols}{Passed to \code{\link[=.network_upsert]{.network_upsert()}} for the log upsert.}
}
\value{
The \code{pheatmap} object, with \code{attr(., "proj")} set when
\code{save = TRUE}.
}
\description{
The rendering tail shared by \code{\link[=plot_heatmap]{plot_heatmap()}} and \code{\link[=plot_rank]{plot_rank()}}'s
\code{view = "heatmap"} -- both need "hierarchical clustering on both axes,
cell values printed, save as PDF, log the path" and differ only in how
\code{mat}/\code{title}/\code{filename} are built. Requires \code{pheatmap} (checked by the
caller, not here, so the caller's error message can name itself).
}
\keyword{internal}
