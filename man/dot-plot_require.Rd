% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-helpers.R
\name{.plot_require}
\alias{.plot_require}
\title{Check plotting dependencies and resolve the drawing engine}
\usage{
.plot_require(engine = NULL, extra = character())
}
\arguments{
\item{engine}{\code{NULL} (the function has no \code{engine} argument, e.g.
\code{plot_bowtie()}) or a single already-\code{match.arg()}-ed engine string.
When \code{"ggiraph"} and \code{ggiraph} is not installed, a warning is emitted
and \code{"static"} is returned instead.}

\item{extra}{Character vector of additional package names the plot needs
(e.g. \code{"ggalluvial"}); each is checked with the same message shape as
the \code{ggplot2} check.}
}
\value{
The resolved engine (invisibly unchanged when \code{engine} is \code{NULL}
or already \code{"static"}; possibly downgraded from \code{"ggiraph"}).
}
\description{
The \code{ggplot2} availability check plus the \code{engine = "ggiraph"} ->
\code{"static"} fallback that every \verb{plot_*} function opens with. The name of
the calling function is picked up from the call stack so the error
message still reads \verb{plot_<name>()}.
}
\keyword{internal}
