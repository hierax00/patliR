% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-helpers.R
\name{.plot_wrap}
\alias{.plot_wrap}
\title{Wrap title / subtitle / caption text to a maximum line length}
\usage{
.plot_wrap(x, width = 100)
}
\arguments{
\item{x}{Character vector (\code{NULL}/\code{NA} pass through unchanged).}

\item{width}{Maximum characters per line -- typically
\code{.plot_wrap_width()} of the figure's width.}
}
\value{
Character vector, same length as \code{x}.
}
\description{
ggplot2 never wraps \code{labs()} text, so a long subtitle runs off the right
edge of the saved PNG. Each element of \code{x} is wrapped with
\code{\link[=strwrap]{strwrap()}} at \code{width} characters; explicit \code{"\\n"} line breaks already
in the text are kept (each line is wrapped on its own).
}
\keyword{internal}
