% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ppi_network.R
\name{.ppi_layout}
\alias{.ppi_layout}
\title{Force-directed layout of the drawn PPI graph, scaled into the unit disc}
\usage{
.ppi_layout(ids, ppi, layout = "fr", min_dist = 0.15)
}
\value{
Numeric matrix, one row per \code{ids}, columns x/y.
}
\description{
Each connected component is laid out on its own and the components are
packed with \code{\link[igraph:merge_coords]{igraph::merge_coords()}}, so small components do not drift to
the edge of the figure. The result is centred, its short axis stretched
by the square root of the aspect ratio, scaled so that the farthest node
lies at radius 1, radially stretched (\code{r^0.6}) to open up the dense core
that force-directed layouts give hub-rich PPI graphs, and decluttered
(\code{\link[=.ppi_declutter]{.ppi_declutter()}}). With
\code{layout = "kk"} edge lengths follow \verb{1000 / STRING score}.
}
\keyword{internal}
