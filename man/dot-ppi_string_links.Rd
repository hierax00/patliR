% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ppi_network.R
\name{.ppi_string_links}
\alias{.ppi_string_links}
\title{STRING \code{protein.links} at a score threshold, cached}
\usage{
.ppi_string_links(proj, species, version, score_threshold)
}
\value{
\code{data.frame(protein1, protein2, combined_score)}.
}
\description{
Reads \verb{<species>.protein.links.v<version>.txt.gz} from
\verb{cacheDir(proj)/stringdb/} (downloading it once when absent, like
\code{\link[=network_proximity]{network_proximity()}}'s \code{STRINGdb} object does), keeps the links with
\code{combined_score >= score_threshold}, one row per undirected pair, and
caches that table as \verb{ppi_links_<species>_<version>_<threshold>.rds}. A
cached table at a lower threshold is filtered instead of re-reading the
flat file. Safe to delete: the flat file is the real cache.
}
\keyword{internal}
