% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_candidates.R
\name{.rank_export_structures}
\alias{.rank_export_structures}
\title{Write the top-N ranked compounds' structures to an \code{.sdf}/\code{.smi} file}
\usage{
.rank_export_structures(proj, compound_ids, condition, top_n, export)
}
\value{
The path written to, invisibly.
}
\description{
Reuses the same \code{.parse_smiles_safe()} identity chain
\code{\link[=compounds_similarity]{compounds_similarity()}} already relies on -- no new dependency, no
re-lookup of chemical identity. \code{rcdk::write.molecules()} always emits an
SDF mol block regardless of the output filename's extension (verified
directly against the installed \code{rcdk} before writing this) -- so
\code{export = "smi"} is built by hand from \code{rcdk::get.smiles()} instead of
delegating to \code{write.molecules()}.
}
\keyword{internal}
