% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_candidates.R
\name{.rank_pareto_front}
\alias{.rank_pareto_front}
\title{From-scratch non-dominated sort (NSGA-II-style fronts)}
\usage{
.rank_pareto_front(mat)
}
\arguments{
\item{mat}{Numeric matrix, rows = compounds, columns = criteria.}
}
\value{
Integer vector, length \code{nrow(mat)}, the front/tier of each row
(\code{1} = non-dominated).
}
\description{
\code{mat} must already be direction-normalised (every column higher = better)
with no \code{NA} (the caller substitutes \code{-Inf} for a missing value -- same
"missing = worst" convention as the RRA imputation). Front \code{1} = the
non-dominated set (no other row is at-least-as-good on every column and
strictly better on at least one); front \code{2} = non-dominated after
removing front \code{1}; etc. \code{O(n^2)} per front -- fine at the candidate-list
scale \code{rank_candidates()} targets (tens to a few hundred rows); not
meant for anything larger.
}
\keyword{internal}
