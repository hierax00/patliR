% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_candidates.R
\name{.rank_rollup_target_to_compound}
\alias{.rank_rollup_target_to_compound}
\title{Roll up a target-level value to compound level via the compound's own
predicted targets}
\usage{
.rank_rollup_target_to_compound(
  target_values,
  edges_cond,
  compound_ids,
  roll_up
)
}
\arguments{
\item{target_values}{Named numeric vector, names = \code{uniprot_id}.}

\item{edges_cond}{\code{data.frame(compound_id, uniprot_id, weight)} for one
condition.}
}
\value{
Named numeric vector over \code{compound_ids}.
}
\description{
For each compound, looks up its targets and their \code{\link[=network_build]{network_build()}} edge
weights (import probability) in \code{edges_cond}, drops targets with no
value in \code{target_values}, and combines the rest per \code{roll_up}. A
compound with no targets carrying a value gets \code{NA} (worst-rank
imputation happens later, at the ranking step, not here).
}
\keyword{internal}
