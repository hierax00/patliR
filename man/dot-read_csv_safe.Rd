% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal.R
\name{.read_csv_safe}
\alias{.read_csv_safe}
\title{Read a CSV from an external/user-provided file, without the cosmetic
"incomplete final line" warning drowning out real problems}
\usage{
.read_csv_safe(path)
}
\value{
A \code{data.frame}, as \code{\link[utils:read.csv]{utils::read.csv()}} with
\verb{stringsAsFactors = FALSE, check.names = FALSE}.
}
\description{
Real exports from third-party platforms (SuperPred, SwissADME, ...)
routinely lack a trailing newline on the last line -- harmless (R still
reads every row correctly), but \code{\link[utils:read.csv]{utils::read.csv()}} raises a warning for
it every time, indistinguishable at a glance from a warning that
actually matters. This wraps \code{\link[utils:read.csv]{utils::read.csv()}} and muffles only that
specific warning, letting every other warning (malformed quoting,
embedded nulls, ...) through unchanged.
}
\keyword{internal}
