% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adme_local.R
\name{.smiles_ring_count_approx}
\alias{.smiles_ring_count_approx}
\title{Count ring-closure digit pairs in a SMILES string (approximate ring count)}
\usage{
.smiles_ring_count_approx(s)
}
\value{
Numeric scalar (ring count), \code{NA_real_} if \code{s} is empty.
}
\description{
Per the SMILES specification, a ring bond is written as a digit (or a
\verb{\%nn} two-digit escape) appearing exactly twice in the string -- once at
each ring-closure atom. Counting closure tokens and halving gives the
ring count for well-formed SMILES. Bracket contents (\verb{[...]}, e.g.
isotope labels like \verb{[13C]} or explicit H counts) are stripped first so
those digits are never mistaken for ring closures.
}
\section{What this does not handle}{

Only correct for well-formed SMILES that follow the digit-pair
convention; does not attempt real graph-theoretic SSSR ring perception
(\code{rcdk} has no high-level wrapper for that -- see the "Radar-chart
descriptors" note in \code{\link[=adme_local]{adme_local()}} for why a CDK descriptor column was
not guessed instead). Good enough for \code{oprea_pass}'s ring-count
criterion, not for anything needing an exact SSSR count.
}

\keyword{internal}
