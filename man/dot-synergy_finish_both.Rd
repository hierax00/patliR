% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_synergy.R
\name{.synergy_finish_both}
\alias{.synergy_finish_both}
\title{Save/log both \code{plot_synergy()} figures and chain the returned \code{proj}}
\usage{
.synergy_finish_both(
  proj,
  p_main,
  p_sab,
  scope_label,
  engine,
  save,
  out_dir,
  width,
  height,
  dpi
)
}
\description{
\code{.plot_finish()} is written for one figure; \code{plot_synergy()} produces
two (main scatter + \code{s_AB} companion). Runs it twice, feeding the \code{proj}
that the first call attached (when \code{save = TRUE}) into the second, so
both log rows land in the same project -- then re-attaches the final
\code{proj} to the main result, which is the only one returned to the caller.
}
\keyword{internal}
