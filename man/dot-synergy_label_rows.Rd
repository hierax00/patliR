% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_synergy.R
\name{.synergy_label_rows}
\alias{.synergy_label_rows}
\title{Pairs to label in \code{plot_synergy()}}
\usage{
.synergy_label_rows(dat, top_n)
}
\arguments{
\item{dat}{Plot data with \code{panel}, \code{is_p2}, \code{synergy_score}, \code{s_ab},
\code{za_plot}, \code{zb_plot}.}

\item{top_n}{Pairs per panel; \code{0} labels nothing.}
}
\value{
Subset of \code{dat} with a \code{label_kind} column.
}
\description{
Per panel: the \code{top_n} \code{P2} (Complementary Exposure) pairs by
\code{synergy_score} (descending, \code{NA} last), \code{label_kind = "P2"}. A panel
with no \code{P2} pair at all instead labels its \code{top_n} pairs nearest to
\code{P2} -- both compounds individually proximal (\code{z < 0}) and the highest
\code{s_ab} (closest to topological separation) -- as \code{label_kind = "nearest"}, so the figure still names the most relevant pairs.
}
\keyword{internal}
