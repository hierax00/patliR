% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_target_chord.R
\name{.target_chord_actions_scores}
\alias{.target_chord_actions_scores}
\title{Best-effort per-edge STRING actions \code{score}, read directly from the
cached raw flat file}
\usage{
.target_chord_actions_scores(proj, species, version)
}
\value{
\code{data.frame(string_a, string_b, score)} (undirected, deduplicated
by keeping the strongest of any multiple action "modes" between the
same pair), or \code{NULL} if the raw file is not on disk, unreadable, or
missing the expected columns.
}
\description{
\code{\link[=network_bowtie]{network_bowtie()}}'s \code{.network_stringdb_actions_graph()} (internal)
downloads and caches this exact file but drops its \code{score} column when
building the parsed graph. Modifying that shared function was out of
scope for this plot (see the "Arc colour" section of
\code{plot_target_chord()}'s own docs); this makes a second, independent pass
over the same cached file, when it is still on disk, reusing
\code{\link[=network_bowtie]{network_bowtie()}}'s own \code{.network_actions_flag_true()} for the
direction filter rather than re-deriving it.
}
\keyword{internal}
