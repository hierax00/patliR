% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_target_chord.R
\name{.target_chord_label_x}
\alias{.target_chord_label_x}
\title{Spread label anchors along the baseline so no two are closer than
\code{min_gap}}
\usage{
.target_chord_label_x(x, min_gap, lower, upper)
}
\arguments{
\item{x}{Numeric node positions.}
}
\value{
Numeric label positions, same order as \code{x}.
}
\description{
\code{plot_target_chord()} orders nodes by ascending degree, so the labeled
(highest-degree) targets are always the rightmost, adjacent ticks --
drawn at their own x their labels pile into one smear. Each label is
moved the least needed to keep \code{min_gap} between neighbours (a forward
pass pushing right, then -- if that overshoots \code{upper} -- a backward pass
pulling left); order is preserved, so leader lines from node to label
never cross. If \code{min_gap} cannot fit at all, labels are spaced evenly
over \verb{[lower, upper]}.
}
\keyword{internal}
