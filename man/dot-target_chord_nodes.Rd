% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_target_chord.R
\name{.target_chord_nodes}
\alias{.target_chord_nodes}
\title{Node placement + label selection for \code{plot_target_chord()}'s arc diagram}
\usage{
.target_chord_nodes(targets, edge_df, top_n_labels, labels)
}
\arguments{
\item{targets}{Character vector of UniProt ids, one node each.}

\item{edge_df}{\code{data.frame} with (at least) \code{uniprot_a}, \code{uniprot_b}
columns -- e.g. \code{.target_chord_edges()}'s output.}

\item{top_n_labels}{Integer >= 0.}

\item{labels}{Character vector, same length/order as \code{targets} -- e.g.
\code{\link[=.plot_label_nodes]{.plot_label_nodes()}}'s output.}
}
\value{
\code{data.frame(uniprot_id, x, degree, label)}, one row per target,
ordered by \code{x} (ascending degree).
}
\description{
Every one of \code{targets} gets a node, placed by ascending degree (within
\code{edge_df}, i.e. only counting edges among the plotted targets), ties
broken alphabetically by \code{uniprot_id} -- deterministic given the same
input, no unseeded randomness anywhere in this plot. The \code{top_n_labels}
highest-degree targets (same tie-break) get their pre-computed \code{labels}
entry; the rest get \code{NA} (drawn as an unlabeled tick by the caller).
}
\keyword{internal}
