% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pathway_network.R
\name{kegg_routes}
\alias{kegg_routes}
\title{Routes along KEGG relations from the extract's targets to disease genes}
\usage{
kegg_routes(
  proj,
  condition = NULL,
  disease = NULL,
  to = NULL,
  from = NULL,
  compounds = NULL,
  max_len = 6,
  top_n = 10,
  n_alternatives = 3,
  max_per_gene = 2,
  min_hops = 1,
  rank_by = c("destination", "pair"),
  undirected_binding = TRUE
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character scalar with rows in \code{network_kegg_topology};
\code{NULL} (default) when only one condition has topology.}

\item{disease}{\code{NULL} or a \code{disease_id} of
\code{patliRResults(proj, "disease_genes")}. One of \code{disease} / \code{to} is
required.}

\item{to}{\code{NULL} or a character vector of destination genes (UniProt
accessions, KEGG gene IDs such as \code{"hsa:5594"}, or gene symbols).
When both are given, destinations are the disease genes in \code{to}.}

\item{from}{\code{NULL} (default, every target of the extract in the
topology) or a character vector restricting the sources (same ID types
as \code{to}).}

\item{compounds}{\code{NULL} (default) or \code{compounds(proj)$id} values: only
targets of these compounds are sources.}

\item{max_len}{Integer, default \code{6}: longest route (hops) considered.}

\item{top_n}{Integer, default \code{10}: number of (source, destination) pairs
returned.}

\item{n_alternatives}{Integer, default \code{3}: equally short routes kept
per pair.}

\item{max_per_gene}{Integer, default \code{2}.}

\item{min_hops}{Integer, default \code{1}: shortest route length kept (e.g.
\code{2} to look only at mechanisms through at least one intermediate gene).}

\item{rank_by}{\code{"destination"} (default) or \code{"pair"}, see the section
above.}

\item{undirected_binding}{Logical, default \code{TRUE}.}
}
\value{
A \code{data.frame}, one row per route, best first: \code{route_id}
(\code{"R1"}, \code{"R1b"}, ...), \code{pair_rank}, \code{alternative}, \code{source},
\code{source_symbol}, \code{compounds}, \code{n_compounds}, \code{source_max_weight},
\code{destination}, \code{destination_symbol}, \code{destination_score}, \code{n_hops},
\code{path} (symbols with the hop effects, e.g. \code{"EGFR --> GRB2 --- SOS1"}),
\code{path_kegg}, \code{sign}, \code{pathways} (KEGG IDs the hops belong to),
\code{pathway_titles}, \code{score}. Attributes: \code{hops} (one row per hop: route,
hop number, genes, effect, sign, pathways of the hop), \code{direct_hits}
(targets that are destinations themselves), \code{graph_size}.
}
\description{
"How do the extract's targets reach the disease?" -- a route planner
over the directed gene-gene relations KEGG draws inside its pathways
(as stored by \code{\link[=network_kegg_topology]{network_kegg_topology()}}). All pathways of \code{condition}
are merged into one directed graph (a gene pair seen in several
pathways becomes one edge that remembers them all). Starting points
("sources") are the extract's predicted targets; end points
("destinations") are the genes of a disease gene set (\code{disease}) or an
explicit list (\code{to}). For every reachable (source, destination) pair the
shortest directed route is found (breadth-first search); the best pairs
are kept, each with up to \code{n_alternatives} equally short alternatives.
}
\section{Relations, direction and sign}{

Each KGML relation keeps its subtype (activation, inhibition,
phosphorylation, expression, ...). A hop is \code{+1} for activation /
expression, \code{-1} for inhibition / repression and unknown otherwise
(phosphorylation, indirect effect, binding, ...: KEGG does not state
whether they activate). The route sign is the product of its hops:
\code{"+"} (the source, when active, promotes the destination), \code{"-"}
(represses it), or \code{"?"} when any hop is unsigned. The sign describes the
relation chain, not what a compound does to its target (which is
generally unknown). Binding/association relations have no direction and
can be travelled both ways (\code{undirected_binding = TRUE}).
}

\section{Ranking}{

Only shortest routes are considered, of \code{min_hops} to \code{max_len} hops.
Each (source, destination) pair gets
\deqn{\mathrm{score} = \frac{w_s \, \log_2(1 + c_s) \, a_d}{\mathrm{hops}}}
with \eqn{w_s} the highest predicted probability among the compounds
hitting the source, \eqn{c_s} the number of those compounds and
\eqn{a_d} the destination's Open Targets association score (1 when not
available, e.g. with \code{to}).
\itemize{
\item \code{rank_by = "destination"} (default) answers "how does the extract
reach the genes that matter most for the disease": destinations are
taken by decreasing association score (ties: best pair score), each
with its best-scoring route.
\item \code{rank_by = "pair"} takes pairs by decreasing score -- the shortest,
best-supported routes overall (typically one-hop neighbours).
}

In both, a source is used at most \code{max_per_gene} times (and, with
\code{"pair"}, a destination too), so the result is not dominated by one hub.
Among equally short alternatives, those passing through more of the
extract's other targets come first. Sources that are themselves
destinations need no route; they are returned in
\code{attr(., "direct_hits")}.
}

\examples{
\dontrun{
proj <- network_kegg_topology(proj, condition = "FLO-ET", restrict_to_network = FALSE)
proj <- disease_genes_fetch(proj, "hypertension")
r <- kegg_routes(proj, condition = "FLO-ET", disease = "MONDO_0005044")
kegg_routes_table(r)
}
}
\seealso{
\code{\link[=plot_kegg_routes]{plot_kegg_routes()}}, \code{\link[=network_kegg_topology]{network_kegg_topology()}}, \code{\link[=network_proximity]{network_proximity()}}
}
