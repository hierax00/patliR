% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch_app.R
\name{launch_app}
\alias{launch_app}
\title{Launch the patliR Shiny wizard}
\usage{
launch_app(...)
}
\arguments{
\item{...}{Passed on to \code{\link[shiny:runApp]{shiny::runApp()}} (e.g. \code{launch.browser}, \code{port}).}
}
\value{
Called for its side effect (blocks the R session while the app is
running, same as any \code{\link[shiny:runApp]{shiny::runApp()}} call). Invisibly returns \code{NULL}.
}
\description{
\code{launch_app()} starts an optional graphical wizard over the same
\verb{prep_*}/\verb{refdb_*}/\verb{adme_*}/\verb{tox_*}/\verb{targets_*}/
\verb{network_*}/\verb{plot_*} functions already exported by \code{patliR} -- the GUI
orchestrates step by step, it never reimplements any analysis logic.
\code{library(patliR)} continues to work standalone without ever calling this
function; it is purely an optional convenience layer on top of the core
package, requiring the \pkg{shiny} package (Suggests).
}
