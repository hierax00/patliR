% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_bowtie.R
\name{network_bowtie}
\alias{network_bowtie}
\title{Bowtie architecture of the STRING directed-action network, annotated per
condition's targets}
\usage{
network_bowtie(
  proj,
  condition = NULL,
  species = 9606,
  version = "12.0",
  actions_version = "11.0"
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{species}{NCBI taxonomy ID passed to \code{STRINGdb$new()}. Default
\code{9606} (human).}

\item{version}{Deprecated and unused -- both the actions graph and the
ID mapping now use \code{actions_version} (mixing releases dropped retired
ENSP IDs into the "not in the actions network" bucket). Kept so old
calls do not error.}

\item{actions_version}{STRING release for the directed "actions" file
\emph{and} the UniProt -> STRING_id mapping (see
\code{.network_stringdb_actions_graph()}, internal). Defaults to \code{"11.0"}:
STRING discontinued the "actions" flat file after v11.0.}
}
\value{
The updated \code{proj}, with two entries in \code{\link[=patliRResults]{patliRResults()}}:
\code{network_bowtie} (columns \code{condition}, \code{compound_id}, \code{uniprot_id},
\code{string_id}, \code{bowtie_component} -- one row per (condition, compound,
target). \code{bowtie_component} is \code{"core"} / \code{"in_component"} /
\code{"out_component"} / \code{"other"} (tendrils, tubes, disconnected) for a
target in the action graph, \code{"not_in_action_network"} for a target
that maps to STRING but is absent from the sparse directed action
graph, or \code{"unmapped"} for a target with no STRING_id at all -- none
are dropped) and
\code{network_bowtie_summary} (columns \code{species}, \code{version}, \code{n_nodes},
\code{n_edges}, \code{n_core}, \code{n_in_component}, \code{n_out_component}, \code{n_other} --
one row per \verb{(species, version)}; \code{version} here is \code{actions_version}).
Both written to their matching \verb{results/*.csv}.
}
\description{
Downloads STRING's directed "actions" file for \code{species} (activation/
inhibition/... interactions with known directionality -- a much sparser,
but genuinely \emph{directed}, subset of the interactome \code{network_proximity()}
uses), decomposes it into the classic bowtie structure (Broder, A. et al.
2000, "Graph structure in the Web," \emph{Computer Networks} 33(1-6), 309-320),
and reports which component each of a condition's mapped targets falls
into:
\itemize{
\item \strong{core}: the largest strongly connected component (every node can
reach every other node by a directed path).
\item \strong{in_component}: nodes that can reach the core but cannot be reached
from it (upstream regulators of the core).
\item \strong{out_component}: nodes reachable from the core but that cannot reach
it back (downstream effectors of the core).
\item \strong{other}: everything else (disconnected, or connected only via
"tendrils"/"tubes" that neither reach nor are reached by the core --
Broder et al.'s full taxonomy distinguishes these further; collapsed
here into one category, consistent with how this simplified 4-way
split is normally reported in network-biology papers).
}
}
\section{This is a species-wide structural landmark, not a per-condition subgraph}{

Unlike most of the \verb{network_*} family, the bowtie decomposition itself
does not depend on \code{condition} at all -- it is a property of the whole
directed action network for \code{species}, computed once and cached (see
\code{.network_stringdb_actions_graph()}, internal). \code{condition} only
controls which compounds/targets get annotated with their (fixed)
component membership.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_bowtie(proj, condition = "FLO-ET") # needs STRINGdb + internet on first call
patliRResults(proj, "network_bowtie")
patliRResults(proj, "network_bowtie_summary")
}

}
