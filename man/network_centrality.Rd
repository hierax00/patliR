% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_centrality.R
\name{network_centrality}
\alias{network_centrality}
\title{Node centrality (degree, betweenness, hub score) per condition}
\usage{
network_centrality(
  proj,
  condition = NULL,
  measures = c("degree", "betweenness", "hub_score"),
  normalize = TRUE
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{measures}{Character vector, any of \code{"degree"}, \code{"betweenness"},
\code{"hub_score"} (default: all three).}

\item{normalize}{Logical (default \code{TRUE}). Controls whether the
bipartite-aware columns are \emph{computed}: the \verb{_norm} sibling for each
requested measure, plus \code{hub_score_component} / \code{component_id} (if
\code{"hub_score"} was requested) and the \code{n_compounds} / \code{n_targets}
constants. Those columns are emitted either way -- with \code{normalize = FALSE} they are all \code{NA} -- so the stored schema does not depend on
this flag.}
}
\value{
The updated \code{proj}, with a \code{network_centrality} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{condition}, \code{node_id}, \code{node_type}
(\code{"compound"}/\code{"target"}), one column per requested measure, plus
\code{degree_norm} / \code{betweenness_norm} / \code{hub_score_component} /
\code{component_id} for the requested measures and the \code{n_compounds} /
\code{n_targets} constants -- computed when \code{normalize = TRUE}, \code{NA} when
\code{normalize = FALSE}), also written to
\code{results/network_centrality.csv}.
}
\description{
Computes classic node-level centrality measures over each condition's
compound-target graph from \code{\link[=network_build]{network_build()}}, for \strong{every node} --
compounds and targets alike (\code{node_type} distinguishes them). This is
deliberate: a target's raw degree here is exactly what
\code{\link[=network_hub_penalty]{network_hub_penalty()}} needs as input ("how many compounds touch this
target"), and a compound's degree/betweenness is a first, structural
read of how central it is to the condition's target profile.
}
\section{Unweighted on purpose}{

\code{\link[=network_build]{network_build()}}'s edges carry a \code{weight} column, but it is the
\strong{import probability} of a compound-target prediction (higher = more
confident), not a distance/cost. \code{igraph::betweenness()} and
\code{igraph::hits_scores()} both default to \emph{using} an edge attribute
literally named \code{weight} if present, treating it as something to
minimize along a path -- silently applying that default here would
invert the intended meaning (weak/uncertain predictions would look like
the "shortest, cheapest" paths). All three measures are computed
unweighted (\code{weights = NA}) to avoid that mismatch. If a future version
wants probability-weighted centrality, it needs to explicitly transform
\code{weight} into a proper distance (e.g. \code{1 - probability}) first, not pass
the raw column through.
}

\section{Hub score is eigenvector centrality here}{

The graph is \strong{undirected}, so Kleinberg's hub and authority scores
coincide and both equal the principal (Perron) eigenvector of the
adjacency matrix -- i.e. \code{hub_score} is eigenvector centrality, not a
directional "hub vs. authority" distinction (that only exists on a
directed graph). It is kept under the name \code{hub_score} for continuity
with earlier versions. Implementation note: \code{hub_score} is
\code{igraph::eigen_centrality(g, weights = NA)$vector} (scaled to \code{max = 1}).
Earlier versions used \code{igraph::hits_scores(g, weights = NA)$hub}, which
on a two-mode graph has a \strong{degenerate} top eigenspace -- ARPACK then
returns an RNG-seeded arbitrary vector from it, so the column was not
reproducible across runs. The Perron eigenvector is unique on each
connected component, so \code{eigen_centrality()} is deterministic.

On a \strong{disconnected} graph the principal eigenvector of the whole
adjacency matrix concentrates on the component with the largest spectral
radius, so every node in every other component gets \code{hub_score} ~ 0
regardless of how central it is \emph{within its own component}. When
\code{normalize = TRUE} an additional \code{hub_score_component} column is
computed by running \code{eigen_centrality()} \strong{per connected component}
(each scaled to \code{max = 1} inside that component); \code{component_id} records
which component each node is in. Use \code{hub_score} for a whole-graph
ranking, \code{hub_score_component} for \emph{within-component relative}
centrality (each component is independently rescaled, so a 2-node
component's top node scores the same \code{1} as the giant component's). An
isolated node gets \code{hub_score_component = NA}.
}

\section{Bipartite-aware normalisation (\code{normalize = TRUE}, the default)}{

This is a two-mode (bipartite) graph -- a compound's degree ranges over
\verb{[0, |T|]}, a target's over \verb{[0, |C|]}, and \verb{|T|} is usually much larger,
so a joint ranking of the raw \code{degree} (or \code{betweenness}) column mostly
reflects which mode a node is in, not its importance. \code{normalize = TRUE}
adds mode-aware \verb{_norm} siblings that all land in \verb{[0, 1]} and \emph{are}
comparable across \code{node_type}, following Borgatti & Everett (1997):
\itemize{
\item \code{degree_norm} = \verb{degree(v) / |T|} for a compound, \verb{degree(v) / |C|}
for a target -- the opposite mode's size is the structural maximum.
\item \code{betweenness_norm} = \code{betweenness(v) / B_max}, with \code{B_max} the
mode-specific maximum betweenness of a bipartite graph of these mode
sizes (Borgatti & Everett 1997; cross-checked against NetworkX's
\code{bipartite.betweenness_centrality}). \code{igraph::betweenness()} on an
undirected graph already counts each unordered pair once, so no
factor-of-two correction is applied to the raw score.
}

The raw \code{degree} / \code{betweenness} / \code{hub_score} columns are always kept.
Two per-condition constants, \code{n_compounds} and \code{n_targets}, are added so
the normalisation is self-documenting in the CSV.

\code{normalize = FALSE} reproduces the pre-normalisation \emph{values} -- the raw
\code{degree} / \code{betweenness} / \code{hub_score} columns are unchanged and every
\verb{_norm} / \code{component_id} / \code{n_compounds} / \code{n_targets} column is set to
\code{NA}. The column \emph{set} is deliberately the same as \code{normalize = TRUE}
(for the requested \code{measures}), so a \code{normalize = FALSE} run still
upserts cleanly onto a project whose stored table was produced with
\code{normalize = TRUE}.
}

\section{Comparable across \code{node_type}, not across conditions}{

\verb{|C|} and \verb{|T|} are per-condition constants, so a \verb{_norm} value is
comparable between a compound and a target \emph{of the same condition} but
\strong{not} between two conditions of different size -- exactly as the
R-index of \code{\link[=network_module_robustness]{network_module_robustness()}} is not comparable across
modules of different size. A disconnected graph makes \code{betweenness_norm}
a slight \emph{under}-estimate (cross-component pairs contribute no paths,
while \code{B_max} assumes connectivity).
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_centrality(proj)
patliRResults(proj, "network_centrality")
}

}
\references{
Borgatti SP, Everett MG (1997). "Network analysis of 2-mode data."
\emph{Social Networks} 19(3):243-269.
\doi{10.1016/S0378-8733(96)00301-2}

Freeman LC (1978). "Centrality in social networks: conceptual
clarification." \emph{Social Networks} 1(3):215-239.
}
