% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_enrich.R
\name{network_enrich}
\alias{network_enrich}
\title{Pathway/ontology enrichment over each condition's target set}
\usage{
network_enrich(
  proj,
  condition = NULL,
  db = c("reactome", "go", "kegg"),
  ont = c("BP", "MF", "CC", "ALL"),
  pvalueCutoff = 0.05,
  qvalueCutoff = 0.2,
  simplify_go = TRUE,
  simplify_cutoff = 0.7
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must already have
been built by \code{\link[=network_build]{network_build()}}), or \code{NULL} (default) for every built
condition.}

\item{db}{\code{"reactome"} (default), \code{"go"}, or \code{"kegg"} -- one at a time;
call again with a different \code{db} to accumulate results (existing
\verb{(condition, db)} pairs are replaced, not duplicated).}

\item{ont}{Only used when \code{db = "go"}: \code{"BP"} (default), \code{"MF"}, \code{"CC"},
or \code{"ALL"} -- passed straight to \code{\link[clusterProfiler:enrichGO]{clusterProfiler::enrichGO()}}.}

\item{pvalueCutoff, qvalueCutoff}{Passed straight to the underlying
\code{clusterProfiler}/\code{ReactomePA} enrichment function (defaults \code{0.05}/
\code{0.2}, same as their own defaults).}

\item{simplify_go}{Logical, default \code{TRUE}. Only used when \code{db = "go"}.
GO terms are hierarchical and highly redundant -- a real target set
routinely comes back with hundreds of "significant" GO terms, many of
them near-duplicate parent/child terms covering almost the same genes
(confirmed against real Chilcuague data, 2026-08-25: this redundancy is
what made \code{\link[=network_motifs]{network_motifs()}}'s derived \code{compound -> pathway} layer
explode to compound out-degrees in the thousands, since every
near-duplicate GO term adds its own derived edge). When \code{TRUE}, runs
\code{\link[clusterProfiler:simplify]{clusterProfiler::simplify()}} (semantic-similarity-based, via
\pkg{GOSemSim}) on the \code{db = "go"} result before returning it -- this
removes redundant terms by \emph{similarity}, not by tightening
\code{pvalueCutoff}/\code{qvalueCutoff} further, so it does not silently drop
real signal the way a stricter cutoff would. If \code{GOSemSim} is not
installed, or \code{simplify()} itself fails for this gene set (has
happened for \code{ont = "ALL"} in some \code{clusterProfiler} versions -- it is
designed for one ontology at a time), the unsimplified result is kept
and the fallback is logged (\code{"network_enrich_simplify_failed"}), never
a hard error -- this is enrichment quality, not a required step.}

\item{simplify_cutoff}{Single number in \verb{(0, 1]}, default \code{0.7} (same as
\code{clusterProfiler::simplify()}'s own default). Lower keeps fewer, more
dissimilar terms (more aggressive collapsing); \code{1} keeps everything
\code{simplify()} would otherwise consider a near-duplicate. Only used when
\code{simplify_go = TRUE} and \code{db = "go"}.}
}
\value{
The updated \code{proj}, with a \code{network_enrichment} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{condition}, \code{db}, \code{ID}, \code{Description},
\code{GeneRatio}, \code{BgRatio}, \code{pvalue}, \code{p.adjust}, \code{qvalue}, \code{geneID}
(Entrez IDs, \code{/}-separated -- \code{clusterProfiler}'s own convention,
preserved as-is rather than reformatted), \code{Count}), also written to
\code{results/network_enrichment.csv}. UniProt IDs that could not be mapped
to an Entrez ID are logged (\code{"network_enrich_unmapped_target"}) and
excluded from that condition's gene set, never silently included as
\code{NA}.
}
\description{
For each condition already built by \code{\link[=network_build]{network_build()}}, maps its distinct
UniProt target IDs to Entrez Gene IDs (\code{org.Hs.eg.db}) and runs an
over-representation test against GO, Reactome, or KEGG gene sets. This is
the "vía" (pathway) annotation layer referenced in \code{patliR_manual.md},
section 6 -- it does not change the graph built by \code{\link[=network_build]{network_build()}}
itself, it produces a separate enrichment results table keyed by
condition.
}
\section{Heavy, optional dependencies -- not installed by default}{

Unlike \code{igraph} (a hard \code{Imports} dependency of the whole \verb{network_*}
family since \code{\link[=network_build]{network_build()}}), the packages this function needs are
large Bioconductor annotation databases, kept in \code{Suggests} rather than
\code{Imports} so that installing \code{patliR} itself stays light. You need:
\code{org.Hs.eg.db} and \code{clusterProfiler} always (ID mapping, plus GO/KEGG
enrichment); \code{ReactomePA} additionally for \code{db = "reactome"}. See
\code{TESTING_GUIDE.Rmd}, section 0, for the install chunk. Calling this
without the right package installed raises a clear error naming exactly
which package is missing, rather than a cryptic "could not find
function".
}

\section{KEGG needs internet, GO/Reactome do not}{

\code{org.Hs.eg.db} and \code{reactome.db}/\code{ReactomePA} ship their gene-set data
as local packages -- once installed, \code{db = "go"}/\code{db = "reactome"} never
touch the network. \code{db = "kegg"} is different:
\code{clusterProfiler::enrichKEGG()} queries KEGG's REST API live on every
call (there is no current local Bioconductor package for this -- the old
\code{KEGG.db} is deprecated). The original design sketch in
\code{patliR_manual.md} claimed the whole family needed "no new fetch"; that
was true for GO/Reactome but not KEGG, and has been corrected there.
}

\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_enrich(proj, db = "go") # needs org.Hs.eg.db + clusterProfiler
patliRResults(proj, "network_enrichment")
}

}
