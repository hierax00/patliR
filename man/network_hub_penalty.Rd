% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_hub_penalty.R
\name{network_hub_penalty}
\alias{network_hub_penalty}
\title{Promiscuity-adjusted target score per condition (hub penalty)}
\usage{
network_hub_penalty(proj, condition = NULL)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}
}
\value{
The updated \code{proj}, with a \code{network_hub_penalty} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{condition}, \code{uniprot_id}, \code{degree_raw},
\code{n_compounds_total}, \code{score_adjusted}), also written to
\code{results/network_hub_penalty.csv}.
}
\description{
For every target node in a condition's graph, computes
\deqn{score_{adjusted} = degree_{raw} \times \log(N_{compounds} / degree_{raw})}
where \code{degree_raw} is how many of the condition's present compounds hit
that target, and \code{N_compounds} is the total number of compounds present
in that condition. This is a direct, self-contained computation from
\code{\link[=network_build]{network_build()}}'s graph -- it does \strong{not} require \code{\link[=network_centrality]{network_centrality()}}
to have run first (deliberately: coupling this to another function's
specific \code{measures} choice would be a fragile dependency for a two-line
formula that only ever needs plain degree).

A target hit by every compound in the condition (\code{degree_raw == N_compounds}) gets \code{score_adjusted = 0} -- maximally promiscuous, fully
discounted. A target hit by only one compound out of many keeps close to
its raw degree (\code{log(N_compounds / 1)} is large) -- same intuition as
TF-IDF, and the same log-ratio \code{\link[=bias_reweight]{bias_reweight()}} will reuse later at the
reference-database level (\code{patliR_manual.md}, section 7) instead of at
this per-condition-network level.
}
\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_hub_penalty(proj)
patliRResults(proj, "network_hub_penalty")
}

}
