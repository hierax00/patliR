% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_kegg_topology.R
\name{network_kegg_topology}
\alias{network_kegg_topology}
\title{Fetch and add KEGG's directed pathway topology to the network}
\usage{
network_kegg_topology(
  proj,
  condition = NULL,
  pathway_ids = NULL,
  species = "hsa",
  relation_types = c("PPrel", "GErel", "ECrel"),
  restrict_to_network = TRUE
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{pathway_ids}{\code{NULL} (default, see above) or a character vector of
KEGG pathway IDs (e.g. \code{c("hsa04151", "hsa04010")}).}

\item{species}{KEGG organism code, default \code{"hsa"} (human) -- must match
what \code{\link[=network_enrich]{network_enrich()}}/\code{\link[=network_pathview]{network_pathview()}} were run against.}

\item{relation_types}{Character vector of KGML relation \code{type}s to keep.
Default \code{c("PPrel", "GErel", "ECrel")} -- every gene-gene relation type
KGML defines. \code{"PCrel"} (protein-compound) is a valid value you can
pass explicitly, but is not in the default: this function only ever
resolves \verb{<entry type="gene">} (and, as of the group-entry fix, the
\code{type="group"} complexes built from them) to UniProt IDs, never
\verb{<entry type="compound">} -- so a \code{PCrel} relation's compound-side
endpoint can never resolve and that relation is always dropped as an
unmapped pair. Kept as an explicit opt-in (rather than removed
outright) so a future patch that does add compound-entry resolution
doesn't need to change this argument's contract, only what happens
when it's used. \code{"maplink"} (link to another pathway diagram, not a
biological relation) is never included.}

\item{restrict_to_network}{Logical, default \code{TRUE}. See the section above.}
}
\value{
The updated \code{proj}, with a \code{network_kegg_topology} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{condition}, \code{pathway_id}, \code{pathway_title},
\code{from_kegg}, \code{to_kegg}, \code{from_uniprot}, \code{to_uniprot}, \code{relation_type},
\code{relation_subtype}, \code{relation_value} (KGML's own arrow-style code, e.g.
\code{"-->"} activation, \code{"--|"} inhibition -- kept verbatim, not
reinterpreted), \code{restrict_to_network}), also written to
\code{results/network_kegg_topology.csv}. Entries KEGG could not map to a
UniProt accession are logged and excluded, never silently dropped.
}
\description{
For each KEGG pathway in scope, downloads its KGML (KEGG Markup
Language) file and parses the \verb{<relation>} elements -- directed,
typed gene-gene relations (\code{PPrel} protein-protein, e.g. activation/
inhibition/binding; \code{GErel} gene expression; \code{ECrel} enzyme-catalysis)
-- into UniProt-keyed edges, including relations between \code{type="group"}
entries (protein complexes), resolved to the union of their member
genes. This is the piece \code{\link[=network_pathview]{network_pathview()}} does not provide: that
function only renders KEGG's own pre-made pathway diagrams (via the
\code{pathview} package), it never parses topology; nothing else in
\code{patliR} reads a KGML file before this function. \code{PCrel}
(protein-compound) relations are parsed but always dropped -- see
\code{relation_types} below for why.
}
\section{Where the pathway list comes from}{

\code{pathway_ids = NULL} (default) pulls every KEGG pathway ID already found
significant by \link[=network_enrich]{network_enrich(db = "kegg")} for the
condition(s) in scope -- so this function completes a network you have
already characterised, rather than dumping an unrelated bulk topology.
Pass \code{pathway_ids} explicitly (e.g. \code{"hsa04151"}) to bypass that and fetch
specific pathways regardless of enrichment.
}

\section{\code{restrict_to_network} (default \code{TRUE})}{

A KGML relation is kept only when \strong{both} its endpoints, after UniProt
mapping, are among the condition's own predicted targets
(\code{network_edges$uniprot_id}) -- directly usable to annotate the existing
compound-target graph with directionality/mechanism, not a disconnected
topology dump. \code{restrict_to_network = FALSE} keeps the full pathway
topology (every gene KEGG maps in that pathway, whether or not any
compound in this project happens to target it) -- useful for pathway-level
analysis independent of the current target predictions.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
# ... build the project, run network_build() and
# network_enrich(condition = "FLO-ET", db = "kegg") ...
proj <- network_kegg_topology(proj, condition = "FLO-ET") # needs internet
patliRResults(proj, "network_kegg_topology")
}

}
\seealso{
\code{\link[=network_enrich]{network_enrich()}}, \code{\link[=network_pathview]{network_pathview()}}
}
