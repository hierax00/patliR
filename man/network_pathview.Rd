% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_pathview.R
\name{network_pathview}
\alias{network_pathview}
\title{Render KEGG pathway diagrams (\code{pathview}) for a condition's top enriched
pathways, colored by target score}
\usage{
network_pathview(
  proj,
  condition = NULL,
  gene_score = c("max_weight", "mean_weight", "n_compounds"),
  pathway_id = NULL,
  top_n_pathways = 10,
  low = "white",
  mid = "yellow",
  high = "red",
  out_dir = NULL,
  kegg_dir = NULL
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character scalar -- a single condition already enriched
against KEGG by \code{\link[=network_enrich]{network_enrich()}}. Unlike most \verb{network_*} functions,
\code{NULL} (default) only works if exactly one condition has \code{db = "kegg"}
enrichment results; otherwise pass it explicitly (same rule
\code{\link[=plot_gochord]{plot_gochord()}} uses, for the same reason -- pathway diagrams are
inherently per-condition, pooling them is not meaningful).}

\item{gene_score}{\code{"max_weight"} (default -- the highest import
probability among compounds hitting that target in \code{condition}),
\code{"mean_weight"}, or
\code{"n_compounds"} (count of distinct compounds hitting that target --
not on a 0-1 scale, \code{limit} is adjusted automatically, see below).}

\item{pathway_id}{Character vector of KEGG pathway IDs (e.g.
\code{"hsa04066"}, as returned in \code{network_enrichment$ID}), or \code{NULL}
(default) to use the \code{top_n_pathways} most significant KEGG results
for \code{condition} (lowest \code{p.adjust}). If given, every ID must already
appear in \code{condition}'s \code{db = "kegg"} \code{\link[=network_enrich]{network_enrich()}} results.}

\item{top_n_pathways}{Integer, default \code{10}. Ignored if \code{pathway_id} is given.}

\item{low, mid, high}{Colors passed straight to \code{pathview::pathview()}'s
\code{low}/\code{mid}/\code{high} (each internally wrapped as \code{list(gene = ...)},
\code{cpd.data} is never used here). Default \code{"white"}/\code{"yellow"}/\code{"red"},
not \code{pathview}'s own default (\code{"green"}/\code{"gray"}/\code{"red"}).}

\item{out_dir}{Directory the rendered PNGs are written to. Defaults to
\code{file.path(projectDir(proj), "plots", "kegg_pathview")}.}

\item{kegg_dir}{Directory \code{pathview} reads/caches each pathway's
downloaded \code{.xml}/\code{.png} from. Defaults to
\code{file.path(cacheDir(proj), "kegg_pathview")}.}
}
\value{
The updated \code{proj}, with a \code{kegg_pathview_log} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{condition}, \code{pathway_id}, \code{description},
\code{gene_score}, \code{n_genes_mapped}, \code{ok}, \code{path}, \code{message}), also written
to \code{results/kegg_pathview_log.csv}. A pathway that fails to render
(e.g. \code{pathview} cannot reach KEGG, or the pathway has no mappable
nodes) gets \code{ok = FALSE} and a \code{message}, is logged
(\code{"network_pathview_render_failed"}), and does \strong{not} stop the other
pathways in the same call from rendering.
}
\description{
For one \code{condition} already enriched against KEGG by \code{\link[=network_enrich]{network_enrich()}}
(\code{db = "kegg"}), renders a native KEGG pathway PNG for each of its
\code{top_n_pathways} most significant pathways (or an explicit
\code{pathway_id} vector), using \code{\link[pathview:pathview]{pathview::pathview()}}. Each pathway's
target/gene nodes are colored by \code{gene_score} -- a per-Entrez-ID summary
of that condition's own \code{\link[=network_build]{network_build()}} compound-target edges.
}
\section{Why this needs its own gene-to-Entrez mapping, not \code{geneID} off \code{network_enrichment}}{

\code{\link[=network_enrich]{network_enrich()}}'s output stores the Entrez IDs it fed into
\code{enrichKEGG()} only as a single \code{/}-separated string per pathway
(\code{geneID}, \code{clusterProfiler}'s own format) -- convenient for display,
not for building a numeric-scored named vector. This function instead
re-maps \code{condition}'s own \code{\link[=network_build]{network_build()}} UniProt targets to Entrez
(\code{clusterProfiler::bitr()}, same call \code{\link[=network_enrich]{network_enrich()}} itself makes)
so it can attach a real per-gene score, not just presence/absence.
}

\section{Output files, not an in-memory plot object}{

Unlike the \verb{plot_*} family, \code{pathview()} does not return anything this
package would call a "plot object" -- it renders and writes PNG files as
its side effect. \code{network_pathview()} follows the \verb{network_*} family's
own convention instead (input \code{proj} -> output \code{proj}, every result
written to disk and logged), not \verb{plot_*}'s (input \code{proj} -> returned
\code{ggplot}/\code{girafe}).
}

\section{Heavy, optional dependencies}{

Needs \code{pathview} (Bioconductor, \strong{not} installed by default -- add via
\code{BiocManager::install("pathview")}) plus \code{clusterProfiler}/\code{org.Hs.eg.db}
(already needed by
\code{\link[=network_enrich]{network_enrich()}}). \code{pathview} downloads each pathway's KGML/PNG from
KEGG's REST API on first use (needs internet, like \code{db = "kegg"} in
\code{\link[=network_enrich]{network_enrich()}} itself) and caches them under \code{kegg_dir} afterwards
(default \code{cacheDir(proj)/kegg_pathview}) -- deleting that cache is
always safe, same principle as every other cache in \code{patliR}.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_enrich(proj, condition = "FLO-ET", db = "kegg") # needs internet
proj <- network_pathview(proj, condition = "FLO-ET", top_n_pathways = 3)
patliRResults(proj, "kegg_pathview_log")
}

}
\references{
Luo, W. & Brouwer, C. (2013), "Pathview: an R/Bioconductor
package for pathway-based data integration and visualization",
\emph{Bioinformatics} 29(14), 1830-1831. \doi{10.1093/bioinformatics/btt285}
}
