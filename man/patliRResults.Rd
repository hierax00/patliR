% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/methods-accessors.R
\name{patliRResults}
\alias{patliRResults}
\alias{patliRResults<-}
\alias{patliRResults,PatliRProject-method}
\alias{patliRResults<-,PatliRProject-method}
\title{Access one entry of the flexible results bag of a patliR project}
\usage{
patliRResults(proj, name = NULL)

patliRResults(proj, name) <- value

\S4method{patliRResults}{PatliRProject}(proj, name = NULL)

\S4method{patliRResults}{PatliRProject}(proj, name) <- value
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{name}{\code{NULL} (the default) to get the whole named list, or a
character scalar to get one entry (\code{NULL} if that entry does not exist
yet).}

\item{value}{Replacement value for the entry named \code{name}.}
}
\value{
A named \code{list}, or one element of it.
}
\description{
\code{patliRResults()} is the read accessor for everything that is not one of
the structural slots of \code{\link[=PatliRProject-class]{PatliRProject}} (i.e. everything besides
\code{compounds}, \code{matrix_raw}, \code{binarized}): ADME tables, target predictions,
one network per condition, bias audits, rankings, and so on. Each family
of functions documents which name(s) it writes to this bag.
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
patliRResults(proj) # empty list: nothing has been computed yet
}
