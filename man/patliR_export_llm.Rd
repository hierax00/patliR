% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patliR_export_llm.R
\name{patliR_export_llm}
\alias{patliR_export_llm}
\title{Export a project's full state as plain text, for an LLM to read}
\usage{
patliR_export_llm(proj, out_file = NULL, max_rows = 200)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{out_file}{Character scalar path to write the text to, or \code{NULL}
(default) to only return it.}

\item{max_rows}{Integer, default \code{200}. Tables longer than this are
truncated (noted explicitly, never silently) so the export stays a
reasonable size for an LLM context window.}
}
\value{
Invisibly, the full export as a single character string.
}
\description{
Serializes \code{\link[=compounds]{compounds()}}, every entry of \code{\link[=patliRResults]{patliRResults()}}, and the
full \code{\link[=projectLog]{projectLog()}} into one plain-text document (CSV-formatted
tables under Markdown-style headings) -- meant to be pasted into or
uploaded to an LLM chat for analysis/discussion of a project's
results, without needing R itself. Read-only: nothing is written to
\code{proj}, only optionally to \code{out_file}.
}
\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
txt <- patliR_export_llm(proj)
cat(substr(txt, 1, 200))
}

}
