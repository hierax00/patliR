% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patliR_project.R
\name{patliR_project}
\alias{patliR_project}
\alias{patliR_load}
\title{Create or reload a patliR project}
\usage{
patliR_project(project_dir, cache_dir = NULL)

patliR_load(project_dir, cache_dir = NULL)
}
\arguments{
\item{project_dir}{Character scalar. Path to the project directory.
Created if it does not exist yet.}

\item{cache_dir}{Character scalar or \code{NULL}. Path to a directory for
derived caches. Defaults to \code{file.path(project_dir, ".patliR_cache")}.
Created if it does not exist yet. Never required for the analysis to
continue -- see \code{\link[=refdb_rebuild_cache]{refdb_rebuild_cache()}}.}
}
\value{
A new, empty \code{\link[=PatliRProject-class]{PatliRProject}} object.
}
\description{
\code{patliR_project()} creates a brand new, empty project rooted at
\code{project_dir}. \code{patliR_load()} reconstructs a project from the CSV files
a previous session left in \code{project_dir} -- this is what makes the
pipeline resumable without depending on the R session (or even the
\code{patliR} version) that produced those files staying alive. See
\code{\link[=PatliRProject-class]{PatliRProject}} for what "reconstructed" means in detail.
}
\details{
\code{patliR_load()} looks for the following files inside \code{project_dir} and
reloads whichever of them are present (all are optional -- a project can
be reloaded at any point of the pipeline):
\itemize{
\item \verb{01_compounds.csv} -> \code{\link[=compounds]{compounds()}}
\item \verb{02_matrix_raw.csv} -> \code{\link[=matrixRaw]{matrixRaw()}}
\item \verb{03_binarized.csv} -> \code{\link[=binarizedMatrix]{binarizedMatrix()}}
\item \code{patliR_log.csv} -> \code{\link[=projectLog]{projectLog()}}
\item any \verb{<name>.csv} inside \verb{<project_dir>/results/} -> that entry of
\code{\link[=patliRResults]{patliRResults()}}
}
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
proj

}
