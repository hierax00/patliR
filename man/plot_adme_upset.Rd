% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_adme_upset.R
\name{plot_adme_upset}
\alias{plot_adme_upset}
\title{UpSet-style plot: drug-likeness/lead-likeness rule overlaps across compounds}
\usage{
plot_adme_upset(
  proj,
  top_n = 15,
  save = TRUE,
  out_dir = NULL,
  width = 8,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{top_n}{Integer, default \code{15}. Only the \code{top_n} largest
intersections are shown.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{patchwork} object (bar chart on top of the dot-matrix,
\code{print()}-able and \code{ggsave()}-able like a single \code{ggplot}). If
\code{save = TRUE} (default), also writes a PNG and logs it to
\code{patliRResults(proj, "adme_upset_log")}.
}
\description{
Which compounds pass which \emph{combination} of \code{\link[=adme_filter]{adme_filter()}}'s rules, and
how many -- e.g. "42 compounds pass Ro5+Veber+Ghose but fail Oprea
(not lead-like)". Needs \code{\link[=adme_filter]{adme_filter()}} to have been run first (any
\code{rules} selection with 2+ rules).
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- adme_local(proj)
proj <- adme_filter(proj, rules = c("ro5", "veber", "ghose", "egan", "oprea"))
plot_adme_upset(proj, save = FALSE)
}

}
