% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_admet_radar.R
\name{plot_admet_radar}
\alias{plot_admet_radar}
\title{Bioavailability radar ("spiderweb") plot, SwissADME-style}
\usage{
plot_admet_radar(
  proj,
  compound_ids = NULL,
  engine = c("ggiraph", "static"),
  save = TRUE,
  out_dir = NULL,
  width = 6,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id}, or \code{NULL}
(default) for every compound with \code{\link[=adme_local]{adme_local()}} results.}

\item{engine}{\code{"ggiraph"} (default, if installed): interactive plot with
a tooltip per vertex showing the raw value. \code{"static"}: plain
\code{ggplot2}, no extra dependency beyond \code{ggplot2} itself.}

\item{save}{Logical, default \code{TRUE}. If \code{TRUE}, also writes two PNG files
per compound to \code{out_dir} -- one with axis labels
(\verb{<compound_id>_labeled.png}) and one without
(\verb{<compound_id>_plain.png}), the latter meant for tiling into reports
or side-by-side comparisons where six repeated axis labels are just
noise.}

\item{out_dir}{Directory to write the PNG files to (only used if
\code{save = TRUE}). Defaults to
\code{file.path(projectDir(proj), "plots", "admet_radar")}.}

\item{width, height, dpi}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} for the saved PNGs.}
}
\value{
A named \code{list} (one element per compound, named by
\code{compound_id}) of either \code{girafe} htmlwidgets (\code{engine = "ggiraph"})
or \code{ggplot} objects (\code{engine = "static"}). Print/plot each element
individually, e.g. \code{radars[["C0001"]]}, or \code{for (p in radars) print(p)}
to see them all. If \code{save = TRUE} (the default), the PNG paths are also
recorded in \code{patliRResults(proj, "admet_radar_log")} -- but note this
function itself only returns the plot list, not the updated \code{proj};
grab the log from \code{attr(radars, "proj")} if you need the updated
project object without re-running \code{\link[=adme_local]{adme_local()}}.
}
\description{
Draws a six-axis radar plot -- lipophilicity (LIPO), molecular size
(SIZE), polarity (POLAR), solubility (INSOLU), saturation (INSATU), and
flexibility (FLEX) -- with the published "pink zone" of optimal oral
drug-likeness shaded behind the compound's own polygon, the same idea as
SwissADME's Bioavailability Radar. Needs \code{\link[=adme_local]{adme_local()}} to have been run
first (that is where every one of these six properties is computed).

\strong{One plot per compound is returned} (a named list, keyed by
\code{compound_id}) rather than a single faceted plot -- with 6+ compounds a
facet grid makes every axis label unreadably small and cramped; a full
-size hexagon per compound is legible and closer to how SwissADME itself
presents one molecule at a time.
}
\section{Geometry note}{

This is built with plain Cartesian trigonometry (\code{x = r*sin(theta)},
\code{y = r*cos(theta)}) plus \code{ggplot2::coord_fixed()}, not
\code{ggplot2::coord_polar()}. \code{coord_polar()} interpolates \emph{along the arc}
between two data points, so a hexagon drawn with it comes out with
visibly curved edges and can look rotated/off-center depending on the
panel's x-range; drawing the hexagon's vertices directly in Cartesian
space gives straight edges and full control over where axis 1 sits (12
o'clock here, going clockwise).
}

\section{On the approximations involved (please read)}{

The pink zone boundaries are the published ones (see \code{.radar_axes} in
the package source for the exact reference); the outer axis scale is
our own display choice, not a literature value. INSOLU (\code{logs_esol}) and
INSATU (\code{fraction_csp3_approx}) both inherit the approximations
documented in \code{\link[=adme_local]{adme_local()}} -- in particular, INSATU is a rough
aliphatic-vs-aromatic-carbon proxy from the SMILES string, not a true
Fsp3 from hybridization perception. Treat this plot as a good first
read, not a certified reproduction of SwissADME's own figure.
}

\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- adme_local(proj)
radars <- plot_admet_radar(proj, engine = "static", save = FALSE)
radars[["C0001"]]
}

}
