% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_boiled_egg.R
\name{plot_boiled_egg}
\alias{plot_boiled_egg}
\title{BOILED-Egg plot: predicted GI absorption and BBB penetration}
\usage{
plot_boiled_egg(
  proj,
  compound_ids = NULL,
  engine = c("ggiraph", "static"),
  save = TRUE,
  out_dir = NULL,
  width = 7,
  height = 5,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id}, or \code{NULL}
(default) for every compound with \code{\link[=adme_local]{adme_local()}} results.}

\item{engine}{\code{"ggiraph"} (default, if installed): interactive plot with
a tooltip per point. \code{"static"}: plain \code{ggplot2}.}

\item{save}{Logical, default \code{TRUE}. If \code{TRUE}, also writes two static
PNGs to \code{out_dir} -- \code{boiled_egg_labeled.png} (with each point labeled
by its \code{compound_id}) and \code{boiled_egg_plain.png} (same scatter, no
point labels -- useful once you have many compounds and the labels
start overlapping).}

\item{out_dir}{Directory to write the PNG files to (only used if
\code{save = TRUE}). Defaults to \code{file.path(projectDir(proj), "plots")}.}

\item{width, height, dpi}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} for the saved PNG.}
}
\value{
A \code{girafe} htmlwidget (\code{engine = "ggiraph"}) or a \code{ggplot}
object (\code{engine = "static"}) -- one plot for all requested compounds
together (this is a comparative overview, unlike \code{\link[=plot_admet_radar]{plot_admet_radar()}}
which is one compound at a time). If \code{save = TRUE} (the default), the
PNG path is also recorded in \code{patliRResults(proj, "boiled_egg_log")},
retrievable via \code{attr(result, "proj")}.
}
\description{
Scatter of every compound's TPSA (x) against a WLogP proxy (y), with the
two published BOILED-Egg regions drawn to scale: the white "egg white"
ellipse (high probability of passive gastrointestinal absorption) and
the yellow "yolk" ellipse (high probability of BBB penetration).
Compounds falling outside both are flagged neither. Needs \code{\link[=adme_local]{adme_local()}}
to have been run first.
}
\section{Provenance of the ellipse boundaries}{

The two ellipses are digitized coordinates from the supporting
information of Daina, A. & Zoete, V. (2016), "A BOILED-Egg To Predict
Gastrointestinal Absorption and Brain Penetration of Small Molecules",
\emph{ChemMedChem} 11, 1117-1121, \doi{10.1002/cmdc.201600182} -- via the
GPL-3 reference implementation
\url{https://github.com/bfmilne/PyBOILEDegg} (Milne, B.F., 2021,
\doi{10.5281/zenodo.4725530}), which states the same source. These are
the real published boundaries, not an approximation.
}

\section{The one real caveat -- WLogP}{

The original model's y-axis is \strong{WLogP} (Wildman & Crippen's
atom-contribution logP, RDKit's implementation). \code{rcdk}/CDK has no
identical descriptor, so this plot (and \code{adme_local()}'s
\code{gi_absorption}/\code{bbb_permeant} columns) use CDK's ALogP
(\code{rcdk::get.alogp()}) as the closest available proxy -- a different,
but methodologically related, atom-contribution method. A compound near
an ellipse boundary could land on a different side of the line than it
would in the real SwissADME tool.
}

\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- adme_local(proj)
plot_boiled_egg(proj, engine = "static", save = FALSE)
}

}
