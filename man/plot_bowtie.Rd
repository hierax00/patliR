% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bowtie.R
\name{plot_bowtie}
\alias{plot_bowtie}
\title{Alluvial plot: which compounds' targets fall into which bowtie component}
\usage{
plot_bowtie(
  proj,
  condition = NULL,
  top_n_compounds = NULL,
  save = TRUE,
  out_dir = NULL,
  width = 8,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{top_n_compounds}{Integer or \code{NULL} (default \code{NULL}, all). If set,
restricts to the \code{top_n_compounds} with the most targets in scope --
with many compounds the alluvial gets visually crowded.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{ggplot} object (\code{ggalluvial} has no \code{ggiraph} equivalent, so
this function has no \code{engine} argument, unlike the rest of the
family). If \code{save = TRUE} (default), also writes a PNG and logs it to
\code{patliRResults(proj, "bowtie_plot_log")}.
}
\description{
Two-axis alluvial of \code{patliRResults(proj, "network_bowtie")}: left
stratum is \code{compound_id} (labeled by name), right stratum is
\code{bowtie_component} (\code{"core"}/\code{"in_component"}/\code{"out_component"}/
\code{"other"}/\code{"unmapped"}), flow thickness is the number of that
compound's targets landing in that component for \code{condition}. Answers
"for this extract, which compounds actually reach the core of the
directed action network, versus only its periphery (or don't map at
all)?" at a glance, instead of reading \code{network_bowtie()}'s row-per-
target table directly.
}
\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_bowtie(proj, condition = "FLO-ET")
plot_bowtie(proj, condition = "FLO-ET", save = FALSE)
}

}
