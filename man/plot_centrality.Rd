% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_centrality.R
\name{plot_centrality}
\alias{plot_centrality}
\title{Bar plot of the top-\code{top_n} nodes by a \code{network_centrality()} measure}
\usage{
plot_centrality(
  proj,
  condition = NULL,
  measure = c("degree", "betweenness", "hub_score", "degree_norm", "betweenness_norm"),
  node_type = c("both", "compound", "target"),
  top_n = 20,
  engine = c("static", "ggiraph"),
  save = TRUE,
  out_dir = NULL,
  width = 8,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{measure}{Single measure to plot: \code{"degree"}, \code{"betweenness"},
\code{"hub_score"}, or the bipartite-normalised \code{"degree_norm"} /
\code{"betweenness_norm"} -- must already be a column of
\code{patliRResults(proj, "network_centrality")}, i.e. requested (and, for
the \verb{_norm} variants, \code{normalize = TRUE}) in the \code{\link[=network_centrality]{network_centrality()}}
call that produced it. When \code{measure} is not supplied \strong{and}
\code{node_type = "both"}, it defaults to \code{"degree_norm"} (raw \code{degree} is
not comparable between compounds and targets; Borgatti & Everett
1997), falling back to \code{"degree"} with a warning if the normalised
column is absent (a result from before normalisation). Otherwise the
default is \code{"degree"}.}

\item{node_type}{\code{"both"} (default), \code{"compound"}, or \code{"target"} --
restrict to one side of the bipartite graph.}

\item{top_n}{Integer, default \code{20}. Only the \code{top_n} highest-\code{measure}
nodes (within the \code{condition}/\code{node_type} scope) are drawn -- with
hundreds of targets, plotting all of them produces an unreadable wall
of bars.}

\item{engine}{\code{"static"} (default) or \code{"ggiraph"}, save/out_dir/width/
height/dpi -- same as \code{\link[=plot_network_layers]{plot_network_layers()}}.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{ggplot} object or \code{girafe} htmlwidget. If \code{save = TRUE}
(default), also writes a PNG and logs it to
\code{patliRResults(proj, "centrality_plot_log")}.
}
\description{
Horizontal bar chart of the \code{top_n} highest-scoring nodes (labeled with
their real name/symbol, same lookup as \code{\link[=plot_network_layers]{plot_network_layers()}}) for one
\code{measure} (\code{"degree"}, \code{"betweenness"}, or \code{"hub_score"}), colored by
\code{node_type} (\code{"compound"}/\code{"target"}), faceted by \code{condition} when more
than one is in scope.
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_centrality(proj)
plot_centrality(proj, condition = "FLO-ET", save = FALSE)
}

}
