% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_chemical_space.R
\name{plot_chemical_space}
\alias{plot_chemical_space}
\title{Chemical space plot: 2D projection of compounds, colored by family}
\usage{
plot_chemical_space(
  proj,
  condition = NULL,
  compound_ids = NULL,
  dims = 2,
  method = c("pca", "umap"),
  color_by = "family",
  show_hulls = TRUE,
  seed = NULL,
  engine = c("ggiraph", "static", "plotly"),
  save = TRUE,
  out_dir = NULL,
  width = 7,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character scalar, a single condition column of
\code{\link[=binarizedMatrix]{binarizedMatrix()}}, or \code{NULL} (default) for every compound with
\code{\link[=adme_local]{adme_local()}} results regardless of condition.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id}, or \code{NULL}
(default). Combined with \code{condition} if both given (intersection).}

\item{dims}{\code{2} (default) or \code{3}. \code{3} projects onto three components and
renders either a rotatable 3D scatter with a translucent hull mesh
("halo") per family (\code{engine = "plotly"}, needs the \code{plotly} package)
or a 3-panel matrix of PC pairs with 2D hulls (\code{engine = "static"}).}

\item{method}{\code{"pca"} (default) or \code{"umap"}.}

\item{color_by}{\code{"family"} (default, needs \code{\link[=compounds_classify]{compounds_classify()}} to
have run -- colors by NPClassifier pathway), or any of \code{"ro5_pass"},
\code{"veber_pass"}, \code{"ghose_pass"}, \code{"egan_pass"}, \code{"oprea_pass"},
\code{"route_oral"}, \code{"gi_absorption"}, \code{"bbb_permeant"} (any column
\code{\link[=adme_local]{adme_local()}} produced -- validated against the actual columns
present, not a fixed list, so a route you did not request in
\code{\link[=adme_local]{adme_local()}} raises a clear error rather than silently plotting
nothing).}

\item{show_hulls}{Logical, default \code{TRUE}. Draw a convex-hull outline
(\code{grDevices::chull()}, base R) around each \code{color_by} group with 3+
points. Only really legible for a categorical \code{color_by} (i.e.
\code{"family"}); ignored with a warning for continuous/logical \code{color_by}.}

\item{seed}{Integer or \code{NULL} (default). Only used by \code{method = "umap"}
(UMAP's neighbor search has a stochastic component); restores the
pre-call RNG state afterwards, same principle as \code{network_proximity()}
/ \code{network_module_robustness()}'s \code{seed} argument.}

\item{engine}{\code{"ggiraph"} (default, if installed): interactive 2D plot
with a tooltip per point. \code{"static"}: plain \code{ggplot2}. \code{"plotly"}:
rotatable 3D (only meaningful with \code{dims = 3}; needs the \code{plotly}
package).}

\item{save}{Logical, default \code{TRUE}. If \code{TRUE}, also writes a PNG.}

\item{out_dir}{Directory to write the PNG to (only used if
\code{save = TRUE}). Defaults to \code{file.path(projectDir(proj), "plots")}.}

\item{width, height, dpi}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{girafe} htmlwidget (\code{engine = "ggiraph"}) or a \code{ggplot}
object (\code{engine = "static"}). If \code{save = TRUE} (the default), the PNG
path and PC/UMAP scores per compound are also recorded in
\code{patliRResults(proj, "chemical_space_log")} /
\code{results/chemical_space_log.csv}, retrievable via
\code{attr(result, "proj")}.
}
\description{
Projects every compound's physicochemical descriptors (from
\code{\link[=adme_local]{adme_local()}}) to 2D -- PCA by default, UMAP optional -- and colors
points by chemical family (\code{\link[=compounds_classify]{compounds_classify()}}'s NPClassifier
pathway) or by any drug-likeness rule/route flag \code{\link[=adme_local]{adme_local()}}
computed. Meant to answer "where do my extract's compounds sit, and do
compounds from the same chemical family cluster together" at a glance --
a companion, not a replacement, to the compound-by-compound detail in
\code{\link[=plot_admet_radar]{plot_admet_radar()}}.
}
\section{Method}{

\code{method = "pca"} (default) standardizes (\code{scale = TRUE}) every numeric
descriptor and runs \code{\link[stats:prcomp]{stats::prcomp()}} -- deterministic, no new
dependency. \code{method = "umap"} needs the \code{umap} package (CRAN, not
installed by default) -- non-linear, can separate clusters PCA
compresses together, but the axes have no direct physicochemical
meaning and results can shift slightly between runs unless \code{seed} is
set.
}

\section{Descriptors used}{

\code{mw}, \code{logp}, \code{hbd}, \code{hba}, \code{tpsa}, \code{rotatable_bonds}, \code{amr},
\code{fraction_csp3_approx}, \code{aromatic_proportion_approx}, \code{n_rings_approx}
-- every numeric column \code{\link[=adme_local]{adme_local()}} computes except the derived
pass/fail rule flags and the BOILED-Egg call (those are outcomes, not
independent axes). Compounds with \code{NA} in any of these are excluded and
logged (\code{"plot_chemical_space_excluded_na"}), the same principle
\code{\link[=network_proximity]{network_proximity()}} uses for compounds STRINGdb cannot map.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- adme_local(proj)
plot_chemical_space(proj, color_by = "ro5_pass", engine = "static", save = FALSE)
# three axes, one hull per family, rotatable:
plot_chemical_space(proj, dims = 3, color_by = "family", engine = "plotly", save = FALSE)
}

}
