% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_disease_network.R
\name{plot_disease_network}
\alias{plot_disease_network}
\title{Compound-target-disease network, disease nodes as large blocks with a
convex-hull halo over their targets}
\usage{
plot_disease_network(
  proj,
  condition = NULL,
  disease = NULL,
  max_rank = NULL,
  compound_ids = NULL,
  engine = c("static", "ggiraph"),
  save = TRUE,
  out_dir = NULL,
  width = 9,
  height = 7,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{disease}{\code{NULL} (default, every disease in scope) or a character
vector of \code{disease_id}s to restrict to.}

\item{max_rank}{\code{NULL} (default, every row) or an integer -- keep only
\code{targets_disease_profile()} rows with \code{rank <= max_rank} (its explore
mode can carry several diseases per target; this trims the plot to each
target's top few without re-fetching).}

\item{compound_ids}{Character vector of \code{compounds(proj)$id} to restrict
to, or \code{NULL} (default) for every compound present in scope.}

\item{engine}{\code{"static"} (default) or \code{"ggiraph"}.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{ggplot}/\code{girafe} object, with \code{attr(., "proj")} set when
\code{save = TRUE} (also writes a PNG, logged to
\code{patliRResults(proj, "disease_network_plot_log")}).
}
\description{
Reads \code{\link[=targets_disease_profile]{targets_disease_profile()}}'s output (not \code{\link[=targets_disease_filter]{targets_disease_filter()}}
-- that table has no resolved disease name to label with) and draws a
three-layer network: compounds and targets at the usual small point size
(same palette as \code{\link[=plot_network_layers]{plot_network_layers()}}), disease nodes drawn
substantially larger, each with a translucent convex-hull polygon behind
it enclosing the positions of every target associated with that disease --
so a compound's target set visibly clusters toward the disease
neighbourhood(s) it is actually implicated in, rather than every disease
looking like just another same-size node in the layout.

Only targets that have at least one disease association in
\code{\link[=targets_disease_profile]{targets_disease_profile()}}'s output are drawn -- this is deliberately a
focused view of the compound-target-disease structure, not a full replay
of every predicted target (\code{\link[=plot_network_layers]{plot_network_layers()}} already does that).
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
# ... build the project, run network_build() and targets_disease_profile() ...
plot_disease_network(proj, condition = "FLO-ET", save = FALSE)
}

}
\seealso{
\code{\link[=targets_disease_profile]{targets_disease_profile()}}, \code{\link[=plot_network_layers]{plot_network_layers()}}
}
