% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_enrichment.R
\name{plot_enrichment}
\alias{plot_enrichment}
\title{Bubble plot of \code{network_enrich()} over-representation results}
\usage{
plot_enrichment(
  proj,
  condition = NULL,
  db = NULL,
  top_n = 20,
  engine = c("static", "ggiraph"),
  save = TRUE,
  out_dir = NULL,
  width = 8,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{db}{Character vector of database names (\code{"go"}, \code{"reactome"},
\code{"kegg"}) to restrict to, or \code{NULL} (default) for every \code{db} present
in scope.}

\item{top_n}{Integer, default \code{20}. Number of most-significant
(lowest-\code{p.adjust}) terms to draw per \verb{(condition, db)} group.}

\item{engine}{\code{"static"} (default) or \code{"ggiraph"}, save/out_dir/width/
height/dpi -- same as \code{\link[=plot_network_layers]{plot_network_layers()}}.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{ggplot} object or \code{girafe} htmlwidget. If \code{save = TRUE}
(default), also writes a PNG and logs it to
\code{patliRResults(proj, "enrichment_plot_log")}.
}
\description{
The standard \code{clusterProfiler::dotplot()}-style enrichment bubble: one
point per enriched term from \code{patliRResults(proj, "network_enrichment")},
term name (\code{Description}) on \code{y}, \code{GeneRatio} (parsed from its \code{"k/n"}
string, e.g. \code{"3/20"} -> \code{0.15}) on \code{x}, point \strong{size} = \code{Count} (genes
in the input set annotated to the term), point \strong{colour} = \code{p.adjust}
(\code{scale_colour_gradient(low = "#c0392b", high = "#2980b9")} -- low
\code{p.adjust} = more significant = the "hot" red end, matching
\code{\link[=plot_proximity]{plot_proximity()}}'s \code{TRUE}-is-red convention for "the thing worth
looking at"). Terms are ranked by \code{p.adjust} (most significant first)
to pick the \code{top_n} shown per \verb{(condition, db)} group, then the \code{y}-axis
within that selection is ordered by \code{GeneRatio} -- the two orderings
answer different questions ("which terms are most confidently enriched"
vs. "which terms involve the largest fraction of the input set") and
\code{clusterProfiler::dotplot()} makes the same split. Faceted by \code{db} when
more than one database is in scope.
}
\section{\code{qvalue} may be absent}{

\code{clusterProfiler} omits the \code{qvalue} column from its result whenever the
\code{qvalue} package's pi0 estimation fails, which is routine for the small
p-value vectors a 10-50-gene target set produces (see
\code{\link[=network_enrich]{network_enrich()}}'s own review notes). This plot does not use \code{qvalue}
at all, and does not require the column to be present.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_enrich(proj, db = "go") # needs org.Hs.eg.db + clusterProfiler
plot_enrichment(proj, condition = "FLO-ET", save = FALSE)
}

}
\references{
Yu, Wang, Han & He (2012), \emph{OMICS} 16(5):284-287,
\doi{10.1089/omi.2011.0118} (the \code{clusterProfiler} dotplot form this
figure follows). Li S. (2021), \emph{World J Tradit Chin Med} 7(1):146-154,
\doi{10.4103/wjtcm.wjtcm_11_21} (network-pharmacology reporting
guidance codifying the enrichment bubble as expected output).
}
