% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gochord.R
\name{plot_gochord}
\alias{plot_gochord}
\title{Circular gene-pathway ribbon chord diagram (real \code{GOplot::GOChord()})}
\usage{
plot_gochord(
  proj,
  condition = NULL,
  db = NULL,
  top_n_terms = 10,
  engine = c("static", "ggiraph"),
  save = TRUE,
  out_dir = NULL,
  width = 14,
  height = 14,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{db}{Passed through to filter \code{network_enrichment$db} if more than
one enrichment database was run for \code{condition} (e.g. \code{"go"} vs.
\code{"reactome"}); \code{NULL} (default) uses whichever is present, and errors
if both are and \code{db} was not specified.}

\item{top_n_terms}{Integer, default \code{10}. Only the \code{top_n_terms} most
significant terms (lowest \code{p.adjust}) are drawn -- with dozens of
significant terms the ribbon diagram becomes unreadable.}

\item{engine}{\code{"static"} (default) or \code{"ggiraph"}, save/out_dir/width/
height/dpi -- same as \code{\link[=plot_network_layers]{plot_network_layers()}}.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{ggplot} object or \code{girafe} htmlwidget. If \code{save = TRUE}
(default), also writes a PNG and logs it to
\code{patliRResults(proj, "gochord_plot_log")}.
}
\description{
The \code{top_n_terms} most significant \code{\link[=network_enrich]{network_enrich()}} pathway/GO terms
for \code{condition}, plus every gene (Entrez -> symbol, via the same
\code{org.Hs.eg.db} mapping \code{\link[=network_enrich]{network_enrich()}} itself uses) belonging to at
least one of them, rendered as a true ribbon chord diagram via
\code{GOplot::GOChord()} -- curved ribbons from each gene to every term it
belongs to, colored by term.
}
\section{No \code{logFC} ring}{

\code{GOChord()} optionally colors an inner ring by a \code{logFC} column. This
package has no real per-gene fold-change to put there (target
prediction, not differential expression), so this function always calls
\code{GOChord(..., nlfc = 0)}: ribbons only, no inner ring, no \code{logFC} legend.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_enrich(proj, condition = "FLO-ET", db = "go")
plot_gochord(proj, condition = "FLO-ET", top_n_terms = 8, save = FALSE)
}

}
