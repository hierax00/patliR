% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmap.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Heatmap of compound-target association or compound-condition presence
(real \code{pheatmap()})}
\usage{
plot_heatmap(
  proj,
  condition = NULL,
  what = c("compound_target", "compound_condition"),
  save = TRUE,
  out_dir = NULL,
  width = NULL,
  height = NULL
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{what}{\code{"compound_target"} (default) or \code{"compound_condition"}.}

\item{save}{Logical, default \code{TRUE}. If \code{TRUE}, writes a PDF via
\code{pheatmap::pheatmap(..., filename = ...)} and logs the path to
\code{patliRResults(proj, "heatmap_plot_log")}. If \code{FALSE}, draws to the
current graphics device instead (same as calling \code{pheatmap()}
directly without \code{filename}).}

\item{out_dir}{Directory for the PDF, default \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{\code{NULL} (default) to size the plot to the matrix
(\code{max(8, n_cols * 0.9 + 2)} / \code{max(6, n_rows * 0.5 + 2)}), or an
explicit number of inches.}
}
\value{
The \code{pheatmap} object returned by \code{pheatmap::pheatmap()}
(\code{list(tree_row, tree_col, kmeans, gtable)}) -- re-drawable with
\code{grid::grid.draw(result$gtable)}. If \code{save = TRUE}, also carries the
updated \code{proj} as \code{attr(result, "proj")} (same pattern as every other
\verb{plot_*} function).
}
\description{
\code{what = "compound_target"} (default): compound x target matrix, filled
by \code{network_edges$weight} (the import probability), for one or more
conditions pooled. \code{what = "compound_condition"}: compound x condition
matrix straight from \code{\link[=binarizedMatrix]{binarizedMatrix()}} (presence/absence, 0/1) --
which compounds were actually detected in which extract. Both are
rendered with \code{pheatmap::pheatmap()} -- hierarchical clustering on both
axes (dropped automatically, with a warning, on whichever axis has
fewer than 2 rows/columns -- \code{hclust()} cannot cluster a single item),
cell values printed on top of the color scale.
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
plot_heatmap(proj, what = "compound_condition", save = FALSE)
plot_heatmap(proj, condition = "FLO-ET", what = "compound_target", save = FALSE)
}

}
