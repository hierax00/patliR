% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pathway_network.R
\name{plot_kegg_routes}
\alias{plot_kegg_routes}
\title{Metro map of the routes from the extract's targets to disease genes}
\usage{
plot_kegg_routes(
  proj,
  condition = NULL,
  disease = NULL,
  to = NULL,
  routes = NULL,
  top_n_routes = 6,
  alternatives = FALSE,
  background = TRUE,
  max_compound_labels = 2,
  engine = c("static", "ggiraph"),
  save = TRUE,
  out_dir = NULL,
  width = NULL,
  height = NULL,
  dpi = 150,
  ...
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character scalar with rows in \code{network_kegg_topology};
\code{NULL} (default) when only one condition has topology.}

\item{disease}{\code{NULL} or a \code{disease_id} of
\code{patliRResults(proj, "disease_genes")}. One of \code{disease} / \code{to} is
required.}

\item{to}{\code{NULL} or a character vector of destination genes (UniProt
accessions, KEGG gene IDs such as \code{"hsa:5594"}, or gene symbols).
When both are given, destinations are the disease genes in \code{to}.}

\item{routes}{\code{NULL} (default: computed with \code{\link[=kegg_routes]{kegg_routes()}} from the
other arguments) or an existing \code{\link[=kegg_routes]{kegg_routes()}} result.}

\item{top_n_routes}{Integer, default \code{6}: routes drawn (best pairs,
first alternative each unless \code{alternatives = TRUE}).}

\item{alternatives}{Logical, default \code{FALSE}.}

\item{background}{Logical, default \code{TRUE}: faint grey KEGG relations
among the drawn stations.}

\item{max_compound_labels}{Integer, default \code{2}: compound names printed
under a source station (the rest as "+n").}

\item{engine}{\code{"static"} (default) or \code{"ggiraph"}.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{...}{Passed to \code{\link[=kegg_routes]{kegg_routes()}} when \code{routes} is \code{NULL}.}
}
\value{
A \code{ggplot}/\code{girafe} object with attribute \code{routes} (the drawn
routes). With \code{save = TRUE} the PNG is written and logged in
\code{patliRResults(proj, "kegg_routes_plot_log")} (keyed by \code{condition},
\code{target_set}), and the updated project is in \code{attr(., "proj")}.
}
\description{
Draws the best routes of \code{\link[=kegg_routes]{kegg_routes()}} like a transit map: each route
is a coloured line, genes are stations. Source stations (the extract's
targets) are filled and labelled with the compounds that hit them;
destination stations (disease genes) carry a red ring; a station used by
several routes is an interchange (larger, thick outline). Stations are
placed in columns from the sources (left) to the destinations (right)
(a layered, Sugiyama-type layout) and lines run horizontally, diagonally
at 45 degrees or vertically between them. Other KEGG relations among the
drawn genes are shown as faint grey links, and every hop carries its
effect mark (arrow = activation/expression, bar = inhibition/repression).
}
\examples{
\dontrun{
plot_kegg_routes(proj, condition = "FLO-ET", disease = "MONDO_0005044")
}
}
\seealso{
\code{\link[=kegg_routes]{kegg_routes()}}, \code{\link[=kegg_routes_table]{kegg_routes_table()}}, \code{\link[=plot_kegg_topology]{plot_kegg_topology()}}
}
