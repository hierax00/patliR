% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pathway_network.R
\name{plot_pathway_network}
\alias{plot_pathway_network}
\title{Enrichment map: enriched pathways as a network of overlapping gene sets}
\usage{
plot_pathway_network(
  proj,
  condition = NULL,
  db = c("kegg", "reactome", "go"),
  top_n = 30,
  p_cutoff = 0.05,
  similarity = c("jaccard", "overlap"),
  min_similarity = NULL,
  layout = c("kk", "fr"),
  clusters = TRUE,
  show_compounds = FALSE,
  top_n_compounds = 8,
  min_compound_hits = 2,
  max_links_per_compound = 5,
  disease = NULL,
  disease_fdr = 0.05,
  label_width = 24,
  seed = 1,
  engine = c("static", "ggiraph"),
  save = TRUE,
  out_dir = NULL,
  width = 11,
  height = 8.5,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character scalar, a condition with rows in
\code{network_enrichment} for \code{db}. \code{NULL} (default) works only when exactly
one condition has enrichment results for \code{db}.}

\item{db}{One of \code{"kegg"} (default), \code{"reactome"}, \code{"go"}.}

\item{top_n}{Integer, default \code{30}: number of most significant terms
(lowest adjusted p) drawn.}

\item{p_cutoff}{Numeric in (0, 1], default \code{0.05}: terms with adjusted p
above this are never drawn.}

\item{similarity}{\code{"jaccard"} (default) or \code{"overlap"}, see the section
above.}

\item{min_similarity}{Minimum overlap for an edge. \code{NULL} (default)
means \code{0.3} for Jaccard and \code{0.5} for the overlap coefficient.}

\item{layout}{\code{"kk"} (Kamada-Kawai, default: spreads nodes evenly, keeps clusters apart)
or \code{"fr"} (Fruchterman-Reingold), both with edge weights = overlap and a fixed \code{seed}.}

\item{clusters}{Logical, default \code{TRUE}: outline Louvain clusters of
terms (clusters of a single term are not outlined).}

\item{show_compounds}{Logical, default \code{FALSE}: add the extract's
compounds as a second node type (see the section above).}

\item{top_n_compounds}{Integer, default \code{8}.}

\item{min_compound_hits}{Integer, default \code{2}.}

\item{max_links_per_compound}{Integer, default \code{5}.}

\item{disease}{\code{NULL} (default) or a single \code{disease_id} present in
\code{patliRResults(proj, "disease_genes")} (see \code{\link[=disease_genes_fetch]{disease_genes_fetch()}},
\code{\link[=disease_genes_import]{disease_genes_import()}}).}

\item{disease_fdr}{Numeric in (0, 1], default \code{0.05}.}

\item{label_width}{Integer, default \code{24}: characters per line of the
wrapped term labels (labels are also cut at three lines).}

\item{seed}{Integer seed for the layout and the community detection;
the caller's random state is restored afterwards.}

\item{engine}{\code{"static"} (default) or \code{"ggiraph"} (hover tooltips with
the full term name, genes and statistics).}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{ggplot} (or \code{girafe}) object with attributes \code{terms} (one row
per drawn term: statistics, cluster, degree and, with \code{disease}, the
hypergeometric result), \code{edges} (term pairs, \code{similarity},
\code{n_shared}) and, with \code{show_compounds}, \code{compound_links}. With
\code{save = TRUE} the PNG is written and logged in
\code{patliRResults(proj, "pathway_network_plot_log")} (keyed by
\code{condition}, \code{db}, \code{disease}, \code{compounds}), and the updated project is
in \code{attr(., "proj")}.
}
\description{
Draws the \code{top_n} most significant terms of one database in
\code{\link[=network_enrich]{network_enrich()}}'s results as a network: one node per term, an edge
between two terms when the extract's target genes annotated to them
overlap (Jaccard or overlap coefficient at or above \code{min_similarity}).
Node area is the number of the extract's targets in the term (\code{Count}),
node colour its significance (\eqn{-\log_{10}} adjusted p), edge width
the overlap. Groups of related terms are found by community detection
(Louvain) and outlined, each with a cluster name taken from its most
central term, so redundant terms (for instance a family of cancer
pathways that all share the same PI3K/MAPK genes) read as one theme.

Optionally, the extract's compounds can be added as a second node type
(\code{show_compounds = TRUE}), and the terms whose genes are enriched in a
disease gene set can be ringed (\code{disease}).
}
\section{Gene-set overlap}{

For two terms with target-gene sets \eqn{A} and \eqn{B} (the \code{geneID}
column of \code{network_enrichment}, i.e. only the extract's targets in each
term, not the full pathway):
\deqn{J(A,B) = |A \cap B| / |A \cup B| \quad \textrm{(Jaccard)}}
\deqn{O(A,B) = |A \cap B| / \min(|A|, |B|) \quad \textrm{(overlap coefficient)}}
The overlap coefficient reaches 1 when one set is contained in the other
(useful for GO, where child terms nest inside parents); Jaccard penalises
size differences. Merico et al. (2010) use a 0.5 overlap-coefficient
cutoff, or a combination of both measures, to draw an edge.
}

\section{Disease-gene enrichment (\code{disease})}{

For each drawn term, a one-sided hypergeometric test asks whether its
target genes contain more genes of the disease set than expected. The
universe is the extract's own targets in \code{condition} (mapped to Entrez,
\code{N} genes, \code{K} of them disease genes); a term with \code{n} targets, \code{k} of
them disease genes, gets \eqn{p = P(X \ge k)},
\eqn{X \sim \mathrm{Hypergeometric}(N, K, n)}. p-values are adjusted
(Benjamini-Hochberg) across the drawn terms; a term with adjusted p
below \code{disease_fdr} is drawn with a black ring. Every term with at least
one disease gene also gets a dark inner dot whose area is the share
\eqn{k / n} of the node's area, so the disease load stays visible when
no term reaches significance. Because the universe is
the extract's targets, not the genome, the test asks "within what this
extract hits, is this pathway especially disease-related", which does not
re-test the pathway's enrichment itself.
}

\section{Compounds (\code{show_compounds})}{

A compound is linked to a term when at least \code{min_compound_hits} of its
predicted targets (\code{network_edges}, UniProt mapped to Entrez) are among
the term's genes. The \code{top_n_compounds} compounds with the most (term,
gene) hits over the drawn terms are placed, evenly spaced, on an outer
ring, in the circular order of the centre of the terms they hit. Each
compound keeps only its \code{max_links_per_compound} strongest links (largest
share of the term's genes hit): most compounds hit most terms a little,
and drawing every link hides which terms a compound hits hardest.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
# ... build the project, run network_build() and
# network_enrich(condition = "FLO-ET", db = "kegg") ...
plot_pathway_network(proj, condition = "FLO-ET", db = "kegg", save = FALSE)
# rings on the pathways enriched in hypertension genes
proj <- disease_genes_fetch(proj, "hypertension")
plot_pathway_network(proj, condition = "FLO-ET", disease = "MONDO_0005044")
}

}
\references{
Merico, D., Isserlin, R., Stueker, O., Emili, A. & Bader, G. D. (2010),
"Enrichment Map: a network-based method for gene-set enrichment
visualization and interpretation", \emph{PLoS ONE} 5(11), e13984.
\doi{10.1371/journal.pone.0013984}

Blondel, V. D., Guillaume, J.-L., Lambiotte, R. & Lefebvre, E. (2008),
"Fast unfolding of communities in large networks", \emph{Journal of
Statistical Mechanics} P10008. \doi{10.1088/1742-5468/2008/10/P10008}
}
\seealso{
\code{\link[=network_enrich]{network_enrich()}}, \code{\link[=plot_enrichment]{plot_enrichment()}}, \code{\link[=plot_kegg_topology]{plot_kegg_topology()}}
}
