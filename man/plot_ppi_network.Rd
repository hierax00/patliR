% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ppi_network.R
\name{plot_ppi_network}
\alias{plot_ppi_network}
\title{Protein-protein interaction network of an extract's predicted targets,
with a disease overlay}
\usage{
plot_ppi_network(
  proj,
  condition = NULL,
  disease = NULL,
  view = c("compound_ppi", "ppi"),
  colour_by = c("disease", "module"),
  size_by = c("compounds", "degree"),
  max_targets = 60,
  top_n_labels = 30,
  score_threshold = 700,
  disease_partners = 0,
  disease_node = FALSE,
  highlight = c("gpcr", "none"),
  highlight_set = NULL,
  layout = c("fr", "kk"),
  seed = 1,
  species = 9606,
  version = "12.0",
  engine = c("static", "ggiraph"),
  save = TRUE,
  out_dir = NULL,
  width = 10,
  height = 9,
  dpi = 300
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{disease}{\code{NULL} (default, no overlay) or one \code{disease_id} present in
\code{patliRResults(proj, "disease_genes")}, e.g. \code{"MONDO_0005044"}.}

\item{view}{\code{"compound_ppi"} (default) or \code{"ppi"}. See the section on views.}

\item{colour_by}{\code{"disease"} (default) or \code{"module"}. See the section on
colours.}

\item{size_by}{\code{"compounds"} (default) or \code{"degree"}.}

\item{max_targets}{Maximum number of targets drawn (default \code{60}); \code{Inf}
draws every target that has a STRING partner.}

\item{top_n_labels}{Number of targets that get a text label (default
\code{30}); \code{0} labels none. Compounds are always labelled.}

\item{score_threshold}{Minimum STRING combined score (0-1000) of a PPI
edge. Default \code{700} ("high confidence").}

\item{disease_partners}{Integer, default \code{0}. When \verb{> 0} and \code{disease} is
given, up to this many disease genes that are \emph{not} predicted targets but
interact with at least two drawn targets are added as hollow red circles
-- the disease proteins the extract could reach through one PPI step.}

\item{disease_node}{Logical, default \code{FALSE}. When \code{TRUE} (and \code{disease} is
given) a single labelled disease node is drawn beside the network and
joined by dashed lines to every drawn disease gene.}

\item{highlight}{\code{"gpcr"} (default) marks G protein-coupled (metabotropic)
receptors and their interactions in red; \code{"none"} turns this off. See
the section on GPCRs.}

\item{highlight_set}{\code{NULL} (default) or a character vector of UniProt
accessions to highlight in red as well (or instead, with
\code{highlight = "none"}).}

\item{layout}{\code{"fr"} (Fruchterman-Reingold, default) or \code{"kk"}
(Kamada-Kawai) for the PPI graph.}

\item{seed}{Integer seed for the layout (default \code{1}), so the figure is
reproducible.}

\item{species}{NCBI taxonomy ID. Default \code{9606} (human).}

\item{version}{STRING version. Default \code{"12.0"}.}

\item{engine}{\code{"static"} (default) or \code{"ggiraph"} (tooltips on hover).}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{ggplot} (or \code{girafe}) object. \code{attr(., "summary")} is a
one-row \code{data.frame} (\code{n_targets}, \code{n_targets_mapped}, \code{n_ppi_edges}
among all targets, \code{n_no_partner}, \code{n_targets_drawn},
\code{n_ppi_edges_drawn}, \code{n_compounds_drawn}, \code{n_disease_genes},
\code{n_disease_genes_mapped}, \code{n_disease_hit}, \code{n_disease_hit_drawn},
\code{n_disease_overlap_string}, \code{n_universe}, \code{fold_enrichment},
\code{p_hypergeom}, \code{highlight}, \code{n_gpcr_targets}, \code{n_gpcr_drawn},
\code{n_gpcr_edges_drawn}, \code{score_threshold}); \code{attr(., "gpcr")} a
\code{data.frame} with one row per predicted target (\code{uniprot_id},
\code{gene_symbol}, \code{is_highlighted}, \code{source}, GO \code{evidence} codes,
\code{n_compounds}, \code{drawn}); and \code{attr(., "data")} a list with the drawn
\code{nodes}, \code{ppi_edges} (with a \code{gpcr_edge} flag), \code{compound_edges} and
\code{compounds}. With \code{save = TRUE} a PNG is written, logged to
\code{patliRResults(proj, "ppi_network_plot_log")} keyed by
\verb{(condition, disease_id, view, colour_by, variant)} -- \code{variant} is
\code{"default"} or names the non-default overlays (\code{disease_partners},
\code{disease_node}, \code{highlight = "none"}, \code{highlight_set}), which also go
into the file name -- and \code{attr(., "proj")} holds
the updated project.
}
\description{
Draws the proteins the compounds of one condition (or several, pooled) are
predicted to hit (\code{network_edges}), joined by the STRING protein-protein
interactions among them, and marks which of them belong to a disease gene
set (\code{disease_genes}). It is the step between the compound-target network
(\code{\link[=plot_network_layers]{plot_network_layers()}}) and the disease: which hit proteins interact
with each other, and whether the disease genes form a connected block
among them.

With a few hundred targets the full graph is unreadable, so the figure is
simplified by default:
\itemize{
\item Targets are ranked by (1) being a disease gene, (2) the number of the
extract's compounds predicted to hit them, (3) their STRING degree among
the extract's targets, and the top \code{max_targets} are drawn.
\item A target with no STRING partner (at \code{score_threshold}) among the drawn
proteins is not drawn; its slot goes to the next target in the ranking.
The subtitle states how many targets were left out and why.
\item Only \code{top_n_labels} targets are labelled (gene symbols): the
highest-ranked disease genes (at most 60\% of the labels) and the
best-connected other targets. Every node keeps its full name in the
\code{ggiraph} tooltip.
}
}
\section{Views}{

\itemize{
\item \code{"compound_ppi"} (default): the targets and their PPI edges in the
centre, the compounds as labelled diamonds on an outer ring, each
joined to the drawn targets it hits by a thin line. A compound sits on
the side of the ring nearest to its targets.
\item \code{"ppi"}: only the targets and their PPI edges (with the disease
overlay). Use it when the compound lines crowd the figure.
}
}

\section{Colours and sizes}{

\code{colour_by = "disease"} fills disease genes in reddish purple and other
targets in sky blue. \code{colour_by = "module"} fills targets by their
\code{\link[=network_module_robustness]{network_module_robustness()}} module (\code{network_module_membership},
colour-blind-safe Okabe-Ito palette, the same colours as
\code{\link[=plot_network_layers]{plot_network_layers()}}). In both cases a disease gene also gets a dark
purple ring, and an interaction between two disease genes is purple.
Node size is the number of compounds hitting the protein
(\code{size_by = "compounds"}) or its STRING degree in the drawn graph
(\code{size_by = "degree"}). PPI edge opacity follows the STRING combined
score.
}

\section{GPCRs (metabotropic receptors) in red}{

Many plant metabolites are predicted to bind G protein-coupled
(metabotropic) receptors, and such receptors are expected to be hit by
several compounds. With \code{highlight = "gpcr"} (default) these proteins are
drawn as squares with a red outline, their labels in red, and every
interaction with at least one GPCR endpoint as a thicker red line; the
subtitle counts them. Red is used only for this overlay (the disease
overlay is purple, a fill plus a ring), so both can be shown together.

A protein is a GPCR when \code{org.Hs.eg.db} annotates its UniProt accession
(or, failing that, its gene symbol) to GO:0004930 "G protein-coupled
receptor activity" or any descendant term, which includes the
metabotropic glutamate (mGluR, GO:0098988/GO:0001640) and GABA-B
(GO:0004965) receptors. GO:0008066 "glutamate receptor activity" is not
used because it also covers ionotropic receptors. Electronic (IEA)
annotations are kept: without them GO:0004930 covers only about 390 human
genes, fewer than half of the known human GPCRs. Genes supported only by
author statements (TAS/NAS/IC) are dropped, because those are mostly
pathway-level mis-annotations of non-receptors (e.g. PDGFRB, PPARG).
\code{highlight_set} adds any
UniProt accessions of your own (drawn the same way); with
\code{highlight = "none"} and \code{highlight_set} given, only your set is
highlighted. The classification of every predicted target is returned as
\code{attr(., "gpcr")}.
}

\section{Disease-gene enrichment}{

The summary reports a one-sided hypergeometric test of the overlap
between the extract's targets and the disease genes, both mapped to STRING,
with the STRING proteome of \code{species} (every protein in STRING's alias
table) as the universe. Target prediction tools only cover a part of the
proteome (proteins with known ligands), which this universe does not
account for, so read the p-value as descriptive, not as a calibrated test.
}

\section{STRING data}{

STRING's \code{protein.links} and \code{protein.aliases} flat files for \code{species}
and \code{version} are read from \verb{cacheDir(proj)/stringdb/} (downloaded once
from stringdb-downloads.org when absent -- the same files
\code{\link[=network_proximity]{network_proximity()}} uses). The links filtered at \code{score_threshold} are
cached there as \verb{ppi_links_<species>_<version>_<threshold>.rds}, so only
the first call pays for reading the full links file. UniProt accessions
that STRING does not know are counted as unmapped and never drawn.
}

\examples{
\dontrun{
proj <- patliR_load("my_project")
# needs network_build() and disease_genes_fetch() first
p <- plot_ppi_network(proj, condition = "EFLO-S", disease = "MONDO_0005044")
attr(p, "summary")
plot_ppi_network(proj, condition = "EFLO-S", disease = "MONDO_0005044",
                 view = "ppi", colour_by = "module", save = FALSE)
}

}
\seealso{
\code{\link[=plot_network_layers]{plot_network_layers()}}, \code{\link[=network_proximity]{network_proximity()}},
\code{\link[=disease_genes_fetch]{disease_genes_fetch()}}, \code{\link[=network_module_robustness]{network_module_robustness()}}
}
