% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rank.R
\name{plot_rank}
\alias{plot_rank}
\title{Visualise \code{rank_candidates()}'s output: a Pareto scatter, or a top
compounds x top targets heatmap}
\usage{
plot_rank(
  proj,
  condition = NULL,
  view = c("pareto", "heatmap"),
  x = NULL,
  y = NULL,
  top_n_compounds = 15,
  top_n_targets = 20,
  target_relevance = c("breadth", "weight", "centrality"),
  label = c("front", "all", "none"),
  engine = c("static", "ggiraph"),
  save = TRUE,
  out_dir = NULL,
  width = NULL,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{view}{\code{"pareto"} (default) or \code{"heatmap"}.}

\item{x, y}{Column name(s) from \code{rank_candidates}'s output for
\code{view = "pareto"}'s axes. \code{NULL} (default) uses \code{x = "rra_rank"} and
the priority default for \code{y} described above.}

\item{top_n_compounds}{Integer, default \code{15} -- \code{view = "heatmap"} only.}

\item{top_n_targets}{Integer, default \code{20} -- \code{view = "heatmap"} only.}

\item{target_relevance}{\code{"breadth"} (default), \code{"weight"}, or
\code{"centrality"} -- \code{view = "heatmap"} only, see the section above.}

\item{label}{\code{view = "pareto"} only: \code{"front"} (default) names every
Pareto-front-1 (non-dominated) compound; \code{"all"} names every compound
(front 1 in dark bold, the rest in grey); \code{"none"} draws no names.
Names are truncated and repelled (\code{ggrepel}), never dropped for
overlap.}

\item{engine}{\code{"static"} (default) or \code{"ggiraph"} -- \code{view = "pareto"}
only (\code{pheatmap} has no interactive engine, same contract difference
\code{\link[=plot_heatmap]{plot_heatmap()}} documents).}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
\code{view = "pareto"}: a \code{ggplot}/\code{girafe} object. \code{view = "heatmap"}:
the \code{pheatmap} object (\code{list(tree_row, tree_col, kmeans, gtable)}). Both
carry the updated \code{proj} as \code{attr(., "proj")} when \code{save = TRUE}.
}
\description{
\code{view = "pareto"} (default): a 2D scatter over \code{rank_candidates()}'s
output, coloured by \code{pareto_front} (the non-dominated tiering
\code{\link[=rank_candidates]{rank_candidates()}} already computed over every criterion that call
actually used -- not recomputed here, and not limited to the two plotted
axes). Front-1 (non-dominated) compounds are labelled by name. \code{x}
defaults to \code{"rra_rank"} (the single combined answer); \code{y} defaults to
whichever optional criterion is present first in this priority order:
\code{crit_synergy_best}, \code{crit_proximity_z}, \code{crit_module_r_index},
\code{crit_hub_penalty} (falling back to \code{crit_centrality} if none of those
ran) -- the criteria most likely to actually differentiate compounds,
since \code{x}/\code{y} are only ever
an illustrative 2D placement of a decision that used every criterion, not
the decision itself. Both are freely overridable to any numeric column
of \code{patliRResults(proj, "rank_candidates")} (any \verb{crit_*}, \verb{rank_*},
\code{rra_score}, \code{rra_rank}, or \code{n_criteria_used}).

\code{view = "heatmap"}: compound x target matrix restricted to the top
\code{top_n_compounds} compounds (by \code{rra_rank}) and the \code{top_n_targets} most
relevant targets among that restricted set (\code{target_relevance}), filled
by \code{\link[=network_build]{network_build()}}'s edge weight -- delegates to the same
\code{\link[pheatmap:pheatmap]{pheatmap::pheatmap()}} rendering \code{\link[=plot_heatmap]{plot_heatmap()}} uses (via the shared
\code{.plot_pheatmap_render()} helper), just on a subset. Answers "which
targets do the best compounds actually converge on", which the full,
every-compound x every-target matrix in \code{plot_heatmap(what = "compound_target")} is too dense to show.
}
\section{\code{target_relevance} (heatmap view only)}{

\describe{
\item{\code{"breadth"} (default)}{Number of distinct top-\code{top_n_compounds}
compounds hitting each target -- the targets several best compounds
converge on, the mechanistically interesting overlap.}
\item{\code{"weight"}}{Sum of \code{\link[=network_build]{network_build()}} edge weight (import
probability) across the top compounds, per target.}
\item{\code{"centrality"}}{The target's own composite centrality (same
\code{rowMeans()} of available \verb{*_norm} columns \code{\link[=rank_candidates]{rank_candidates()}}'s
\code{"centrality"} criterion uses), independent of which top compounds
hit it. Requires \code{network_centrality}.}
}
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
# ... build the project, run network_build/network_centrality/adme_local/
# adme_filter/rank_candidates() ...
plot_rank(proj, condition = "FLO-ET", view = "pareto", save = FALSE)
plot_rank(proj, condition = "FLO-ET", view = "heatmap", save = FALSE)
}

}
\seealso{
\code{\link[=rank_candidates]{rank_candidates()}}, \code{\link[=plot_heatmap]{plot_heatmap()}}
}
