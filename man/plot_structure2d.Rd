% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_structure2d.R
\name{plot_structure2d}
\alias{plot_structure2d}
\title{Grid of 2D structure depictions from \code{prep_structure2d()}}
\usage{
plot_structure2d(
  proj,
  compound_ids = NULL,
  ncol = 4,
  save = TRUE,
  out_dir = NULL,
  width = 10,
  height = 8,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id}, or \code{NULL}
(default) for every compound with a successful (\code{generated_ok})
\code{structure2d_log} entry.}

\item{ncol}{Integer, default \code{4}. Number of grid columns.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{patchwork} object (see \code{\link[=plot_upset]{plot_upset()}} for why \code{patchwork},
not a single \code{ggplot}). If \code{save = TRUE} (default), also writes a PNG
and logs it to \code{patliRResults(proj, "structure2d_grid_log")}.
}
\description{
Arranges the PNG files \code{\link[=prep_structure2d]{prep_structure2d()}} already rendered per
compound into one grid figure, each panel captioned with the
compound's name (falling back to \code{compound_id}). Purely an assembly
step -- does not regenerate any depiction itself; run
\code{\link[=prep_structure2d]{prep_structure2d()}} first.
}
\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- prep_structure2d(proj)
plot_structure2d(proj, save = FALSE)
}

}
