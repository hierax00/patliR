% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_target_chord.R
\name{plot_target_chord}
\alias{plot_target_chord}
\title{Arc diagram of a condition's target-target STRING "actions" adjacency}
\usage{
plot_target_chord(
  proj,
  condition = NULL,
  actions_score_threshold = 400,
  top_n_labels = 15,
  engine = c("static", "ggiraph"),
  save = TRUE,
  out_dir = NULL,
  width = 8,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{actions_score_threshold}{Single number in \verb{[0, 999]}, default
\code{400} -- the same STRING confidence default \code{\link[=network_proximity]{network_proximity()}} /
\code{\link[=network_synergy]{network_synergy()}} use for the \emph{undirected} interactome (the actions
file's own \code{score} column uses the same 0-999 scale). Only has an
effect when a per-edge \code{score} could be recovered -- see the section
above; a \code{cli_inform} names the fallback when it could not.}

\item{top_n_labels}{Integer, default \code{15}. The \code{top_n_labels}
highest-degree targets (ties broken by \code{uniprot_id}, for a
deterministic order) get a text label; every other plotted target
gets an unlabeled tick mark instead.}

\item{engine}{\code{"static"} (default) or \code{"ggiraph"}, save/out_dir/width/
height/dpi -- same as \code{\link[=plot_network_layers]{plot_network_layers()}}.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{ggplot} object (\code{engine = "static"}) or a \code{girafe} htmlwidget
(\code{engine = "ggiraph"}). If \code{save = TRUE} (default), also writes a PNG
and logs it to \code{patliRResults(proj, "target_chord_plot_log")}.
}
\description{
network-design-spec.md Sec 3.9: nodes are a condition's targets, placed
along a single horizontal line (ordered by degree, ascending
left-to-right) rather than around a circle -- the spec's own
recommendation is "do the arc version, \link{...} revisit \code{circlize} only if
a user asks for the circular one." One arc is drawn above the line per
pair of targets connected in the STRING directed "actions" network
\code{\link[=network_bowtie]{network_bowtie()}} already downloads and parses (same file, same
\code{actions_version} default \code{"11.0"}, same species default \code{9606};
direction is dropped here -- this plot asks only "do these two targets
interact", not "which one acts on the other", which is what
\code{\link[=network_bowtie]{network_bowtie()}}'s bow-tie decomposition is for). A subset of the
highest-degree targets (\code{top_n_labels}) is labeled via
\code{\link[=.plot_label_nodes]{.plot_label_nodes()}}; every other plotted target gets an unlabeled tick,
to keep a condition with dozens of targets legible.
}
\section{Arc colour -- when a per-edge STRING score is, and is not, available}{

\code{\link[=network_bowtie]{network_bowtie()}}'s cached actions graph
(\code{.network_stringdb_actions_graph()}, internal) keeps only
\code{item_id_a}/\code{item_id_b} when it parses the STRING actions flat file --
the file's own \code{score} column (0-999, per-interaction confidence) is
dropped, since \code{\link[=network_bowtie]{network_bowtie()}} itself has no use for it, and
\code{network_bowtie.R} is used unmodified here. This function makes a
best-effort second, independent pass over the \emph{same}, already
downloaded/cached raw actions flat file (same cache path, same
\code{is_directional}/\code{a_is_acting} filter, reusing
\code{\link[=network_bowtie]{network_bowtie()}}'s own \code{.network_actions_flag_true()}) to recover
\code{score} when that raw file is still present on disk. When it is not
(e.g. only the parsed-graph \code{.rds} cache survived a cache cleanup, or in
every automated test, which mocks the actions graph directly and never
downloads anything), \code{actions_score_threshold} has no effect and every
arc is drawn in one constant colour -- exactly the fallback
network-design-spec.md Sec 3.9 itself anticipates ("otherwise a
constant colour").
A present but unreadable or invalid raw score file raises an error;
confidence filtering is never silently bypassed for a corrupt file.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
# needs STRINGdb + internet on first call (downloads/caches the STRING
# "actions" flat file, same as network_bowtie())
plot_target_chord(proj, condition = "FLO-ET", save = FALSE)
}

}
\references{
Krzywinski et al. (2009), \emph{Genome Res} 19(9):1639-1645,
\doi{10.1101/gr.092759.109}.
}
