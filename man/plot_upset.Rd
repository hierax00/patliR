% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_upset.R
\name{plot_upset}
\alias{plot_upset}
\title{UpSet-style plot: target set overlaps across conditions}
\usage{
plot_upset(
  proj,
  condition = NULL,
  top_n = 15,
  save = TRUE,
  out_dir = NULL,
  width = 8,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{top_n}{Integer, default \code{15}. Only the \code{top_n} largest
intersections are shown -- with more than a handful of conditions the
number of possible combinations grows fast.}
}
\value{
A \code{patchwork} object (bar chart on top of the dot-matrix,
\code{print()}-able and \code{ggsave()}-able like a single \code{ggplot}). If
\code{save = TRUE} (default), also writes a PNG and logs it to
\code{patliRResults(proj, "upset_plot_log")}.
}
\description{
Which targets are shared across which conditions (extracts), and how
many. Builds a \verb{condition x target} presence matrix from
\code{network_edges}, groups targets by their exact combination of
conditions ("intersection"), and shows the \code{top_n} largest
intersections as a bar chart (size) stacked on a dot-matrix (which
conditions make up that intersection) -- the standard UpSet layout
(Lex et al. 2014), hand-rolled in \code{ggplot2} + \code{patchwork} rather than
pulling in \code{UpSetR}/\code{ComplexUpset}.
}
\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
plot_upset(proj, save = FALSE)
}

}
