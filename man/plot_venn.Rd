% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_venn.R
\name{plot_venn}
\alias{plot_venn}
\title{Venn diagram: compound targets vs. disease-associated targets}
\usage{
plot_venn(
  proj,
  condition = NULL,
  disease = NULL,
  sets = c("compound_targets", "disease_targets"),
  save = TRUE,
  out_dir = NULL,
  width = 6,
  height = 6,
  dpi = 150
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{disease}{Character EFO ID from \code{targets_disease$disease_id}. If
\code{NULL} (default) and exactly one disease is present in
\code{patliRResults(proj, "targets_disease")}, that one is used; otherwise
required.}

\item{sets}{Character vector of exactly the two sets to compare,
currently only \code{c("compound_targets", "disease_targets")} (default,
and the only supported value) -- kept as an explicit argument, not
hardcoded, so a third set (e.g. STRING bowtie core membership) can be
added later without changing the function's contract.}

\item{save}{Logical, default \code{TRUE}. Whether to also write the plot to
disk (in addition to returning it).}

\item{out_dir}{Directory to write the file(s) to when \code{save = TRUE}.
\code{NULL} (default) means \code{file.path(projectDir(proj), "plots")}.}

\item{width, height}{Numeric, plot size in inches, passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (or the function's native device).}

\item{dpi}{Numeric resolution in dots per inch for raster output, passed
to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \code{ggplot} object (\code{ggVennDiagram} builds on \code{ggplot2}, no
separate \code{engine} argument). If \code{save = TRUE} (default), also writes
a PNG and logs it to \code{patliRResults(proj, "venn_plot_log")}.
}
\description{
Two-set overlap: \code{"compound_targets"} (every UniProt ID any compound
hits in \code{condition}'s \code{network_edges}) against \code{"disease_targets"}
(every UniProt ID associated with \code{disease} in \code{targets_disease}, from
\code{\link[=targets_disease_filter]{targets_disease_filter()}}). The overlap is literally "this extract's
targets that are also known disease genes for this indication" -- a
quick sanity check before running the heavier, statistically rigorous
version of the same question, \code{\link[=network_proximity]{network_proximity()}}.
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- targets_disease_filter(proj, disease = "type 2 diabetes mellitus")
plot_venn(proj, condition = "FLO-ET", save = FALSE)
}

}
