% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_binarize.R
\name{prep_as_condition}
\alias{prep_as_condition}
\title{Treat a compound list as a single experimental condition (no abundance matrix)}
\usage{
prep_as_condition(proj, condition = "all", compound_ids = NULL)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character scalar, the condition name to create (default
\code{"all"}).}

\item{compound_ids}{Character vector of \code{compounds(proj)$id} to mark
present, or \code{NULL} (default) for every compound currently in
\code{\link[=compounds]{compounds()}}.}
}
\value{
The updated \code{proj}, with \code{\link[=binarizedMatrix]{binarizedMatrix()}} (and a matching
\code{\link[=matrixRaw]{matrixRaw()}} of all \code{1}s) carrying a single \code{condition} column, and
\verb{02_matrix_raw.csv} / \verb{03_binarized.csv} written. Re-running with a
different \code{condition} adds a column rather than replacing the matrix.
}
\description{
The \verb{network_*} family keys everything off \code{\link[=binarizedMatrix]{binarizedMatrix()}} -- one
network per condition column. When you have a plain compound list rather
than a GC-MS abundance matrix (Scenario B, \code{\link[=prep_compounds]{prep_compounds()}}), there is
no matrix and no conditions, so \code{network_build()} has nothing to fork on.
\code{prep_as_condition()} fills that gap: it marks every compound (or a
subset) as present in one named condition, so the rest of the pipeline
-- \verb{network_*}, \verb{plot_*} -- runs on the list as if it were a single
extract. This is the entry point for a list-only, exploratory flow.
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- prep_as_condition(proj, condition = "my_extract")
binarizedMatrix(proj)

}
\seealso{
\code{\link[=prep_binarize]{prep_binarize()}} for a real replicate-abundance matrix.
}
