% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_binarize.R
\name{prep_binarize}
\alias{prep_binarize}
\title{Average replicates and binarize a raw abundance matrix (presence/absence)}
\usage{
prep_binarize(proj, data, id_col = "Name", average_replicates = TRUE, q = 0.25)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{data}{A \code{data.frame} with one identifier column (\code{id_col}) and one
or more \verb{R<n>-<CONDITION>} columns.}

\item{id_col}{Character scalar. Name of the compound identifier column in
\code{data} (default \code{"Name"}). If \code{\link[=compounds]{compounds()}} already has entries, each
value is matched against \code{compounds(proj)$name} (case- and
whitespace-insensitive); unmatched rows are still included (using the
raw identifier from \code{data}) but logged as \code{"compound_not_found"}, so
this function never blocks on \code{\link[=prep_compounds]{prep_compounds()}} not having run yet.}

\item{average_replicates}{Logical. If \code{TRUE} (default), replicate columns
sharing the same condition are averaged. If \code{FALSE}, every condition
must already have exactly one column (no \verb{R<n>} replication) or this
errors.}

\item{q}{Numeric scalar in \verb{(0, 1)}. Quantile used as the per-condition
presence threshold (default \code{0.25}, i.e. Q1).}
}
\value{
The updated \code{proj}, with \code{\link[=matrixRaw]{matrixRaw()}} and \code{\link[=binarizedMatrix]{binarizedMatrix()}}
set (never overwriting one with the other) and \verb{02_matrix_raw.csv} /
\verb{03_binarized.csv} written inside \code{\link[=projectDir]{projectDir()}}.
}
\description{
\code{prep_binarize()} is the main entry point of the \code{patliR} pipeline for
\strong{Scenario A}: a raw abundance matrix (e.g. GC-MS peak areas) with
compounds in rows and one column per replicate-condition combination. For
an already-curated compound table (\strong{Scenario B}), use
\code{\link[=prep_compounds]{prep_compounds()}} instead.

Condition columns must follow the pattern \verb{R<n>-<CONDITION>} (replicate
first, e.g. \code{R1-LEA-ET}, \code{R2-FLO-AQ}; the condition itself may contain
hyphens). Replicates are averaged per condition, and a per-condition
first-quartile (Q1) threshold is applied to the \emph{averaged} values to call
presence/absence: a compound is called present (\code{1}) in a condition if
its average abundance is greater than 0 and at or above that condition's
Q1 across all compounds; otherwise absent (\code{0}). Columns that are already
binary (only \code{0}/\code{1} values) are detected and passed through unchanged
(no Q1 thresholding applied), and this is recorded in the log.
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
matrixRaw(proj)
binarizedMatrix(proj)

}
\seealso{
\code{\link[=prep_compounds]{prep_compounds()}} for Scenario B (already-curated compound
tables).
}
