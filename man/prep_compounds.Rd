% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_compounds.R
\name{prep_compounds}
\alias{prep_compounds}
\title{Import and validate compound identity}
\usage{
prep_compounds(
  proj,
  data,
  identifier = c("pubchem", "smiles"),
  id_col = NULL,
  name_col = NULL,
  dedup = TRUE,
  on_missing_smiles = c("abort", "fetch", "drop"),
  fetch_mode = c("warn_and_cache", "abort")
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object, typically from
\code{\link[=patliR_project]{patliR_project()}}.}

\item{data}{A \code{data.frame} with one row per compound.}

\item{identifier}{\code{"pubchem"} (default) if \code{data} is keyed by PubChem CID
(column \code{id_col}, default \code{"PubChemCID"}); \code{"smiles"} if \code{data} is keyed
directly by a SMILES column (default \code{"SMILES"}). In both cases, if a
\code{SMILES} column is present it is always used directly (no network
lookup needed) -- \code{identifier} only changes what the \emph{default} \code{id_col}
is. \code{on_missing_smiles} (below) applies the same way regardless of
\code{identifier}: any row with a \code{PubChemCID} (whether or not that is what
you set \code{identifier} to) but no SMILES is eligible for the PubChem
fetch when \code{on_missing_smiles = "fetch"}.}

\item{id_col}{Character scalar or \code{NULL}. Name of the identifier column
in \code{data}. Defaults to \code{"PubChemCID"} when \code{identifier = "pubchem"} and
to \code{"SMILES"} when \code{identifier = "smiles"}.}

\item{name_col}{Character scalar or \code{NULL}. Name of the compound-name
column in \code{data}. Defaults to \code{"Name"} if present, else \code{NA} names are
used.}

\item{dedup}{Logical. If \code{TRUE} (default), duplicate compounds (matched
by canonical SMILES, against both \code{data} itself and any compounds
already in \code{proj}) are removed, keeping the first occurrence, and
logged as \code{"duplicate"}.}

\item{on_missing_smiles}{What to do with rows that have an identifier but
no SMILES. \code{"abort"} (default): stop with an informative error, since
silently guessing structure is unsafe. \code{"fetch"}: try to resolve SMILES
from PubChem via the \pkg{webchem} package (Suggests), following the
\code{abort}/\code{warn_and_cache} policy of \code{.fetch_external()} (internal)
with \code{fetch_mode}. \code{"drop"}: exclude these rows and log them as
\code{"smiles_not_resolved"}.}

\item{fetch_mode}{\code{"warn_and_cache"} (default) or \code{"abort"}; only used
when \code{on_missing_smiles = "fetch"}. See \code{.fetch_external()} (internal).}
}
\value{
The updated \code{proj}, with \code{\link[=compounds]{compounds()}} extended and
\verb{01_compounds.csv} (re)written inside \code{\link[=projectDir]{projectDir()}}.
}
\description{
\code{prep_compounds()} is the main entry point of the \code{patliR} pipeline for
\strong{Scenario B}: a curated table of compounds (one row per compound,
already identified by name/CAS/PubChem CID and, ideally, SMILES). For raw
GC-MS abundance matrices (\strong{Scenario A}), start with \code{\link[=prep_binarize]{prep_binarize()}}
instead.

Every row is validated as a real chemical structure (via \code{rcdk}), given a
canonical SMILES (CDK's own canonicalization -- see the note in
\code{.check_structures()}, internal, for why this is not an InChIKey),
checked for duplicates, and appended to
\code{\link[=compounds]{compounds()}}. Rows that fail any of these checks are \strong{never silently
dropped}: they are excluded from \code{compounds()} but always recorded in
\code{\link[=projectLog]{projectLog()}} with the reason (\code{"invalid_structure"},
\code{"duplicate"}, or \code{"smiles_not_resolved"}).
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
compounds(proj)
projectLog(proj)

}
\seealso{
\code{\link[=prep_compound]{prep_compound()}} for a single compound, \code{\link[=prep_binarize]{prep_binarize()}} for
Scenario A (raw abundance matrices).
}
