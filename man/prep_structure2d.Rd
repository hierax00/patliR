% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_structure2d.R
\name{prep_structure2d}
\alias{prep_structure2d}
\title{Generate and QC 2D structure depictions for every compound}
\usage{
prep_structure2d(proj, engine = c("rcdk", "chemminer"), out_dir = NULL)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{engine}{\code{"rcdk"} (default): use \code{rcdk}'s own 2D depiction, no extra
dependency. \code{"chemminer"}: use \pkg{ChemmineR} (Suggests) if installed,
useful as a second opinion when \code{rcdk} disagrees with what you expect.}

\item{out_dir}{Directory to write the PNG files to. Defaults to
\code{file.path(projectDir(proj), "structure2d")}.}
}
\value{
The updated \code{proj}, with a \code{structure2d_log} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{id}, \code{generated_ok}, \code{engine_used},
\code{path}), also written to \code{results/structure2d_log.csv}.
}
\description{
Renders a 2D depiction (PNG) for every compound currently in
\code{\link[=compounds]{compounds()}} and records whether generation succeeded. This is a visual
QC step, additional to (and independent from) the structural validation
\code{\link[=prep_compounds]{prep_compounds()}} already performs via \code{rcdk} -- it exists to catch
structures that parse correctly but depict in a degenerate or unexpected
way (disconnected fragments, wrong valence rendering, etc.), and to give
you a quick sanity-check image per compound before moving further down
the pipeline.
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- prep_structure2d(proj)
patliRResults(proj, "structure2d_log")
}

}
