% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_candidates.R
\name{rank_candidates}
\alias{rank_candidates}
\title{Rank candidate compounds by Robust Rank Aggregation over network/ADME
criteria}
\usage{
rank_candidates(
  proj,
  condition = NULL,
  disease = NULL,
  criteria = NULL,
  roll_up = c("weighted_mean", "mean", "max"),
  top_n = 15,
  export = c("none", "sdf", "smi")
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{disease}{\code{NULL} (default, auto-resolve when unambiguous) or a
\code{disease_id} string identifying which \code{\link[=network_proximity]{network_proximity()}} /
\code{\link[=network_synergy]{network_synergy()}} run to use for the \code{"proximity"}/\code{"synergy"}
criteria.}

\item{criteria}{\code{NULL} (default, auto-detect: use every criterion with
data) or a character vector naming a subset of \code{"adme"},
\code{"centrality"}, \code{"hub_penalty"}, \code{"proximity"}, \code{"synergy"},
\code{"module_robustness"} to restrict to (the two mandatory ones must be
included).}

\item{roll_up}{\code{"weighted_mean"} (default), \code{"mean"}, or \code{"max"} -- see
the roll-up section above.}

\item{top_n}{How many top-\code{rra_rank} compounds \code{export} writes out.
Default \code{15}.}

\item{export}{\code{"none"} (default), \code{"sdf"}, or \code{"smi"}. When not
\code{"none"}, writes \verb{results/rank_candidates_top<top_n>_<condition>.<ext>}
per condition from the compounds' own SMILES (via the same
\code{.parse_smiles_safe()} identity chain \code{\link[=compounds_similarity]{compounds_similarity()}} uses --
no re-lookup, no new dependency). \code{"sdf"} uses
\code{rcdk::write.molecules()}; \code{"smi"} writes plain \verb{SMILES<tab>compound_id}
lines (\code{write.molecules()} always emits an SDF mol block regardless of
the file extension -- verified directly against the installed \code{rcdk}
before relying on it here -- so \code{"smi"} is built by hand from
\code{rcdk::get.smiles()}).}
}
\value{
The updated \code{proj}, with a \code{rank_candidates} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{condition}, \code{compound_id}, one \verb{crit_*} per
criterion used anywhere in this call, one paired \verb{rank_*},
\code{n_p2_partners}, \code{n_criteria_used}, \code{rra_score}, \code{rra_rank},
\code{pareto_front}), also written to \code{results/rank_candidates.csv}.
}
\description{
Combines up to 6 per-compound criteria into one ranked table using
Robust Rank Aggregation (RRA; Kolde, Laur, Adler & Vilo 2012,
\emph{Bioinformatics} 28(4):573-580). RRA compares each criterion's \emph{rank}
ordering, not its raw values, so a criterion with a broken or skewed
scale cannot silently dominate the others -- the standard failure mode
of a weighted linear sum of z-scored criteria. Two criteria are
mandatory (\code{"adme"}, \code{"centrality"}); the rest (\code{"hub_penalty"},
\code{"proximity"}, \code{"synergy"}, \code{"module_robustness"}) are auto-detected from
\code{\link[=patliRResults]{patliRResults()}} and silently degrade -- a project that never ran
\code{\link[=network_synergy]{network_synergy()}} still gets a ranking, just over fewer criteria, and
\code{\link[=projectLog]{projectLog()}} records exactly which criteria were used vs skipped for
each condition.
}
\section{Criteria}{

Direction is already normalised to "higher = better" internally for
ranking purposes; the \emph{raw} value, in its own natural direction, is what
the output table actually shows.
\describe{
\item{\code{"adme"} (mandatory)}{\code{crit_adme_pass_frac} -- fraction of
\code{\link[=adme_filter]{adme_filter()}}'s selected rules a compound passes, from
\code{adme_filtered}. Aborts if \code{adme_filtered} is absent (run
\code{\link[=adme_local]{adme_local()}} then \code{\link[=adme_filter]{adme_filter()}} first).}
\item{\code{"centrality"} (mandatory)}{\code{crit_centrality} -- for each of a
compound's own predicted targets, \code{rowMeans()} of whichever of
\code{degree_norm} / \code{betweenness_norm} / \code{hub_score_component} are
non-\code{NA} in \code{network_centrality}, then rolled up to the compound via
\code{roll_up}. Aborts if \code{network_centrality} was run with
\code{normalize = FALSE} (no normalised columns to build a composite
from) -- re-run it with \code{normalize = TRUE}.}
\item{\code{"hub_penalty"} (optional)}{\code{crit_hub_penalty} --
\code{network_hub_penalty}'s \code{score_adjusted}, rolled up target->compound.}
\item{\code{"proximity"} (optional)}{\code{crit_proximity_z} --
\code{network_proximity}'s \code{z_score} (lower/more negative is better --
the only criterion whose raw scale runs the opposite way; direction
is flipped internally before ranking).}
\item{\code{"synergy"} (optional)}{\code{crit_synergy_best} -- for each compound,
the best (\code{max}) \code{synergy_score} among every partner it was scored
against in \code{network_synergy} (its best achievable complementary
pairing, not an average across all partners). \code{n_p2_partners} (count
of \code{cheng_class == "P2"} partnerships) is kept as an \emph{informational}
column only -- not fed to the RRA, since it is a different view of
the same underlying table as \code{crit_synergy_best} and including both
would double-count one signal.}
\item{\code{"module_robustness"} (optional)}{\code{crit_module_r_index} -- the
\code{r_index} of the module a compound belongs to (direct join via
\code{network_module_membership}, not an aggregation -- one compound has
exactly one module per condition).}
}
\code{"proximity"} and \code{"synergy"} both key off a \code{disease_id}; if \code{disease}
is \code{NULL} and \code{network_proximity} holds results for exactly one disease
for a condition, that one is used automatically -- with more than one,
\code{disease} must be supplied explicitly (same policy \code{\link[=network_synergy]{network_synergy()}}
already uses for its own \code{disease} argument).
}

\section{Roll-up (target->compound, \verb{roll_up=})}{

\code{"weighted_mean"} (default) -- weighted by the \code{\link[=network_build]{network_build()}} edge
weight (import probability) for each \verb{(compound_id, uniprot_id)} pair,
so a compound's score reflects its whole predicted target profile
weighted by confidence rather than being hijacked by one high-scoring
but low-probability predicted target. \code{"mean"} and \code{"max"} are also
available, always explicit -- never a hidden default.
}

\section{Missing data}{

Two different situations get two different treatments. A whole
\emph{optional} table absent from the project (e.g. \code{\link[=network_synergy]{network_synergy()}} never
run) drops that criterion entirely for the affected condition(s) --
logged, not imputed (imputing a constant discount for a criterion nobody
computed would be a silent no-op dressed up as data). A table that
exists but lacks a value for one particular compound (e.g. a mono-target
compound with no \code{\link[=network_synergy]{network_synergy()}} partner) gets \strong{worst-rank
imputation} on that one criterion -- the standard convention for
incomplete rank lists under RRA, implemented via
\code{order(..., na.last = TRUE)} so the compound is tied last on that axis
rather than silently dropped. \code{n_criteria_used} records, per compound,
how many criteria it had a \emph{real} (non-imputed) value for.
}

\section{Output columns not otherwise documented above}{

\verb{rank_<criterion>} -- one per \verb{crit_*} column actually used for this
condition, 1 = best, direction already normalised. \code{rra_score} --
\code{RobustRankAggreg::aggregateRanks()}'s output (lower = more robustly
top-ranked across every criterion used). \code{rra_rank} --
\code{rank(rra_score, ties.method = "min")}, the table's primary sort key.
\code{pareto_front} -- integer tier (\code{1} = non-dominated) from a from-scratch
non-dominated sort (NSGA-II-style; O(n^2) per tier, fine at the
candidate-list scale this function targets -- tens to a few hundred
compounds) over every \verb{crit_*} column actually used, with a missing
value treated as \code{-Inf} (worst), the same convention as the RRA
imputation. See \code{\link[=plot_rank]{plot_rank()}} for the companion visualisations.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
abundance <- read.csv(
  system.file("extdata", "input_abundance_matrix.csv", package = "patliR"),
  check.names = FALSE
)
proj <- prep_binarize(proj, abundance)
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
proj <- network_build(proj)
proj <- network_centrality(proj)
proj <- adme_local(proj)
proj <- adme_filter(proj)
proj <- rank_candidates(proj)
patliRResults(proj, "rank_candidates")
}

}
\references{
Kolde, Laur, Adler & Vilo (2012), \emph{Bioinformatics} 28(4):573-580,
\doi{10.1093/bioinformatics/btr709}.
}
\seealso{
\code{\link[=plot_rank]{plot_rank()}}
}
