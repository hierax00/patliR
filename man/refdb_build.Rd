% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refdb.R
\name{refdb_build}
\alias{refdb_build}
\title{Build the local reference database of compound identity and bioactivity}
\usage{
refdb_build(
  proj,
  sources = c("pubchem", "chembl", "coconut"),
  compound_ids = NULL,
  fetch_mode = c("warn_and_cache", "abort")
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{sources}{Character vector, any of \code{"pubchem"}, \code{"chembl"},
\code{"coconut"}.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id} to include,
or \code{NULL} (default) for all compounds currently in \code{proj}.}

\item{fetch_mode}{\code{"warn_and_cache"} (default) or \code{"abort"}. See
\code{.fetch_external()} (internal): this is enrichment
data, so failures do not stop the pipeline by default.}
}
\value{
The updated \code{proj}, with \code{reference_compounds} and
\code{reference_bioactivity} entries in \code{\link[=patliRResults]{patliRResults()}}, also written to
\code{reference_compounds.csv} / \code{reference_bioactivity.csv} inside
\code{\link[=projectDir]{projectDir()}}.
}
\description{
\code{refdb_build()} populates \code{reference_compounds.csv} and
\code{reference_bioactivity.csv} inside \code{\link[=projectDir]{projectDir()}} for the compounds
currently in \code{\link[=compounds]{compounds()}}, pulling from public sources. This reference
database is what later families (\verb{bias_*}, \code{network_degeneracy()}) treat
as "the background" -- it grows across an entire \code{patliR} install if you
point several projects at the same \code{project_dir} root, or stays
project-local otherwise; that choice is yours.

Only sources with a real, confirmed REST API are fetched automatically
(\code{"pubchem"}, \code{"chembl"}). \code{"coconut"} is accepted as a source name for
forward-compatibility but is not implemented yet in this version --
request it once \code{coconut_fetch()} (natural-product family) ships; asking
for it now raises an informative error rather than pretending to
succeed.
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- refdb_build(proj, sources = c("pubchem", "chembl")) # needs internet
}

}
