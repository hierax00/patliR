% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refdb.R
\name{refdb_update}
\alias{refdb_update}
\title{Incrementally update the reference database for new compounds}
\usage{
refdb_update(
  proj,
  compound_ids,
  sources = c("pubchem", "chembl"),
  fetch_mode = c("warn_and_cache", "abort")
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id} to fetch.
Required (unlike in \code{\link[=refdb_build]{refdb_build()}}, there is no "all" default, to
avoid accidentally re-fetching everything).}

\item{sources}{Character vector, any of \code{"pubchem"}, \code{"chembl"},
\code{"coconut"}.}

\item{fetch_mode}{\code{"warn_and_cache"} (default) or \code{"abort"}. See
\code{.fetch_external()} (internal): this is enrichment
data, so failures do not stop the pipeline by default.}
}
\value{
The updated \code{proj}. See \code{\link[=refdb_build]{refdb_build()}}.
}
\description{
Same fetch logic as \code{\link[=refdb_build]{refdb_build()}}, restricted to (and additive for)
the given compounds -- use this after adding new compounds with
\code{\link[=prep_compounds]{prep_compounds()}} instead of rebuilding the whole reference database
from scratch.
}
