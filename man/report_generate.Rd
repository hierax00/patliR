% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_generate.R
\name{report_generate}
\alias{report_generate}
\title{Generate a self-contained HTML report per condition}
\usage{
report_generate(proj, condition = NULL, out_dir = NULL, top_n = 15)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{condition}{Character vector of condition names (must match columns
of \code{\link[=binarizedMatrix]{binarizedMatrix()}}), or \code{NULL} (default) to (re)build every
condition. Rebuilding a subset leaves previously-built conditions
untouched in \code{patliRResults(proj, "network_edges")}.}

\item{out_dir}{Directory to write the report(s) to. \code{NULL} (default) ->
\code{file.path(projectDir(proj), "reports")}.}

\item{top_n}{How many rows of the bias-audit "promiscuous" table and the
\code{\link[=rank_candidates]{rank_candidates()}} ranking to show. Default \code{15}.}
}
\value{
The updated \code{proj}, with a \code{report_log} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{condition}, \code{path}), also written to
\code{results/report_log.csv}. Each condition's HTML file is written to
\verb{out_dir/report_<condition>.html}.
}
\description{
One plain HTML file per condition, covering compounds present, ADME
filtering, the database-bias audit, modular robustness, the
\code{\link[=rank_candidates]{rank_candidates()}} ranking, and the full \code{\link[=projectLog]{projectLog()}} -- each section
auto-included when its source table exists in \code{\link[=patliRResults]{patliRResults()}} and
replaced by a one-line "not run" placeholder otherwise, so a project that
skipped a step still gets a report, just a shorter one. Deliberately
plain HTML (base R string building, no \code{rmarkdown}/\code{pandoc}, no CSS
framework) -- see the file header for why.
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
# ... build the project, run whichever of adme_local/adme_filter,
# bias_audit/bias_reweight, network_module_robustness, rank_candidates ...
proj <- report_generate(proj)
patliRResults(proj, "report_log")
}

}
