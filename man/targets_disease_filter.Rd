% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets_disease.R
\name{targets_disease_filter}
\alias{targets_disease_filter}
\title{Filter/annotate predicted targets by their association to a disease}
\usage{
targets_disease_filter(
  proj,
  disease,
  source = c("open_targets"),
  min_score = NULL,
  fetch_mode = c("warn_and_cache", "abort")
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{disease}{A disease or phenotype name (e.g. \code{"hypertension"}) or an
existing, \emph{currently valid} ontology identifier (EFO/MONDO/Orphanet/etc.,
e.g. \code{"MONDO_0005044"}, hypertension's current MONDO ID). Free text is
the safer default -- ontology IDs do get retired (e.g. hypertension's
old EFO ID, \code{EFO_0000537}, was obsoleted in EFO 3.88.0 and replaced by
\code{MONDO_0005044}; other catalogs, like GWAS Catalog, still link the old
one, but Open Targets does not), and passing an obsolete ID fails with
no error from the API itself -- just an empty result -- which this
function surfaces as a clear "could not resolve" error rather than
silently returning nothing (see \code{.open_targets_resolve_disease()},
internal). An ID-shaped string (\code{PREFIX_alphanumeric}) is looked up
directly and validated against Open Targets; anything else (free text)
is resolved via Open Targets' \code{mapIds} full-text search (same engine
behind their web search box), which always resolves to the \emph{current}
term -- ambiguous free-text terms take the top-ranked hit
(\code{mapIds} does not match on the ID string itself,
confirmed against the live API -- this is why the two paths are
separate rather than always going through \code{mapIds}). Required: this
function only makes sense for one disease at a time, call it again for
another.}

\item{source}{Only \code{"open_targets"} is implemented. Kept as an explicit
argument (rather than hardcoding the source) so a future alternative
source raises an informative error instead of silently being ignored --
see \code{\link[=refdb_build]{refdb_build()}} for the same pattern with \code{"coconut"}.}

\item{min_score}{\code{NULL} (default): keep every UniProt ID that was
successfully queried, whether or not Open Targets reports an
association (rows with no association get \code{association_score = NA} and
are dropped, since there is nothing left to rank -- this is always
logged). A numeric threshold in \verb{[0, 1]}: additionally drop rows whose
\code{association_score} is below it (also always logged, by UniProt ID and
score, so nothing disappears silently).}

\item{fetch_mode}{\code{"warn_and_cache"} (default) or \code{"abort"}. See
\code{.fetch_external()} (internal): this is enrichment
data, so failures do not stop the pipeline by default -- a UniProt ID
that cannot be resolved/queried is logged and dropped, not fatal.}
}
\value{
The updated \code{proj}, with a \code{targets_disease} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{compound_id}, \code{target_id} (the UniProt ID),
\code{disease_id} (the resolved EFO ID), \code{association_score}, \code{evidence}
(semicolon-separated \code{datatype=score} breakdown, or \code{NA} when Open
Targets has no evidence at all for the pair)), also written to
\code{results/targets_disease.csv}.
}
\description{
Looks up, for every distinct UniProt ID already present in
\code{patliRResults(proj, "targets_imported")}, its overall association score
to a single disease/phenotype in the Open Targets Platform (target-disease
association, aggregated across all of Open Targets' evidence types --
genetic association, known drug, literature, etc.). This is an optional,
enrichment-only step: nothing else in \code{patliR} requires it to run, but
\code{network_proximity()} (network family, not implemented yet) will require
its output once that function exists.

\code{targets_disease_filter()} never predicts targets itself and never touches
\code{\link[=compounds]{compounds()}} -- it only annotates the UniProt IDs already imported via
\code{\link[=targets_import]{targets_import()}}/\code{\link[=targets_import_batch]{targets_import_batch()}}. Run one of those first.
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- targets_import(
  proj,
  system.file("extdata", "import_targets", "Targets5280443.csv", package = "patliR"),
  platform = "superpred"
)
proj <- targets_disease_filter(proj, disease = "hypertension") # needs internet
patliRResults(proj, "targets_disease")
}

}
