% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets_disease.R
\name{targets_disease_profile}
\alias{targets_disease_profile}
\title{Organize every predicted target's Open Targets disease profile -- one
named disease, or an unfiltered top-N landscape per target}
\usage{
targets_disease_profile(
  proj,
  disease = NULL,
  top_n_diseases = 5,
  source = c("open_targets"),
  min_score = NULL,
  fetch_mode = c("warn_and_cache", "abort")
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{disease}{\code{NULL} (default, explore every target's top diseases) or a
single disease name/ID (same resolution as \code{\link[=targets_disease_filter]{targets_disease_filter()}}).}

\item{top_n_diseases}{Integer, default \code{5}. Only used in explore mode
(\code{disease = NULL}) -- how many top-scoring diseases to keep per target.}

\item{source}{Only \code{"open_targets"} is implemented. Kept as an explicit
argument (rather than hardcoding the source) so a future alternative
source raises an informative error instead of silently being ignored --
see \code{\link[=refdb_build]{refdb_build()}} for the same pattern with \code{"coconut"}.}

\item{min_score}{\code{NULL} (default): keep every UniProt ID that was
successfully queried, whether or not Open Targets reports an
association (rows with no association get \code{association_score = NA} and
are dropped, since there is nothing left to rank -- this is always
logged). A numeric threshold in \verb{[0, 1]}: additionally drop rows whose
\code{association_score} is below it (also always logged, by UniProt ID and
score, so nothing disappears silently).}

\item{fetch_mode}{\code{"warn_and_cache"} (default) or \code{"abort"}. See
\code{.fetch_external()} (internal): this is enrichment
data, so failures do not stop the pipeline by default -- a UniProt ID
that cannot be resolved/queried is logged and dropped, not fatal.}
}
\value{
The updated \code{proj}, with a \code{targets_disease_profile} entry in
\code{\link[=patliRResults]{patliRResults()}} (columns \code{compound_id}, \code{target_id} (UniProt),
\code{disease_id}, \code{disease_name}, \code{association_score}, \code{evidence}, \code{rank}
(1 = highest-scoring disease for that target; always \code{1} in
single-disease mode), \code{mode} (\code{"explore"} or \code{"single"})), also written
to \code{results/targets_disease_profile.csv}. Distinct from, and does not
modify, the \code{targets_disease} slot \code{\link[=targets_disease_filter]{targets_disease_filter()}} writes.
}
\description{
For every distinct UniProt ID in \code{patliRResults(proj, "targets_imported")},
queries the Open Targets Platform for its disease associations and
resolves each disease's \strong{display name} (not just its EFO ID -- a gap
\code{\link[=targets_disease_filter]{targets_disease_filter()}} has: it stores \code{disease_id} only). Two modes,
picked by whether \code{disease} is supplied:
\itemize{
\item \code{disease = NULL} (default, \strong{explore mode}): no disease is fixed in
advance. For each target, takes its top \code{top_n_diseases} associated
diseases (Open Targets' own score ordering), so you see the landscape
of diseases a compound's target set is actually implicated in, not just
one you already picked.
\item \code{disease = "<name or ID>"} (\strong{single-disease mode}, same resolution
rules as \code{\link[=targets_disease_filter]{targets_disease_filter()}}'s \code{disease} argument): every target
is queried against that one disease only (\code{top_n_diseases} is ignored),
same shape as \code{\link[=targets_disease_filter]{targets_disease_filter()}} but with the name resolved too.
}

Companion to \code{\link[=plot_disease_network]{plot_disease_network()}}, which reads this table's output to
draw the compound-target-disease network. This function does not touch
\code{\link[=network_proximity]{network_proximity()}}/\code{\link[=network_synergy]{network_synergy()}} or their prerequisites at all --
it is a separate, purely descriptive/organizational view, not a disease
module (see \code{\link[=disease_genes_fetch]{disease_genes_fetch()}} for that, and the circularity note on
\code{\link[=targets_disease_filter]{targets_disease_filter()}} -- it applies here identically).
}
\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- targets_import(
  proj,
  system.file("extdata", "import_targets", "Targets5280443.csv", package = "patliR"),
  platform = "superpred"
)
proj <- targets_disease_profile(proj, top_n_diseases = 5) # needs internet
patliRResults(proj, "targets_disease_profile")
}

}
\seealso{
\code{\link[=plot_disease_network]{plot_disease_network()}}, \code{\link[=targets_disease_filter]{targets_disease_filter()}}
}
