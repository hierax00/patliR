% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets_import.R
\name{targets_import_batch}
\alias{targets_import_batch}
\title{Import a whole folder of per-compound target-prediction exports}
\usage{
targets_import_batch(
  proj,
  dir,
  platform = c("swisstargetprediction", "superpred", "other"),
  target_col = NULL,
  probability_col = NULL,
  confidence_col = NULL
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{dir}{Directory containing \verb{Targets<pubchem_id>.csv} files.}

\item{platform}{\code{"swisstargetprediction"}, \code{"superpred"}, or \code{"other"}.
\code{"superpred"} has a built-in default for \code{target_col}/\code{probability_col}/
\code{confidence_col} (confirmed against a real SuperPred export: \code{"UniProt ID"}, \code{"Probability"}, \code{"Model accuracy"}) which is used whenever you
don't pass those arguments explicitly. \code{"swisstargetprediction"} and
\code{"other"} have no default -- \code{target_col} and \code{probability_col} are
always required for them.}

\item{target_col}{Name of the column holding the UniProt ID in \code{path}.
Always required unless \code{platform = "superpred"} supplies the default.
Matched case/whitespace/decoration-insensitively against \code{path}'s
actual columns (e.g. \code{"*Probability"} still matches \code{"Probability"}),
so a cosmetic export difference between runs of the same platform
does not abort the import.}

\item{probability_col}{Name of the column holding the prediction
probability/score. Always required unless \code{platform = "superpred"}
supplies the default. Parsed tolerant of either convention real
platforms use -- a plain \verb{0..1} fraction, or a \code{"96.55\%"} percentage
string -- and always stored as a \verb{0..1} fraction in the result (see
\code{.parse_percent_column()}, internal).}

\item{confidence_col}{Name of an optional model-confidence/accuracy
column, or \code{NULL} if the platform does not provide one. Same
percentage/fraction handling as \code{probability_col}.}
}
\value{
The updated \code{proj} (same \code{targets_imported} slot as
\code{\link[=targets_import]{targets_import()}}, accumulated across every file in \code{dir} that
imported successfully), plus a \code{targets_import_batch_log} entry in
\code{\link[=patliRResults]{patliRResults()}} (also written to
\code{results/targets_import_batch_log.csv}) with \strong{one row per file found
in \code{dir}} -- columns \code{path}, \code{pubchem_id}, \code{compound_id},
\code{compound_name} (the last two resolved from \code{\link[=compounds]{compounds()}} whenever
\code{pubchem_id} matches, so an excluded file is identifiable by name, not
just by CID), \code{ok}, \code{reason} (\code{"imported"}, \code{"skipped_naming"} -- file
name does not match \verb{^Targets\\\\d+\\\\.csv$}, or \code{"import_failed"} --
matched the naming pattern but \code{\link[=targets_import]{targets_import()}} errored on it), and
\code{message} (\code{NA} when \code{ok}, the real error text otherwise). Every file
is logged (\code{"targets_import_batch_skipped"}/
\code{"targets_import_batch_excluded"}), not silently ignored.
}
\description{
Convenience wrapper around \code{\link[=targets_import]{targets_import()}} for the common case: a
folder full of \verb{Targets<pubchem_id>.csv} files (one per compound,
downloaded one at a time from a platform like SuperPred). Reads every
file matching that naming pattern, calls \code{\link[=targets_import]{targets_import()}} on each, and
returns everything unified into \code{proj}'s single \code{targets_imported} table.
}
\section{One bad file never loses the rest of the batch}{

Real exports are not always uniform -- a platform can change its export
columns between compounds/sessions (e.g. a file with only \code{"PDB Visualization"}/\code{"TTD ID"} and no \code{"Probability"} column at all, seen
against real Chilcuague data). \code{\link[=targets_import]{targets_import()}} itself still aborts
loudly on a single direct call (a bad file is a real problem to
surface immediately when you only import one), but here each file goes
through its own \code{tryCatch()} -- same per-item pattern already used by
\code{\link[=network_pathview]{network_pathview()}} for per-pathway rendering failures -- so one file
failing (missing/renamed columns, unparseable content, ...) is logged
and excluded, never crashes the whole folder and loses every file
already imported before it.
}

\examples{
\dontrun{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- targets_import_batch(
  proj,
  system.file("extdata", "import_targets", package = "patliR"),
  platform = "superpred"
)
patliRResults(proj, "targets_imported")
patliRResults(proj, "targets_import_batch_log")
}

}
