% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tox_import.R
\name{tox_export_smiles}
\alias{tox_export_smiles}
\title{Export compounds as a plain SMILES list, ready to paste into a toxicity
platform}
\usage{
tox_export_smiles(proj, compound_ids = NULL, out_file = NULL)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{compound_ids}{Character vector of \code{compounds(proj)$id}, or \code{NULL}
(default) for every compound currently in \code{\link[=compounds]{compounds()}}.}

\item{out_file}{Character scalar, base path to write to (e.g.
\code{"chilcuague_tox_smiles"}), or \code{NULL} (default) to only return the
result. When given, writes \verb{<out_file>.txt} (the plain SMILES list)
and \verb{<out_file>_map.csv} (columns \code{row_order}, \code{compound_id}, \code{name},
\code{smiles}).}
}
\value{
Invisibly, \code{list(smiles_text, mapping)} -- see
\code{\link[=adme_export_smiles]{adme_export_smiles()}}, which shares its implementation.
}
\description{
The toxicity-side counterpart of \code{\link[=adme_export_smiles]{adme_export_smiles()}}. Platforms like
ADMETlab or pkCSM take their input as a pasted list of SMILES, one per
line, and return their output in that same order with no compound id of
their own. This writes that list plus a companion mapping file so the
export can be reconciled back to \code{compound_id} by row position --
pass the mapping file to \code{\link[=tox_import]{tox_import()}}\code{(mapping_file = ...)}, which is
more reliable than matching on SMILES (a third-party toolkit's canonical
SMILES need not match patliR's; see \code{DESIGN.md}).

Round trip: \code{tox_export_smiles()} -> paste into the platform -> download
its export -> \code{tox_import(..., mapping_file = "<out_file>_map.csv")}.
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
export <- tox_export_smiles(proj)
cat(export$smiles_text)
export$mapping

}
