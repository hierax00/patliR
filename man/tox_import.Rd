% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tox_import.R
\name{tox_import}
\alias{tox_import}
\title{Import toxicity properties exported from an external platform}
\usage{
tox_import(
  proj,
  path,
  platform = c("admetlab", "swissadme", "other"),
  column_map = NULL
)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}

\item{path}{Path to the CSV file to import.}

\item{platform}{\code{"swissadme"}, \code{"admetlab"}, or \code{"other"}. Selects a
built-in \code{column_map} (see Details) so you do not have to write one by
hand for a supported platform's default export headers.}

\item{column_map}{Named character vector mapping \emph{your} file's column
names to patliR's property names, e.g.
\code{c(MW = "mw", Consensus.Log.Po.w = "logp")}. Required when
\code{platform = "other"}; ignored (a preset is used instead) for
\code{"swissadme"}/\code{"admetlab"} unless you explicitly pass one, in which
case yours takes precedence.}
}
\value{
The updated \code{proj}, with a \code{tox_imported} entry in
\code{\link[=patliRResults]{patliRResults()}} in long format (columns \code{compound_id}, \code{property},
\code{value}, \code{source}, \code{import_date}), also written to
\code{results/tox_imported.csv}. Rows that could not be matched to a known
compound are logged (\code{"tox_import_unmatched"}) and excluded.
}
\description{
Imports a CSV exported from a third-party toxicity platform (SwissADME,
ADMETlab, pkCSM, ...) -- hERG liability, hepatotoxicity (DILI), Ames
mutagenicity, and similar model-derived predictions that are not
calculable locally in R (see \code{\link[=tox_local]{tox_local()}} for what \emph{is} local: PAINS/
Brenk structural alerts). Same import philosophy as \code{\link[=adme_import]{adme_import()}}:
\code{patliR} never scrapes these platforms, you download the CSV yourself,
and this function reconciles it against \code{\link[=compounds]{compounds()}}.
}
\details{
Built-in presets (confirmed against the column headers the bundled
example file actually uses):
\itemize{
\item \code{platform = "admetlab"}: \code{smiles} (canonicalized the same way
\code{\link[=prep_compounds]{prep_compounds()}} does, matched against
\code{compounds(proj)$canonical_smiles}), \code{Ames}, \code{DILI}, \code{hERG} (all
mapped to lowercase property names).
\item \code{platform = "swissadme"}: no built-in preset -- the bundled
\code{import_adme_swissadme.csv} example only carries ADME properties, not
toxicity ones; pass your own \code{column_map} if your SwissADME export
includes toxicity-relevant columns.
}
}
\examples{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- tox_import(
  proj,
  system.file("extdata", "import_tox_admetlab.csv", package = "patliR"),
  platform = "admetlab"
)
patliRResults(proj, "tox_imported")

}
