% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tox_report.R
\name{tox_report}
\alias{tox_report}
\title{Summarize toxicity results -- never a pass/fail verdict}
\usage{
tox_report(proj)
}
\arguments{
\item{proj}{A \code{\link[=PatliRProject-class]{PatliRProject}} object.}
}
\value{
A \code{list} with two elements:
\itemize{
\item \code{summary}: a \code{data.frame} (one row per compound with any
\code{\link[=tox_local]{tox_local()}}, \code{\link[=tox_safetyome]{tox_safetyome()}}, and/or \code{\link[=tox_import]{tox_import()}} results),
columns \code{compound_id}, \code{n_pains_alerts}, \code{pains_alert_names}
(comma-separated), \code{n_brenk_alerts}, \code{brenk_alert_names}
(comma-separated), \code{n_safetyome_hits},
\code{safetyome_organ_systems} (comma-separated, deduplicated),
\code{n_imported_properties}, \code{imported_properties} (comma-separated
\code{property=value} pairs).
\item \code{note}: the fixed disclaimer string, always identical
regardless of the data (\code{"A toxicity/structural-alert flag can reflect real pharmacological activity, not just risk -- this is not a pass/fail verdict. Review each flagged compound individually."}).
}
Nothing is written to \code{proj} or to disk -- this is a read-only summary
over existing \code{\link[=patliRResults]{patliRResults()}}.
}
\description{
Builds a per-compound summary combining \code{\link[=tox_local]{tox_local()}}'s structural alerts
(PAINS/Brenk), \code{\link[=tox_safetyome]{tox_safetyome()}}'s target-level systemic-risk flags, and
\code{\link[=tox_import]{tox_import()}}'s imported properties (hERG, DILI, Ames, ...), when any is
available. This function \strong{never} collapses its output into a single
pass/fail flag, and always attaches a fixed disclaimer -- see the
Description of \code{\link[=tox_local]{tox_local()}} for why: many compounds trip these filters
because of genuine pharmacological activity, not because they are
unsafe.
}
\examples{
\donttest{
proj <- patliR_project(tempfile("patliR_demo_"))
compound_list <- read.csv(
  system.file("extdata", "input_compound_list.csv", package = "patliR")
)
proj <- prep_compounds(proj, compound_list, identifier = "pubchem")
proj <- tox_local(proj, alert_sets = c("pains", "brenk"))
report <- tox_report(proj)
report$summary
report$note
}

}
